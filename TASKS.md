# TASKS

本任务队列根据 `README.md`、需求分析文档 V0.2、架构设计文档 V0.1 和当前代码状态生成。后续 Codex 每轮只应完成一个未完成且未阻塞的最小任务。

## 临时任务 - 2026-06-04 worker 任务异步执行与 timeout

- [x] 修复 worker 单类任务同步阻塞主循环的问题
  - Done when:
    - worker 每轮并发调度已注册的邮件、识别和导出 processor
    - 单类 processor 卡住时不会阻塞其他 processor 完成
    - 单类 processor 超过配置 timeout 后记录超时并让主循环继续
    - 上一轮同类 processor 仍在运行时不会重复启动，避免同类任务堆叠
    - timeout 可通过环境变量配置，并有回归测试覆盖

## 临时任务 - 2026-06-04 管理员成员提醒邮件发送

- [x] 为成员提醒增加多选成员、可编辑邮件内容，并发送到成员 primary 邮箱
  - Done when:
    - 管理员成员提醒页可一次选择多个任务成员
    - 默认邮件正文包含自动化邮件、无需回复、任务、提醒对象和提醒内容，可在发送前编辑
    - 后端向每个成员最早绑定的邮箱发送提醒邮件，并在提醒记录中保存收件邮箱、主题、正文、发送状态和失败原因
    - 成员没有 primary 邮箱或 SMTP 不可用时，不伪装成功，页面可看到失败原因
    - 相关前后端测试、迁移验证和仓库级验证通过

## 临时任务 - 2026-05-05 管理员材料审核页复用成员材料简介

- [x] 修复管理员材料审核页非发票材料简介，复用成员材料查看页的通用材料简介口径
  - Done when:
    - 管理员材料审核页中的非发票材料不再展示“未形成主发票”这类发票专用简介
    - 管理员材料选择器和详情头部复用成员材料查看页的材料简介文案
    - 管理员材料审核页原有布局与交互样式保持不变
    - 相关前端测试与仓库级验证通过

- [x] 修正成员费用确认页“本人费用”口径，并收口管理员/其他成员对未提交发票的读取范围
  - Done when:
    - 成员费用确认页“本人费用”明确展示为分摊到当前成员名下的金额，费用条数单独展示
    - 其他成员不能通过共享发票或费用明细看到未提交发票
    - 管理员不能通过复核摘要和就绪度等只读聚合看到未提交发票
    - 相关前后端测试与仓库级验证通过

## 临时任务 - 2026-05-05 修复成员兼任务管理员时的费用口径串视角

- [x] 修复成员账号同时出现在任务 `administrator_ids` 时，成员页被误按管理员视角计算的问题
  - Done when:
    - 成员会话下，即使当前 actor 同时也是任务管理员，`expense-details`、`member-status`、`member-workbench`、`shared-invoices` 仍按成员视角取数
    - 管理员只读聚合仍保持管理员只看已提交发票的口径
    - 有回归测试覆盖“成员同时也是任务管理员且存在未提交发票”场景
    - 相关后端测试与仓库级验证通过

## 临时任务 - 2026-05-05 成员工作台首屏并行预取

- [x] 在已知 `taskId` 的成员工作台首屏并行预取任务聚合数据，减少跨地域串行往返
  - Done when:
    - 成员打开带 `taskId` 的工作台路由时，不再等待 `/api/tasks` 返回后才发起 `/api/tasks/{task_id}/member-workbench`
    - 任务仍需通过 `/api/tasks` 的可见性结果决定最终选中任务，不绕过前端现有可见任务收口
    - 聚合接口预取失败时，现有聚合接口直连与 legacy fallback 路径不回归
    - 相关前端测试与仓库级验证通过

## 临时任务 - 2026-05-05 收口成员发票提交可见性

- [x] 修复成员页本人费用和共享发票可见性，未提交发票不对其他成员或管理员暴露
  - Done when:
    - 成员本人费用不再计入其他成员未提交发票上的分摊金额
    - 其他成员只能看到已提交发票，不能看到未提交发票
    - 管理员通过共享发票和费用明细口径只能看到已提交发票
    - 其他成员点击已提交共享发票时复用成员发票详情路由并保持只读
    - 相关后端与前端测试、仓库级验证通过

## 临时任务 - 2026-05-04 修复任务详情编辑成员无实时搜索

- [x] 管理员编辑草稿任务成员时复用实时成员搜索
  - Done when:
    - 管理员任务详情页编辑成员名单时会调用成员候选搜索接口
    - 候选成员可按姓名、用户名或学号实时检索并加入任务
    - 已选成员仍可展示和删除
    - 已有任务响应缺少 `member_summaries` 时页面不会崩溃
    - 相关前端测试和仓库级验证通过

## 临时任务 - 2026-05-04 支持批量新增与批量确认纸质发票

- [x] 允许成员一次性新增多张相同纸质发票，并允许管理员一次性确认这些纸票收票
  - Done when:
    - 成员手动录入纸质发票时可填写张数，同一金额和费用类型会生成多张独立纸质发票记录
    - 每张批量生成的纸质发票都有独立发票号、占位材料、默认本人全额分摊和纸票收票校验
    - 管理员复核总览可一次性确认当前待收票纸质发票
    - 批量确认会逐张刷新纸票收票校验，并保留原单张确认能力
    - 相关前后端测试和仓库级验证通过

## 临时任务 - 2026-05-04 邮件提交说明补充邮箱绑定前置条件

- [x] 在任务页邮件提交说明中提示发送前必须先绑定邮箱
  - Done when:
    - 管理员任务详情页和成员任务工作台的邮件提交说明都提示“先在个人信息页绑定发件邮箱”
    - 邮件提交说明提供个人信息页入口
    - 未配置邮件提交入口时，也不误导用户可直接发邮件
    - 相关前端测试和仓库级验证通过

## 临时任务 - 2026-05-04 任务页展示材料提交说明

- [x] 在任务页简短展示网页、邮件和 Telegram Bot 材料提交说明
  - Done when:
    - 成员任务工作台和管理员任务详情页都能基于任务 `submission_key` 展示提交说明
    - 邮件说明展示实际收件地址，并明确主题必须以 `<submission_key>` 开头和 `.eml` 邮件包导入边界
    - Telegram 说明展示实际 Bot 入口，并明确 `/bind`、`/task <submission_key>` 和直接发文件流程
    - 邮件收件地址与 Telegram Bot URL 来自运行配置，不在前端写死
    - 相关前后端测试和仓库级验证通过

## 临时任务 - 2026-05-04 修复 actor_id / member_code 混用导致任务列表 500

- [x] 修复成员标识使用 `actor_id` 时任务列表成员摘要排序崩溃的问题
  - Done when:
    - `list_users_by_member_identifiers()` 不再在 `actor_id` 命中、`member_code` 不命中的情况下对不存在的标识做排序索引
    - 任务 `member_summaries` 在 `member_ids` 使用 `actor_id` 或 `member_code` 时都能正确回填用户名、显示名和学号
    - 有回归测试覆盖 `actor_id != member_code` 的成员访问 `/api/tasks` 场景
    - 仓库级验证通过

## 临时任务 - 2026-05-04 Compose 透传剩余 IMAP 环境变量

- [x] 修复生产 Compose 未向容器透传 `TRMS_IMAP_*` 配置的问题
  - Done when:
    - 已确认 `runtime_config.py` 仍未被 Compose 透传的变量只剩 `TRMS_IMAP_*`
    - `deploy/docker-compose.yml` 中相关应用容器显式继承 `TRMS_IMAP_HOST`、`TRMS_IMAP_PORT`、`TRMS_IMAP_USERNAME`、`TRMS_IMAP_PASSWORD`、`TRMS_IMAP_MAILBOX`、`TRMS_IMAP_POLL_INTERVAL_SECONDS`、`TRMS_IMAP_USE_SSL`、`TRMS_IMAP_STARTTLS`
    - 部署文档明确说明 IMAP 轮询只有在 worker 容器实际拿到这些变量后才会启用
    - 仓库级验证通过

## 临时任务 - 2026-05-04 收口 Compose 应用容器环境变量透传根因

- [x] 修复生产 Compose 通过手工白名单透传环境变量导致持续漏传的问题
  - Done when:
    - `migrate`、`api`、`worker` 不再手工枚举后端运行时环境变量，而是统一继承同一份 env 文件
    - 生产启停脚本、验证脚本和备份演练脚本都会把实际使用的 env 文件路径同步传给 Compose 内的 `env_file`
    - 生产部署文档和 README 明确说明后端容器默认继承根目录 `.env`，以及覆盖 env 文件时的边界
    - 仓库级验证通过

## 临时任务 - 2026-05-04 Compose 透传 SMTP 环境变量到生产容器

- [x] 修复生产 Compose 未向应用容器透传 `TRMS_SMTP_*` 配置的问题
  - Done when:
    - `deploy/docker-compose.yml` 中 `api` 与 `worker` 明确继承 `TRMS_SMTP_HOST`、`TRMS_SMTP_PORT`、`TRMS_SMTP_USERNAME`、`TRMS_SMTP_PASSWORD`、`TRMS_SMTP_FROM_ADDRESS`、`TRMS_SMTP_STARTTLS`、`TRMS_SMTP_USE_SSL`、`TRMS_SMTP_TIMEOUT_SECONDS`
    - 生产部署文档明确说明：仅修改 `.env` 不够，容器必须拿到这些环境变量后邮件验证码/回执能力才会可用
    - 仓库级验证通过

## 临时任务 - 2026-05-04 生产脚本增加 build / deploy 命令

- [x] 为生产启停脚本增加 `build` 和 `deploy` 命令
  - Done when:
    - `./scripts/trms-prod.sh build` 会按当前 Compose 基线重建 `migrate`、`api`、`worker`、`web`
    - `./scripts/trms-prod.sh deploy` 会先执行 `build`，再执行当前 `start` 流程
    - README 与生产部署文档同步说明新命令和“源码更新后需 rebuild”边界
    - 仓库级验证通过

## 临时任务 - 2026-05-04 生产环境注册邮箱 host 白名单与验证码闭环

- [x] 为生产环境增加可配置的注册邮箱 host 白名单、注册验证码和自动邮箱绑定
  - Done when:
    - 系统管理员可在系统管理页查看和更新允许注册的邮箱 host 列表，初始化默认空列表
    - `TRMS_ENV=production` 下，自助注册只允许使用白名单 host 邮箱，且必须先通过邮箱验证码验证
    - 注册成功后，注册邮箱会自动写入系统邮箱绑定；同一邮箱不能被两个成员重复绑定
    - 首个系统管理员仍通过受控 `bootstrap-admin` 入口创建，不受“默认禁止自助注册”策略阻塞
    - 相关前后端测试与仓库级验证通过

## 临时任务 - 2026-05-04 删除 Compose 内置反代并改为外部 Caddy 接入

- [x] 删除 Docker Compose 中的 `reverse-proxy`，改为直接暴露 `api` / `web` 到宿主机回环端口
  - Done when:
    - `deploy/docker-compose.yml` 不再包含 `reverse-proxy` 服务
    - `api` 与 `web` 会直接绑定到宿主机 `127.0.0.1` 端口供外部 Caddy 反代
    - `.env.example` 与部署文档同步改为“由宿主机反向代理统一接入”的基线
    - 仓库级验证通过

## 临时任务 - 2026-05-04 增加生产环境统一启停脚本

- [x] 提供基于当前 Docker Compose 基线的生产环境启动/停止脚本
  - Done when:
    - 仓库新增统一生产启停脚本，封装当前生产 Compose 基线的启动和停止流程
    - 脚本会校验 `.env` / `TRMS_ENV=production`，避免误用开发配置
    - README 与生产部署文档同步改为优先使用该脚本
    - 仓库级验证通过

## 临时任务 - 2026-05-04 Docker Compose 本地材料目录宿主持久化

- [x] 让 Docker Compose 在 local 存储模式下持久化 `MATERIAL_STORAGE_DIR`
  - Done when:
    - `deploy/docker-compose.yml` 会把 `MATERIAL_STORAGE_DIR` 传给 `migrate`、`api`、`worker`
    - `api` 与 `worker` 在 Compose 下会把宿主机 `MATERIAL_STORAGE_DIR` bind mount 到容器内同一路径
    - 根目录 `.env.example`、README 和生产部署文档同步说明该行为
    - 仓库级验证通过

## 临时任务 - 2026-05-03 Telegram 默认材料类型收口

- [x] 让 Telegram 和未显式指定类型的上传默认先落为其他材料，再由识别结果修正
  - Done when:
    - Telegram Bot 直接发送文件上传时，不再默认把材料写成 `invoice`
    - Telegram 入站接口在未显式提供 `material_type` 时，默认按 `other_attachment` 入链
    - 识别成功后，`other_attachment` 可继续自动修正为 `payment_record`、`invoice` 等具体类型
    - 已修正数据库中 `telegram-photo-AQADPBFrG6HduVd-.jpg` 被误落为 `invoice` 的历史记录，并补齐其支付记录关联
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 放开生产环境 local 存储限制

- [x] 允许生产环境显式配置本地文件存储
  - Done when:
    - `TRMS_ENV=production` + `TRMS_STORAGE_BACKEND=local` 不再因运行时配置校验直接失败
    - `MATERIAL_STORAGE_DIR` 在生产本地存储场景下继续生效
    - 相关运行时配置测试通过
    - README 与部署文档同步改为“生产可选 local 或 s3，Compose 基线默认 s3”

## 临时任务 - 2026-05-03 缺失材料提示去重与航空附加材料规则收口

- [x] 收口成员侧缺失材料重复提示，并改造航空交通附加材料校验
  - Done when:
    - 成员工作台、发票详情和材料详情中，同一条缺失材料不再同时按 `validation` 和 `missing_materials` 重复展示
    - 航空交通不再单独要求“行程单”作为阻塞缺失材料
    - 航空交通改为识别发票内是否存在乘客、航班号、日期、舱位；任一缺失时要求补订单截图，否则不要求
    - 该规则通过提示词直接让 LLM 判断字段是否存在，而不是继续依赖机场代码/行程单兜底
    - 缺失材料导出与成员侧缺失材料提示同步反映为 `order_screenshot`
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 成员发票详情页误分类材料删除收口

- [x] 让成员在发票详情路径下也能删除尚未形成发票实体的误分类材料
  - Done when:
    - `member/materials/:materialId/invoice` 路径下，若当前条目只有材料、尚未生成 `invoice` 实体，页面仍提供删除入口
    - 删除动作会走成员已有的材料删除接口，而不是错误地要求 `invoice.id`
    - 正常“删除未提交发票”路径保持不回归
    - 相关前端测试和仓库级验证通过

## 临时任务 - 2026-05-03 Telegram Bot 与 CLI 共享任务切换提交流程

- [x] 补齐 Telegram Bot 绑定、任务切换、直接上传，并让 CLI / Telegram 共享同一套提交语义
  - Done when:
    - Telegram Bot 提供 `/bind`、`/tasks`、`/task <submission_key>` 和直接发送文件上传
    - `/bind` 通过一次性网页授权链接完成 Telegram 账号绑定，不再要求命令参数用户名
    - Telegram 直接上传要求已绑定且已切换当前任务；任务不接受成员发票上传时会明确报错
    - Telegram 文件提交复用现有网页上传主链路，而不是独立旁路
    - CLI 与 Telegram 共用“任务列表 / 当前任务切换 / 文件提交”语义，CLI 可在本地会话中记住当前任务
    - 任务对外统一暴露 `submission_key` 命名，旧 `email_submission_key` 继续兼容
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 Telegram 开发轮询入口

- [x] 增加开发环境可直接启动的 Telegram long polling 入口
  - Done when:
    - 后端存在独立于 API / webhook 的 Telegram 轮询启动命令
    - 启动轮询前会移除 webhook，避免 Telegram 继续投递到 webhook
    - 相关后端测试覆盖命令入口
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 worker SQLite 锁库修复

- [x] 收口识别 worker 在线程并发下的 SQLite 锁库问题
  - Done when:
    - 默认 SQLite 数据库运行 `worker` 时，不再让识别线程池并发写库
    - SQLite 连接会显式配置更长的锁等待时间，减少短时竞争直接失败
    - 有回归测试覆盖 worker 并发降级与 SQLite 连接参数
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 管理员删除任务

- [x] 增加管理员删除任务功能，并在任意任务阶段可用
  - Done when:
    - 任务管理员可删除自己可见任务，不受 `draft/open/reviewing/ready_to_export/completed` 阶段限制
    - 删除任务会级联清理任务下材料、发票、附件关联、识别任务、分摊、确认、提醒和导出任务
    - 任务下原始材料文件和导出产物文件会一并从存储中删除
    - 管理员任务详情页提供删除任务入口，并有明确确认提示
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 邮件回执文件名与材料删除权限收口

- [x] 回执列出附件名，并收口成员/管理员删除材料权限
  - Done when:
    - 邮件回执正文会明确列出成功附件名和失败附件名，而不仅是数量
    - 成员对自己的未提交材料可删除；若是未提交发票，继续允许删除，并且不再因已挂未提交附件而被拦住
    - 成员删除材料的前提仍是任务处于 `open`，且材料未进入已提交链路
    - 管理员可在任意时刻删除任务内材料，包括主发票材料和已关联附件材料
    - 非发票材料详情页补齐成员删除入口，相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 邮件附件 octet-stream PDF 兼容与历史补导

- [x] 兼容邮件附件错误标注为 octet-stream 的 PDF，并修复历史漏导
  - Done when:
    - 邮件渠道解析附件时，若文件名可明确识别为 `.pdf/.zip/.png/.jpg/.jpeg/.webp/.eml`，不会因为 `application/octet-stream` 被误拒
    - 邮件 worker 导入原始邮件和 `.eml` 邮件包时，都能复用同一套 MIME 归一化逻辑
    - 普通 `/api/tasks/{task_id}/materials` 上传仍继续拒绝 `message/rfc822`
    - 已定位出历史漏导根因，并补导 `<ccny2026>` 转发邮件里当时漏掉的支持类型 PDF
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 邮件渠道 eml 解包导入

- [x] 仅在邮件渠道支持 eml 邮件包，并把其中附件作为真正材料上传
  - Done when:
    - 普通 `/api/tasks/{task_id}/materials` 上传继续拒绝 `message/rfc822`
    - `/api/email/materials` 接收 `.eml` / `message/rfc822` 文件时，会先解析邮件包并提取其中附件
    - 解析得到的内部附件而不是外层 `.eml` 本身进入统一材料上传链路
    - IMAP worker 导入邮件附件时，若附件本身是 `.eml` 邮件包，也会继续解包导入其中附件
    - API / worker / 非邮件渠道拒绝场景回归测试通过

## 临时任务 - 2026-05-03 邮件尖括号任务标识与自由正文收口

- [x] 让邮件提交只依赖尖括号任务标识，并取消正文格式要求
  - Done when:
    - 邮件主题只从开头尖括号中匹配任务邮件提交标识，例如 `<ccny2026>Fw: ...`
    - 邮件正文不再要求 `material_type`、`task_id` 或其他固定键值对
    - 邮件附件进入统一上传主链路时，与普通上传未显式选择类型时一致，默认按 `other_attachment` 入链
    - API / worker 回归测试覆盖尖括号主题、无格式正文和旧 `[TRMS] task:` 主题拒绝场景
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 邮件尖括号任务标识兼容

- [x] 允许邮件主题使用尖括号任务标识
  - Done when:
    - 邮件主题 `<ccny2026>Fw: ...` 能解析出任务邮件提交标识 `ccny2026`
    - 原有 `[TRMS] task:<task_key>` 主题格式继续兼容
    - 邮件入站仍使用已绑定发件人邮箱解析成员身份，不放宽成员绑定边界
    - API 回归测试覆盖尖括号任务标识主题
    - 相关测试和仓库级验证通过

## 临时任务 - 2026-05-03 前端个人信息邮箱入口与标签颜色修复

- [x] 补齐个人信息页成员邮箱绑定入口并修复状态标签文字颜色
  - Done when:
    - 成员个人信息页展示已绑定邮箱列表，并允许通过验证码继续绑定新的邮箱
    - 前端明确支持同一成员绑定多个邮箱，新绑定不会覆盖既有绑定
    - 有前端测试覆盖已有邮箱展示、新增邮箱绑定和非成员账号隐藏入口
    - MUI 状态标签在有背景色时文字不再被外层文本样式压成灰色
    - 相关前端测试、构建和仓库级验证通过

## 临时任务 - 2026-05-03 邮件收件闭环拆分

- [x] 补齐邮箱验证码绑定与可信发件人解析
  - Done when:
    - 成员账号可通过验证码绑定多个邮箱地址，且同一邮箱不能同时绑定给多个成员
    - 后端通过 SMTP 发送验证码邮件，验证码校验成功后持久化绑定关系
    - 格式化邮件入站在未显式提供受信任 `resolved_member_id` 时，也能根据已绑定发件人邮箱自动解析成员身份
    - 后端测试覆盖验证码请求、验证成功、冲突拒绝、越权拒绝和绑定邮箱后的邮件入站主路径

- [x] 为任务补齐管理员可指定的稳定邮件提交标识
  - Done when:
    - 管理员创建或编辑任务时可配置一个唯一、稳定、可读的邮件提交标识，例如 `wuhan`
    - 邮件接入不再依赖系统内部 UUID 作为用户侧主题前缀
    - 前后端测试覆盖唯一性校验、展示和旧任务兼容读取

- [x] 增加 IMAP 轮询读取已绑定邮箱并忽略未绑定发件人
  - Done when:
    - 后端可按配置周期轮询指定 IMAP 邮箱的新邮件
    - 发件人邮箱未绑定成员身份时，邮件会被显式忽略而不是进入成员主链路
    - 已绑定邮箱但主题不满足任务标识规则时，会记录稳定失败原因或忽略原因
    - 测试覆盖轮询去重、未绑定忽略和主题前缀解析

- [x] 将邮件附件接入统一上传链路并通过 SMTP 回复处理结果
  - Done when:
    - 已绑定邮箱且主题命中任务标识的邮件，其附件会按现有网页上传主链路写入目标任务
    - 上传结果、识别/校验触发和权限边界与网页上传保持一致
    - 系统会通过 SMTP 自动回复“已收到/部分失败/失败原因”等处理结果
    - 测试覆盖单附件、多附件、部分成功和自动回执主路径

## 临时任务 - 2026-05-03 原始材料导出归档

- [x] 导出任务增加全部原始材料压缩包
  - Done when:
    - 导出能力列表新增“全部原始材料压缩包”独立导出任务，管理员可从导出页创建 ZIP 导出
    - ZIP 会收集当前任务下全部材料文件，并尽量保留原始文件名；若存在重名文件，会自动追加顺序后缀避免覆盖
    - 后端异步导出、前端导出页和相关回归测试通过，且仓库级验证通过

## 临时任务 - 2026-05-03 前端 Material 3 收口

- [x] review 当前前端仍未切换到 Material 3 的部件并统一修正
  - Done when:
    - 前端主路径中不再保留原生按钮、伪按钮链接和旧 `section/article` 自造卡片作为主要交互部件
    - 发票摘要行、附件入口和管理员/成员主路径的主要摘要卡统一改为基于 MUI Material 3 的 `ButtonBase/Card/Chip`
    - 相关前端测试与仓库级验证通过，且未新增 lint error

## 临时任务 - 2026-05-03 系统时区展示修复

- [x] 读取环境变量作为系统时区，并在前端展示浏览器提供的时区值
  - Done when:
    - 后端运行时摘要会读取 `TZ` 环境变量，并把系统时区返回给前端
    - 系统管理员页面会同时展示系统时区和浏览器提供的时区值
    - 前后端测试覆盖运行时摘要和系统管理页展示

## 临时任务 - 2026-05-03 前端文案收口

- [x] review 当前前端用户不友好的文案并统一收口
  - Done when:
    - 成员与管理员主路径中明显暴露实现细节、命令式或不友好的文案改为业务语言
    - 相关前端测试同步更新，不保留旧文案断言
    - 仓库级验证通过，且未引入新的 lint error

## 临时任务 - 2026-05-03 导出合并顺序优化

- [x] 让附件跟随发票排序，并尽量聚拢共享附件关联发票
  - Done when:
    - 单发票附件在 merged PDF / 完整材料包中默认排到对应发票后面，而不是统一堆到所有发票之后
    - 若同一附件关联多张发票，排序会尽量把这些发票排成连续块，再把共享附件放到最后一个相关发票后面
    - 回归测试覆盖单发票附件跟随、共享附件聚拢发票和完整材料包实际 PDF 顺序

## 临时任务 - 2026-05-03 当前实例导出失败修复

- [x] 修复纸质发票占位材料导致完整材料包生成失败
  - Done when:
    - 手动新增的纸质发票占位材料不再让 `reimbursement_package` 或 `merged_pdf` 因 `text/plain` 而直接失败
    - 合并 PDF 预览和完整材料包仍保留该纸票的可追踪占位信息，而不是静默丢失
    - 回归测试覆盖纸质发票占位 + 正常电子材料并存时的预览和完整材料包导出

任务覆盖范围：

- FR-001 至 FR-015。
- 需求文档第 7 至 16 节中的 CLI、非功能、数据、权限、异常场景、优先级和第一阶段交付物。
- Web 前端成员提交入口、管理员后台、复核与导出操作界面。
- 架构文档中的模块边界、安全审计、可观测性、测试策略和验收映射。
- FR-011 财务系统 Browser Use 自动录入属于第一阶段 Won't have，只保留边界说明任务，不实现自动录入。
- 生产上线前必须补齐运行配置、端口配置、OpenAI 兼容 LLM Provider 配置、真实识别流水线、部署、迁移、对象存储、权限审计和备份恢复边界。

补充说明：

- 2026-04-28 需求文档对照 review 已发现高优先级偏差：匿名可读任务/材料数据、成员侧接口仍允许匿名自报 `actor_id`、Telegram/邮件渠道身份可伪造、扫描 PDF/图片 OCR 缺失、真实合并 PDF 导出缺失。
- 用户新增产品要求：同一比赛任务内的成员之间应可互相查看当前已上传发票。该要求与需求分析文档 V0.2 中“成员只能查看本人相关费用和材料状态”的旧表述冲突；后续任务按需求变更处理，并要求在 `WORKLOG.md` 中持续记录影响面。

## 临时任务 - 2026-05-01 新一轮产品反馈收口

- [x] 收口管理员任务详情页的就绪度误报与暖色背景
  - Done when:
    - 任务详情页“待关联附件”不再把已存在有效关联的辅助材料重复计成待处理
    - 任务详情页“导出阻塞”不再把“尚未进入可导出阶段”这种正常流程状态计入异常/待处理数量
    - 任务详情页与管理员首页截图中的暖黄色大块背景改回与后台主视觉一致的冷白/浅蓝色系
    - 后端测试覆盖 readiness/review 聚合口径回归，前端测试覆盖任务详情页阶段提示与主渲染不回归

- [x] 收口管理员审核风险与就绪度，避免把非发票待确认误标成重点风险
  - Done when:
    - 支付记录、比赛通知、订单截图、行程单等非发票材料即使识别状态为 `needs_confirmation`，管理员“审核风险”和“任务就绪度”也不再把它们计入“识别待人工确认/低置信待确认”
    - 发票材料的 `needs_confirmation` 仍继续计入管理员风险摘要，不放宽发票主审核链路
    - 后端测试覆盖 `review-summary` 与 `readiness` 两个聚合接口的非发票 `needs_confirmation` 不计数回归

- [x] 收口成员工作状态摘要，避免把非发票待确认误显示为仍待处理
  - Done when:
    - 非发票辅助材料即使识别状态为 `needs_confirmation`，只要工作台分组已判定为 `ready`，成员工作状态页不再继续显示“先核对识别结果”
    - 成员工作台顶部“待处理事项”统计与左侧“工作状态”导航摘要改为使用当前工作台真实分组口径，而不是旧 `report.counts` 混合口径
    - 前端测试覆盖“非发票 `needs_confirmation` 仍在就绪区时，工作状态显示当前无明显异常”

- [x] 让成员材料详情按识别类型进入独立前端页面
  - Done when:
    - 成员点击 `payment_record`、`competition_notice`、`itinerary`、`order_screenshot`、`other_attachment` 材料时，不再进入发票特有页面
    - 非发票材料拥有各自独立的详情页标题、说明和字段区，只展示该类型相关的识别字段与处理动作
    - 发票材料和“已识别为发票但尚未成票”的材料仍进入现有发票处理页，不破坏补录发票字段、分摊和重新识别主路径
    - 工作台相关入口和前端测试覆盖发票/非发票材料分流

- [x] 分析市内交通费用材料归票策略，并让行程单优先自动归属到对应电子发票
  - Done when:
    - 已基于当前识别输出、附件自动归票和真实市内交通材料约束，形成一份明确的归票策略
    - 已说明“电子发票 + 行程单”自动归属的证据优先级、冲突处理和误绑防护边界
    - 若本轮直接实现，则相关后端规则、识别提示词和测试同步更新；若暂不实现，则策略已写入文档或 `WORKLOG.md`

- [x] 允许发票部分报销，并把“分摊金额之和必须等于发票金额”改为确认提示
  - Done when:
    - 用户保存分摊时，不再被强制要求分摊金额之和等于发票金额
    - 当分摊金额之和不等于发票金额时，前端会弹出明确确认提示，说明存在未报销金额或超额风险
    - 确认后的保存、取消后的不保存和相关测试均覆盖

- [x] 支持用公对公转账编号代替支付记录
  - Done when:
    - 用户可为需要支付记录的发票填写公对公转账编号
    - 已填写合法公对公转账编号时，可免提交支付记录材料
    - 校验规则、管理员查看口径和相关测试覆盖“支付记录”与“转账编号替代”两条路径

- [x] 添加纸质发票录入与管理员收票确认
  - Done when:
    - 成员可手动新增“纸质发票”记录，并填写金额、费用类型等必要字段
    - 纸质发票在管理员确认“已收到纸票”前，任务不能按已齐备推进
    - 前后端测试覆盖新增、查看、管理员确认收票和推进门禁

- [x] 支持同一材料作为多张发票的附件
  - Done when:
    - 同一份材料可被关联到多张发票，不再限制为单张归属
    - 相关 API、读模型、校验和前端展示同步支持多对多附件关系
    - 测试覆盖新增多发票附件、解除其中一条关联和校验刷新

- [x] 收口附件归属选择，改为统一下拉归属发票
  - Done when:
    - 用户为材料选择附件归属时，不再看到“每张发票重复展示一次”的列表
    - 改为统一下拉或等价单选控件，候选项至少展示发票编号、金额、原始文件名
    - 前端测试覆盖候选展示、选择、保存和空候选场景

- [x] 管理员选择比赛参与成员时提供搜索框
  - Done when:
    - 管理员选择比赛参与成员的界面提供搜索输入
    - 输入关键字时按字符串匹配过滤成员列表，并实时显示当前匹配结果
    - 前端测试覆盖空查询、部分匹配和无结果提示

- [x] 去掉成员编号展示，仅保留用户名、显示名称和学号
  - Done when:
    - 前端成员相关展示默认不再显示意义不明的成员编号
    - 成员字段统一保留用户名、显示名称和学号
    - 管理员与成员主路径相关测试同步更新，避免残留旧展示

## 临时任务 - 2026-05-02 注册页简化

- [x] 注册时去除身份编号，仅在成员注册时显示学号
  - Done when:
    - 注册表单不再展示“身份编号”输入
    - 成员角色注册时显示“学号”输入
    - 管理员和系统管理员注册时不显示“学号”输入
    - 前端测试覆盖成员/管理员切换时的字段可见性

## 临时任务 - 2026-05-02 管理员界面重构

- [x] 精简管理员导航壳层并统一文案
  - Done when:
    - 左侧导航去掉"导航骨架"标签和冗长描述，改为紧凑图标+标题模式
    - "当前任务上下文"卡片改为轻量摘要条，去掉冗余说明文案
    - 所有英文技术标签替换为中文业务语言
    - 前端测试覆盖导航渲染和文案变更

- [x] 重构管理员首页为任务驱动仪表板
  - Done when:
    - 统计卡片改为紧凑排列
    - 优先任务推荐区文案和样式参照成员端
    - 筛选区精简为内联搜索+下拉
    - 任务列表表格行样式更紧凑
    - 前端测试覆盖首页渲染

- [x] 重构管理员任务详情页
  - Done when:
    - 任务基础信息改为紧凑摘要（参照成员端比赛信息网格）
    - 就绪度指标去掉英文标签，改为中文分组展示
    - 导出阻塞原因改为列表式展示
    - 前端测试覆盖任务详情渲染

- [x] 优化材料审核和成员提醒页面展示
  - Done when:
    - 材料审核列表使用统一摘要样式
    - 去除冗余识别字段展示
    - 成员提醒页统一卡片样式
    - 前端测试覆盖审核和提醒页渲染

- [x] 把管理员发票查看页左侧改成受控下拉发票选择
  - Done when:
    - 左侧不再直接铺开发票或材料列表，而是使用点击后展开的下拉选择控件
    - 下拉层高度受控，并在下拉层内部滚动查看候选发票
    - 管理员材料审核页、分摊编辑页和成员提醒页左侧候选区统一改成该交互
    - 每个候选项至少展示发票号、原始文件名、类型、金额、是否校验通过
    - 前端测试覆盖默认摘要展示与下拉候选展开

- [x] 收口成员提醒为纯提醒页，并删除 missing-materials 独立入口
  - Done when:
    - 管理员任务上下文导航不再展示“分摊确认”，只保留“材料审核”“成员提醒”和独立“导出打印”
    - 成员提醒页只保留选择成员、填写提醒内容和查看已记录提醒，不再展示任何发票列表或更正入口
    - 管理员 `missing-materials` 独立路由和相关入口删除，原入口统一回到材料审核页
    - 成员端纸质发票在提交前不再显示“待管理员确认收票”这一阻塞点
    - 前端测试覆盖导航、提醒页、missing-materials 入口删除和纸票阻塞隐藏

- [x] 将材料审核页聚合发票更正、分摊确认与纸票收票动作
  - Done when:
    - 管理员在材料审核页的“处理动作”中即可完成发票字段更正、分摊调整和纸质发票收票确认
    - 材料审核页不再依赖跳转到独立的发票录入页或分摊确认页完成这些主路径操作
    - 相关前端测试覆盖内联保存、校验刷新和纸票收票动作

- [x] 优化分摊确认和导出页面
  - Done when:
    - 分摊确认列表统一样式
    - 导出页主操作突出，状态展示统一
    - 前端测试覆盖分摊和导出页渲染

## 临时任务 - 2026-05-02 管理员任务配置与成员检索修复

- [x] 修复系统管理员用户身份管理页检索账号不发请求
  - Done when:
    - 系统管理员在“用户身份管理”输入账号关键字后，前端会自动向后端发起检索请求
    - 不再依赖额外点击“检索账号”按钮才能看到候选账号
    - 前端测试覆盖输入后自动检索与后续授予管理员主路径

- [x] 调整管理员任务详情页保存区按钮换行布局
  - Done when:
    - 任务详情页“返回任务列表”“保存任务基础配置”不再被挤压在标题右侧
    - 两个按钮整体移动到保存说明的下一行显示
    - 前端测试覆盖任务详情页主渲染不回归

- [x] 移除异常优先队列中的导出阻塞项并修正异常卡片按钮布局
  - Done when:
    - “导出阻塞原因”只在任务就绪度的导出阻塞区展示，不再出现在“异常优先队列”
    - 异常卡片里的操作按钮不再被旁边说明文本挤压成窄圆形
    - 前端测试覆盖异常优先队列数量、空态文案和主渲染回归

- [x] 调整成员工作台纸质发票录入按钮换行布局
  - Done when:
    - 成员工作台“新增纸质发票”按钮不再和长说明文本挤在同一行
    - 按钮整体移动到说明文案下一行显示
    - 前端测试覆盖纸质发票创建主路径不回归

## 临时任务 - 2026-05-02 当前实例数据库问题收口

- [x] 收口报销任务状态为五阶段主流程
  - Done when:
    - 报销任务主流程对外统一为“草稿 -> 收集中 -> 复核中 -> 可导出 -> 已完成”
    - 管理员任务详情页不再把 `closed` 作为单独的正常阶段动作展示，正常主路径改为从“收集中”直接进入“复核中”
    - 任务截止自动检查不再把任务推进到单独的 `closed` 中间阶段，而是直接进入复核阶段
    - 兼容历史 `closed` 数据：页面展示和后续推进不出现异常

- [x] 收紧辅助材料自动关联为“识别出金额且同金额唯一”规则
  - Done when:
    - 所有自动关联都要求辅助材料已识别出金额
    - 自动关联只允许绑定到金额与识别金额完全一致的发票
    - 若同提交人在同任务下存在多张同金额发票，则不再自动关联，改走手动候选路径
    - 前后端测试覆盖“无金额不自动关联”“金额不一致不自动关联”“同金额唯一才自动关联”

- [x] 非发票材料缺少部分识别字段时不再阻塞成员工作台
  - Done when:
    - 支付记录、比赛通知、行程单、订单截图等非发票材料即使识别状态为 `needs_confirmation`，也不会单独进入问题材料阻塞分组
    - 发票材料的识别失败或待确认仍保持阻塞，不放宽发票主链路
    - 前后端测试覆盖非发票 `needs_confirmation` 不阻塞、发票 `needs_confirmation` 仍阻塞

## 临时任务 - 2026-05-02 管理员首页纵览 UI 收口

- [x] 收口管理员首页概览卡提示徽标的单行展示
  - Done when:
    - 管理员首页概览卡中的提示徽标不再把标题区挤压成竖排或双行异常折行
    - 概览提示文案在当前卡片宽度下保持单行可读
    - 前端测试覆盖管理员首页主渲染不回归

## 临时任务 - 2026-05-02 系统管理与个人资料收口

- [x] 为系统管理员前端补齐用户身份管理入口
  - Done when:
    - 系统管理员工作台存在稳定可见的“用户身份管理”入口，而不是只能靠直接调用接口授予管理员角色
    - 系统管理员可按关键字检索已有账号，并在前端触发“授予管理员角色”
    - 前后端测试覆盖检索、成功授予、重复授予幂等展示和越权拒绝主路径

- [x] 允许用户在个人信息页修改可维护资料并完成密码校验/修改
  - Done when:
    - 已登录用户可在前端个人信息页修改自己可维护的资料字段，不必重新注册
    - 用户可在同页完成密码修改，且后端要求当前密码校验
    - 前后端测试覆盖保存资料、密码校验失败、密码修改成功和登录主路径回归

## 临时任务 - 2026-05-02 验证流程收口

- [x] 优化仓库验证脚本并补充文档改动免验证规则
  - Done when:
    - `scripts/verify.sh` 能根据当前改动范围裁剪校验集，避免无关的全量串行验证
    - 独立校验项可并行执行，减少 Python / Web / 部署检查同时存在时的总耗时
    - 仅修改文档和工作记录文件时，验证脚本与仓库 `AGENTS.md` 都明确允许跳过仓库级验证
    - 本轮修改有实际验证记录，能证明脚本分流逻辑和主路径未被破坏

## 临时任务 - 2026-05-02 成员工作台与辅助材料闭环修复

- [x] 收口已识别辅助材料的阻塞分组与待关联附件展示
  - Done when:
    - 已识别成功的支付记录、比赛通知、行程单等非发票材料，不再因为“未形成发票”被归入“识别失败或待确认”问题发票分组
    - “附件待关联”不再作为成员提交发票的阻塞原因；只要发票自身校验闭合，允许继续提交
    - 待关联辅助材料若已经存在有效关联且没有剩余候选，不再继续显示为待关联项
    - 待关联辅助材料摘要与详情页在“无剩余候选”场景下不再展示“当前仍有 N 张候选发票可勾选”或冗余候选列表
    - 前后端测试覆盖上述回归

- [x] 修复市内交通行程单归票与行程单详情字段收口
  - Done when:
    - 市内交通网约车行程单可作为已有关联发票的有效行程附件，不再出现“无法关联归属发票”的假阻塞
    - 行程单详情页在市内交通场景下不再展示航空机场代码等无关字段
    - 相关校验、成员工作台或详情页测试覆盖真实回归

- [x] 简化纸质发票成员录入口径
  - Done when:
    - 成员手动新增纸质发票时只需要填写金额和费用类型
    - 纸质发票号码、开票日期、交易时间、销售方、抬头、税号、公对公转账编号等字段不再要求或展示
    - 后端创建纸质发票接口与前端表单、测试同步收口

- [x] 收紧辅助材料自动关联条件，并显式暴露未自动关联的单候选材料
  - Done when:
    - 自动把辅助材料关联到发票的前提收紧为：辅助材料已识别出金额、金额与发票完全一致、且同提交人在该任务下同金额发票只有一张
    - 上传辅助材料时不再因“同提交人仅有一张发票”就在识别前直接自动关联
    - 发票晚于辅助材料创建时，回填自动关联仍遵守同一规则，不再仅凭提交人或行程日期自动补绑
    - 未满足自动关联前提但仍存在手动候选发票的材料，会继续出现在待关联列表中，不会静默消失
    - 后端测试覆盖识别前不自动关联、识别后同金额唯一自动关联、金额不一致不自动关联、单候选但未自动关联仍出现在待关联列表

- [x] 修复行程单与订单截图识别混淆，并补充数据库实例回归样本
  - Done when:
    - 至少覆盖“高德打车电子行程单被识别成订单截图”的已知样本
    - 识别提示词、字段归一化或后处理能稳定区分行程单与订单截图
    - 新增针对真实样本特征的识别测试，避免再次回归

- [x] 修复待关联辅助材料入口与归属编辑交互，并删除冗余归属参考区块
  - Done when:
    - 待关联辅助材料主入口从工作台移到辅助材料页面
    - 辅助材料页下方每行展示一个发票候选勾选框，至少包含发票号、原始文件名、金额
    - 用户修改勾选后通过“更改关联”提交
    - 辅助材料详情页中的“归属发票参考”冗余区块删除

- [x] 收口非发票识别失败/待确认展示，避免错误显示票号和金额待补录
  - Done when:
    - 非发票材料在识别失败或待确认时，不再按发票口径显示“票号待补录”
    - 已识别出金额的非发票材料，不再显示“金额待补录”
    - 成员端与管理员端相关列表、详情与测试同步更新

- [x] 排查发票 333e167e-63fb-4797-a9b8-78943b110995 识别不完整根因并修复
  - Done when:
    - 已确认该材料当前缺失哪些关键字段、卡在哪个建票或展示环节
    - 修复覆盖真实根因，而不是手工补数据掩盖问题
    - 新增回归测试或可重复的验证步骤

- [x] 基于数据库识别历史收口 LLM 字段高失败率样本并优化提示词/后处理
  - Done when:
    - 已对当前实例数据库中的识别失败、待确认和高频人工更正样本完成分类
    - 至少挑选一类高频失败样本落地到提示词或后处理优化
    - 优化前后有可复验的样本或测试对比

- [x] 修复管理员创建任务时成员名单未实时请求后端检索候选成员
  - Done when:
    - 管理员在创建任务页输入成员关键字时，前端会实时向后端发送检索请求，而不是只做本地过滤
    - 后端检索至少支持按用户名、显示名称、学号和当前成员业务标识匹配
    - 下拉候选项显示统一成员摘要，选择后写入真实可用于任务权限判断的成员标识
    - 前后端测试覆盖空查询、部分匹配、无结果和选择后创建任务

- [x] 统一前端遗留黄色背景为真实页面背景语义
  - Done when:
    - 前端历史遗留的纯黄色卡片/区块背景被替换为当前设计系统中的真实背景、强调色或状态色
    - 不再把黄色当作默认容器底色滥用
    - 至少覆盖管理员创建任务、任务详情和成员工作台主路径

- [x] 管理员未进入任务时禁用任务管理页并收口导航
  - Done when:
    - 管理员没有进入具体任务上下文时，不能误打开任务管理主界面后直接落到创建任务路径
    - 该情形下管理员导航只显示“首页总览”和“创建任务”
    - 进入具体任务后再显示任务管理相关板块
    - 前端测试覆盖无任务上下文与有任务上下文两种导航

- [x] 管理员创建任务时默认全选发票类型
  - Done when:
    - 创建任务页首次打开时费用类别默认全选
    - 管理员仍可手动取消某些类别
    - 前端测试覆盖默认选中状态

- [x] 去除任务表单中的项目信息和报销人信息
  - Done when:
    - 管理员创建任务与编辑任务页不再展示“项目信息/报销人信息”输入
    - 后端任务创建与更新不再强制要求这两个字段
    - 相关 API 和测试同步更新

- [x] 拆分“支持任务配置多个管理员并复用成员检索交互”为可验证子任务
  - Done when:
    - 已基于当前代码确认单管理员假设主要分布在任务模型、权限判断、管理员列表过滤、创建页和多处管理员 API
    - 已把原任务拆成若干可以独立实现和验证的子任务，避免夜间直接跨越 schema、权限和前端交互做无边界大改
    - `WORKLOG.md` 记录了本轮仅拆分任务的原因和后续建议顺序

- [x] 为任务补齐多管理员数据模型与兼容返回字段
  - Done when:
    - 任务创建、读取、更新和持久化层支持多个管理员标识
    - 对外响应在支持 `administrator_ids` 的同时，保留明确的兼容字段策略，避免现有前后端一次性失效
    - 后端测试覆盖多管理员创建、读取和更新

## 临时任务 - 2026-05-02 新一轮成员发票与角色需求拆分

- [x] 允许成员删除未提交的发票
  - Done when:
    - 成员对自己上传、尚未提交给管理员的发票，可直接从成员发票详情页删除
    - 删除会同时移除该发票主记录及其分摊、确认、校验等关联数据，并把原始材料标记删除
    - 已提交发票、他人发票或管理员路径不受影响
    - 前后端测试覆盖成功删除、权限拒绝和已提交拒绝

- [x] 修复高德打车等支付凭证被误识别为订单截图
  - Done when:
    - 已知样本 `73b1a662-9b6d-414f-b3e9-a127857797cf` 对应特征能稳定归类为 `payment_record`
    - 提示词、后处理或归类规则覆盖“支付记录 / 订单截图”边界
    - 回归测试覆盖至少一条真实样本特征

- [x] 收口附件关联页，允许在已有关联基础上重选全部候选发票并按金额匹配排序
  - Done when:
    - 已关联发票时仍可看到该材料的全部可关联发票候选，而不是只能取消现有关联
    - 候选列表把“发票金额等于材料金额”的项排在最上面
    - 前端测试覆盖已有绑定时的候选展示、排序和保存

- [x] 让识别出的辅助材料类型自动回写到材料主记录
  - Done when:
    - 当识别结果把 `other_attachment`、`order_screenshot`、`payment_record`、`competition_notice`、`itinerary` 等非发票辅助材料重新判成另一种更准确的非发票类型时，材料主记录会自动更新
    - 发票相关类型切换仍保持保守，不会因为识别结果直接把已有材料主记录随意改成 `invoice`
    - 测试覆盖 `order_screenshot -> payment_record` 这类真实回归

- [x] 拆分“多角色账号 + 个人信息管理 + 首个生产用户为系统管理员”任务
  - Done when:
    - 已确认当前用户模型、注册流程、角色切换、系统管理接口与前端登录流对“单活跃角色”的依赖边界
    - 已把“账号多角色支持”“个人信息管理页面”“生产环境首个注册用户升级为系统管理员”“系统管理员授予管理员角色”拆成可独立验证的子任务
    - `WORKLOG.md` 记录本轮只拆分不直接实现该大任务的原因

- [x] 将生产环境首个受控初始化账号固定为系统管理员
  - Done when:
    - 生产环境首个受控初始化入口不再允许把首个 privileged 账号初始化为普通管理员
    - 相关后端测试、README/部署文档中的初始化口径统一改为“首个系统管理员”
    - 不影响开发环境成员自注册和现有 bearer 登录主路径

- [x] 为系统管理员补齐“授予管理员角色”后端接口与审计口径
  - Done when:
    - 系统管理员可把已有账号追加 `admin` 角色，而不需要再依赖公开注册直接写入 `roles`
    - 角色授予操作具备明确权限校验和最小审计信息
    - 后端测试覆盖授予成功、重复授予幂等或拒绝、非系统管理员越权拒绝

- [x] 收口多角色账号的受控赋权路径，移除公开注册对 `roles[]` 的产品级依赖
  - Done when:
    - 生产主路径下，多角色账号不再依赖 `/auth/register` 直接提交 `roles=["member","admin"]` 这类测试式赋权
    - 登录、`/auth/me` 与 `/auth/switch-role` 继续兼容已有多角色账号
    - 相关测试和文档明确区分“测试夹具能力”与“真实产品赋权路径”

- [x] 新增个人信息查询/更新接口并落地前端个人信息页
  - Done when:
    - 已登录账号可查看并更新至少 `display_name`，成员账号可维护 `member_code`
    - 前端存在稳定入口，不必再通过重新注册修改个人基础资料
    - 前后端测试覆盖查看、更新成功、字段校验失败和越权场景

- [x] 将任务访问控制和管理员任务列表切换为按管理员集合授权
  - Done when:
    - 任务访问权限、管理员任务列表和相关管理员操作入口不再只认单个 `administrator_id`
    - 同一任务内的多个管理员都可访问各自有权的任务页面和 API
    - 后端测试覆盖多管理员权限放行与越权拒绝

- [x] 为管理员选择补齐远程检索接口并复用创建任务页交互
  - Done when:
    - 后端提供管理员候选检索能力，至少支持按用户名、显示名称和当前管理员业务标识匹配
    - 前端创建任务页可像添加成员一样搜索并添加多个管理员
    - 前端测试覆盖管理员候选检索、添加、去重和空结果

- [x] 收口任务详情页与管理员主路径的多管理员展示和编辑
  - Done when:
    - 任务详情、管理员工作台和相关主路径不再把单个管理员当作唯一负责人展示或判断
    - 必要的展示文案改为多管理员口径，并保持旧任务可读
    - 前端测试覆盖多管理员任务的展示与进入路径

## 临时任务 - 2026-05-02 成员工作台辅助材料入口回归修复

- [x] 恢复成员工作台进入本人辅助材料总览的稳定入口
  - Done when:
    - 成员工作台侧边导航重新出现进入本人辅助材料总览的入口，而不是只能看到上传页和发票页
    - `/member/materials/status` 不再被重定向到工作台状态锚点，而是恢复为真实的成员材料状态页
    - 非待关联但已上传的支付记录、比赛通知、行程单、订单截图等材料，成员仍可通过该入口进入总览与详情页
    - 前端测试覆盖工作台入口与旧路由回归

## 临时任务 - 2026-05-01 真实库识别归票修复

问题分析：

- 当前运行实例中，票号 `25319166100007042896` 的铁路发票被错误关联了 23 个材料，其中大量材料已经识别为独立发票；根因是默认上传材料类型为 `other_attachment` 时，系统在识别前就允许自动归票，识别后即使材料类型变成 `invoice`，旧附件链接仍保留。
- 真实库中存在识别成功但未形成发票记录的材料：一类是识别结果只有 `expense_type_candidate`、缺少 `expense_type`；另一类是发票号码未被抽取，无法自动建票。
- 市内交通电子发票需要按网约车处理，必须关联对应行程单；当前识别提示和校验规则对“电子发票/电子票”到“网约车行程单”的约束不够硬。
- 真实 PDF 上传体积超过当前 10MiB 默认阈值，批量发票材料上传容易被错误拒绝。

- [x] 修复识别前错误自动归票、补齐市内交通电子票规则并提高上传阈值
  - Done when:
    - 默认 `other_attachment` 上传材料不会在识别前被自动关联到某张发票
    - 识别完成后，材料若被归类为支付记录、比赛通知、行程单或订单截图，仍可在单候选场景自动关联并刷新校验
    - 识别成功的发票若缺少 `expense_type` 但有合法 `expense_type_candidate`，可自动形成发票记录
    - 市内交通电子发票/电子票按网约车处理，缺少行程单时失败，关联行程单后通过
    - OpenAI 兼容识别提示词明确市内交通电子票分类、费用类型和行程单要求
    - 默认上传大小阈值调整为 64MiB，并覆盖后端、CLI 本地预检和前端上传预检
    - 当前运行实例中 `25319166100007042896` 的错误附件链接已清理，已确认剩余识别失败/待确认材料和未成票材料状态

## 临时任务 - 2026-05-01 Worker 并发识别批量上传发票

问题分析：

- 用户批量上传多张发票后，上传接口已经能一次保存多个材料并为每个材料创建识别任务，但独立 worker 的识别处理器仍按单线程串行消费待识别任务。
- 对真实发票识别而言，主要耗时在 PDF/图片处理和外部 LLM/VLM 调用；串行消费会让同一批上传的发票排队等待，用户只能看到长时间“识别排队中”。

- [x] 将 worker 识别任务改为可配置多线程并发处理
  - Done when:
    - worker 模式下识别处理器可用线程池同时消费同一轮轮询取到的多条待识别任务
    - 新增 `TRMS_ASYNC_JOB_WORKER_CONCURRENCY` 配置，默认值为 4，并限制非法并发值
    - 成员批量上传产生的多条识别任务仍保留部分成功、失败明细、排队状态和后续自动建票校验语义
    - worker 启动日志和部署配置暴露当前并发设置，便于排障
    - 后端测试覆盖配置解析、非法配置拒绝、worker 并发执行和批量上传持久化

## 临时任务 - 2026-05-01 前端上传阈值同步

问题分析：

- 后端和 CLI 已将默认单文件上传阈值调整为 64MiB，但前端 `upload-validation.ts`、两个成员上传入口文案和专项上传页测试仍保留 10MB 硬编码。
- 结果是超过 10MB 且不超过 64MiB 的文件会在浏览器提交前被错误拦截，无法到达已放宽的后端上传链路。

- [x] 将前端上传大小预检同步到 64MiB
  - Done when:
    - `web/src/lib/upload-validation.ts` 使用 64MiB 作为前端单文件预检阈值
    - 成员专项上传页和成员工作台上传区文案显示单文件最大 64MB
    - 前端超限错误提示显示 64MB，测试不再硬编码 10MB
    - 相关前端测试通过

## 临时任务 - 2026-05-01 数据库发票规则与工作台 UI 收口

问题分析：

- 当前真实数据库发票触发 `invoice_competition_location_range` 的待确认或失败较多，根因是地点识别字段和比赛地点之间缺少稳定、可解释的匹配证据；继续在主校验链路输出该规则会制造噪音。
- 管理员“材料审核/成员提醒”和“分摊确认”虽然都已使用统一发票摘要组件，但组件仍是横向四列，在窄面板里容易被长文件名、票号和状态挤爆。
- 成员工作台仍把工作状态、上传和发票列表连续铺开，用户不能像管理员工作台一样先选左侧类别，再在右侧处理当前类别。

- [x] 取消发票地点范围规则并统一工作台发票摘要
  - Done when:
    - `validate_invoice(...)` 不再生成 `invoice_competition_location_range` 规则结果，导出和复核摘要不再因该规则产生待确认/失败状态
    - 保留规则函数自身的单元测试边界，避免本轮删除解析辅助逻辑导致改动扩散
    - 管理员材料审核/成员提醒、分摊确认和其他统一摘要入口都按三行展示：第一行票号，第二行原始文件名，第三行金额、状态、附件数量
    - 成员工作台采用左右分栏，左侧类别从上到下为工作状态、上传页面、发票查看页面；右侧按当前类别展示内容
    - 发票查看页面右侧展示发票列表，点击后仍进入单张发票页面修改
    - 相关后端与前端测试覆盖规则取消、导出状态变化、三行摘要和成员工作台分栏导航

## 临时任务 - 2026-05-01 发票摘要与管理员面板 UI 收口

问题分析：

- 成员工作台仍然出现明显纵向下拉，根因不是数据过多本身，而是可提交、已提交、问题发票等列表仍在默认暴露过多字段、状态说明和操作控件；发票摘要没有稳定的一行式信息架构。
- 当前批量提交/撤回区域把不同提交状态的发票混在同一选择上下文中，用户无法清楚区分“准备提交”和“已经提交可撤回”的批处理对象；复选框视觉和交互也还没有按 Material 3 的 checkbox / state layer / focus / disabled 语义统一。
- 管理员识别字段表单暴露了过多识别内部字段、重复字段和技术元数据，增加审核噪音；管理员真正需要的是可复核、可更正的业务字段和校验结论。
- 管理员材料审核查看发票金额显示为真实金额的 100 倍，属于金额单位边界错误，必须单独修复并用测试锁定，不能作为普通 UI 文案问题处理。
- 管理员面板多处仍展示 UUID 等内部标识，说明前端没有统一执行“业务可读标识优先，内部 ID 只在调试或不可避免处出现”的展示边界。
- 发票缩略信息目前在成员端、管理员端和共享摘要中样式不一致，导致同一发票在不同页面的可扫描性和状态语义不一致；后续应统一为一行摘要样式。

- [x] 修复管理员材料审核发票金额显示单位错误
  - Done when:
    - 管理员材料审核、复核详情和相关发票摘要中金额按真实货币金额展示，不再把 `amount_cents` 或分为单位的整数直接当作元显示
    - 金额格式统一显示小数点后两位，空值或无法识别时显示用户可理解的“未识别金额/待补录”，不显示 `0` 或内部原始整数误导用户
    - 后端 API 如返回金额分单位，前端必须通过统一格式化函数展示；若某接口返回元单位，需明确命名或转换，避免两套单位混用
    - 前端测试覆盖管理员材料审核查看发票金额、发票摘要金额和空金额展示
    - 不通过修改测试期望来迎合 100 倍错误，必须确认修复前可复现、修复后通过

- [x] 将成员工作台可提交发票改为一行摘要和分状态批量操作
  - Done when:
    - 成员工作台“可提交发票”区域中每张发票默认只占一行，只展示原始文件名、发票号、是否校验通过、附件数量四类核心信息
    - 已提交发票和未提交发票拆成两个独立列表，各自拥有独立的全选/单选复选框状态
    - 未提交列表支持批量提交，已提交列表支持批量取消提交/撤回，两个列表的选择状态互不污染
    - 复选框符合 Material 3 语义和视觉状态，包括 checked、unchecked、indeterminate、disabled、focus-visible 和错误/强调状态
    - 可提交区域中的问题发票使用强调色标记，但仍保持一行摘要；点击任意发票行可以进入单张发票处理页
    - 前端测试覆盖已提交/未提交拆分、独立选择、批量提交、批量撤回、问题发票强调和点击进入处理

- [x] 统一所有发票缩略信息为一行摘要组件
  - Done when:
    - 抽出或收敛一个统一的发票摘要展示实现，默认字段顺序为原始文件名、发票号、校验状态、附件数量
    - 成员工作台、成员共享发票摘要、管理员任务详情、管理员材料审核、管理员复核/异常队列等所有发票缩略信息使用同一展示口径
    - 不在缩略摘要中展示 UUID、识别任务 ID、材料 ID、原始 provider 响应等内部数据
    - 问题发票、待确认发票、已提交发票和已通过发票使用统一状态标签和颜色语义
    - 摘要行在桌面和移动端都不因长文件名或长发票号导致纵向失控；必要时使用截断、tooltip 或次级折行，但不得默认展开成多字段卡片
    - 前端测试覆盖至少成员端和管理员端两个入口的统一摘要展示

- [x] 收敛管理员识别字段编辑表单为业务字段优先的 Material 3 表单
  - Done when:
    - 管理员面板识别字段编辑区去除重复输入项，同一业务字段只出现一个可编辑入口
    - 默认只展示管理员审核需要的业务字段，如材料类型、费用类型、发票号、金额、抬头、税号、日期、行程/住宿/支付等必要校验证据
    - 识别置信度、字段来源、原始识别响应、provider、worker、任务 ID 等内部信息不在默认审核表单中展示；确需保留时只能进入折叠的调试/审计区，并有明确权限和文案边界
    - 表单控件使用 Material 3 风格的 TextField、Select、Checkbox、Button、Helper text 和错误状态，不继续混用自造输入样式
    - 字段分组按业务审核顺序组织，避免管理员在识别字段、人工字段和校验字段之间来回跳转
    - 前端测试覆盖字段去重、关键业务字段可编辑、内部字段默认隐藏和保存行为

- [x] 清理管理员面板内部标识展示并改为用户友好信息
  - Done when:
    - 管理员任务详情、材料审核、复核、导出和成员相关列表默认不展示 UUID、数据库主键、识别任务 ID、导出任务 ID 等内部标识
    - 需要定位对象时优先展示比赛名称、成员姓名/学号、原始文件名、发票号、费用类型、提交时间和状态
    - 错误提示和操作反馈使用业务对象描述，不要求管理员复制内部 ID 才能理解问题
    - 内部 ID 如需排障，只能放在受控的详情/调试区域，且不得成为主界面第一层信息
    - 前端测试或 UX 测试覆盖管理员主路径不出现 UUID 样式文本

## 临时任务 - 2026-05-01 运行时修复

- [x] 修复航空发票机场代码仍触发行程单缺失
  - Done when:
    - 航空发票主材料或已关联辅助材料识别到明确往返机场代码时，`invoice_airfare_itinerary_required` 不再输出缺失行程单
    - 未识别到机场代码且没有行程单的航空发票仍输出缺失行程单
    - 舱位/订单截图规则与行程单缺失规则使用一致的机场代码证据口径
    - 相关后端测试覆盖机场代码放行行程单规则、发票 API、缺失材料聚合、成员工作台和管理员就绪度

## 临时任务 - 2026-04-30 运行时修复

- [x] 简化成员金额归属确认并调整航空/交易时间校验
  - Done when:
    - 成员上传发票形成发票记录后，默认生成“全额归属本人”的金额归属，不再要求每张发票再单独确认本人金额
    - 发票未提交给管理员前，成员可修改发票字段、材料类型和金额归属；提交后普通成员不能继续修改
    - 航空费用识别在完成基础元数据后，会针对 `airfare` 材料追加第三轮机场代码识别
    - 航空发票若已识别到明确往返机场代码，不再要求额外补订单截图来证明该信息
    - 发票交易产生时间不再做比赛时间范围限制，不因缺失或超出范围产生待确认/失败校验
    - 相关前后端测试覆盖默认归属、提交后锁定、航空机场代码、交易时间不限制和成员单票页交互

- [x] 拆分成员工作台单票处理页并在识别成功后自动建票校验
  - Done when:
    - 成员工作台只保留当前状态、比赛报销项目、上传入口、需要处理的发票列表和所有发票列表，不再默认堆叠每张发票的完整处理表单
    - 发票摘要、问题发票、共享发票和待处理材料入口都进入单张发票处理页
    - 单张发票处理页支持补字段、修改材料类型、重新识别、修改分摊金额、本人费用确认以及查看附件/缺失材料
    - LLM 识别成功且字段完整时自动创建或更新发票，并立即刷新校验，不再依赖人工编辑字段触发建票和校验
    - 相关前后端测试覆盖自动建票、默认材料类型收敛、工作台列表化和单票处理页
    - 已使用当前运行实例、Firefox DevTools 和 `/home/gsh/财务/ICPC区域赛/区域赛报销/武汉/` 真实 PDF 验证主路径

- [x] 收敛成员识别结果列表和 LLM 分类枚举输出
  - Done when:
    - 成员工作台“识别结果”区域默认只展示短摘要列表，不再展开所有需要处理材料的完整字段卡片
    - 超过默认展示数量的识别结果明确提示已收进下方发票处理列表，用户点击进入处理后再查看完整详情
    - OpenAI 兼容识别提示词明确列出 `document_family`、`material_type`、`expense_type_candidate` 允许值，并禁止 `hotel_invoice`、`railway_invoice`、`hotel_order` 等自造类别
    - 识别客户端能把合法 `document_family` 补齐为缺失的同名 `material_type`，并把真实 provider 常见子类别收敛到现有枚举
    - 相关前后端测试覆盖上述行为

- [x] 修复成员工作台发票处理入口和 LLM 识别输出规范化
  - Done when:
    - 成员工作台待关联辅助材料“查看候选发票”能定位到对应发票详情
    - 识别结果材料卡片中的“修改”和“指定归属”能定位到对应发票详情
    - 问题发票分组默认只显示摘要和进入处理入口，不再默认展开所有问题发票卡片
    - OpenAI 兼容识别客户端能规范化模型返回的标量分类字段、缺失置信度和订单类材料别名，避免有效输出被标记为 `llm_output_invalid`
    - 相关前后端测试覆盖上述行为

## 临时任务 - 2026-04-30 报销交互简化方案

- [x] 分析并记录成员/管理员报销交互简化改造方案
  - Done when:
    - 已对照需求、架构、UX 报告和当前成员/管理员工作台能力，明确当前交互繁琐的根因
    - 已形成可分阶段落地的成员端、管理员端、AI 编排和材料包导出方案
    - 方案已写入 `docs/报销交互简化改造方案.md`
    - 本轮不直接修改业务代码

- [x] 将报销交互简化方案拆成可执行任务
  - Done when:
    - `docs/报销交互简化改造方案.md` 中的成员端、管理员端、附件关联、聚合读模型、完整材料包和 UX 验收建议已拆成 `TASKS.md` 中的独立任务
    - 每个任务都有可单轮验证的 Done when，不把 API、前端、导出和 E2E 验收混成一个大任务
    - 已避免与既有未完成任务重复记录
    - 本轮不直接修改业务代码

## 临时任务 - 2026-04-30 报销交互简化落地

- [x] 将成员报销项目页重构为上传优先的材料草稿确认流程
  - Done when:
    - 成员进入单任务页面后标题为“比赛报销材料提交”，项目摘要展示比赛名称、时间、地点、成员、发票抬头、税号和报销截止时间
    - 上传报销材料区域位于识别结果和高级详情之前，支持批量选择与拖拽上传，并不要求先填写完整报销表单
    - 识别结果按“需要你确认 / 已自动识别 / 缺少材料 / 可能有问题”分组展示材料卡片
    - 材料卡片展示文件名、材料类型、金额、日期、费用类别、归属成员、用户可理解状态和确认/修改/指定归属/标记不报销/查看原文件入口
    - 页面提供报销草稿汇总与提交确认区；不能提交时列出还需处理的事项
    - 成员主路径不再暴露 OCR、worker、provider、raw response、材料编号等内部实现词

- [x] 在成员工作台展示批量提交与撤回区
  - Done when:
    - 成员工作台存在稳定的批量选择区域，能显示当前提交状态
    - 支持批量选择、批量提交和批量撤回，并给出失败原因或成功反馈
    - 前端测试覆盖选择、提交、撤回和状态展示

- [x] 收口附件手动关联 API 的成员权限边界
  - Done when:
    - `PUT /api/invoices/{invoice_id}/supporting-materials/{material_id}` 和对应解除关联接口不再允许匿名或无关成员操作
    - 成员只能把本人已归属到同一任务的非发票材料关联到本人可操作的候选发票
    - 管理员仍可在其负责的任务范围内处理全部附件关联
    - API 测试覆盖本人成功、无关成员拒绝、跨任务拒绝、管理员成功和 bearer 身份路径

- [x] 在成员工作台待关联辅助材料区支持选择候选发票并关联
  - Done when:
    - 多候选待关联辅助材料展示“选择归属发票并关联”的明确动作
    - 关联成功后刷新候选发票详情、缺失材料和校验状态
    - 无候选材料仍只引导补录或补传发票，不提供伪关联入口
    - 前端测试覆盖多候选关联成功、关联失败原因展示和无候选提示

- [x] 将成员工作台默认结构调整为待办、可提交发票和问题发票
  - Done when:
    - 成员进入单任务工作台第一屏优先看到待处理事项、可提交发票和不可提交原因，而不是默认浏览完整发票详情长列表
    - 不可提交发票按识别中、识别失败或待确认、附件待关联、缺失材料、分摊未完成、确认未完成分组
    - 发票详情仍可展开处理，但不再作为默认唯一主路径
    - 前端测试覆盖空态、可提交、不可提交分组和详情展开

- [x] 新增成员工作台聚合读模型
  - Done when:
    - 后端提供单任务成员工作台聚合接口，一次返回本人发票、共享摘要、可提交状态、阻塞原因、附件关联状态、分摊和确认摘要
    - 聚合接口不暴露他人原始附件、支付截图全文或识别原始响应
    - 成员工作台主数据加载改为优先使用该聚合接口，减少前端 N+1 请求
    - 后端和前端测试覆盖权限边界、可提交状态和阻塞原因映射

- [x] 上传成功后展示材料自动处理状态刷新
  - Done when:
    - 上传成功后页面展示已接收、识别排队中、识别完成、已归票、需要处理等处理阶段
    - 识别服务未配置、worker 排队等待、识别失败分别展示不同说明和下一步动作
    - 识别或归票状态变化后能刷新待处理事项，不要求用户手动拼接上传结果和工作台状态
    - 前端测试覆盖成功处理、排队等待、识别失败和部分上传失败场景

- [x] 新增管理员任务就绪度读模型
  - Done when:
    - 后端提供任务级就绪度接口，统一返回待识别、识别失败、低置信待确认、待关联附件、缺失材料、异常校验、分摊未完成、成员未确认、有异议和导出阻塞原因
    - 就绪度结果只依据现有任务、材料、发票、校验、分摊、确认和导出 boundary，不引入前端自造状态
    - API 测试覆盖全通过、存在识别阻塞、存在附件阻塞、存在确认阻塞和无关管理员拒绝

- [x] 管理员任务详情接入就绪度总览与异常优先队列
  - Done when:
    - 管理员任务详情第一屏展示任务整体就绪度和剩余阻塞项
    - 异常材料、待关联附件、缺失材料、待确认成员和可导出状态有明确入口
    - 正常材料不要求管理员逐张点开确认，异常队列优先展示需要处理的项目
    - 前端测试覆盖就绪度展示、阻塞入口跳转和全部通过状态

- [x] 新增 `reimbursement_package` 完整材料包导出类型
  - Done when:
    - 导出领域模型新增完整材料包类型，异步导出可生成一个 ZIP 产物
    - ZIP 至少包含 merged PDF、报销汇总、成员明细、发票明细、缺失材料清单、财务草稿和 `manifest.json`
    - `manifest.json` 记录任务数据版本、生成时间、导出人、子文件 hash、仍存在的 warning 和材料清单
    - 导出失败能指出具体子产物或材料编号，不生成残缺包并伪装成功
    - 后端测试覆盖成功打包、数据版本变化拒绝、merged PDF 子产物失败和 manifest 内容

- [x] 导出页主动作收敛为生成完整材料包
  - Done when:
    - 管理员导出页主按钮改为“生成完整材料包”，原有单项导出下沉为高级操作
    - 页面优先展示材料包就绪度、最近一次完整包状态、是否为最新数据版本和下载入口
    - 单项导出仍可用于排障或临时下载，但不再作为默认主流程
    - 前端测试覆盖创建完整包、下载完整包、旧包非最新提示和高级单项导出入口

- [x] 补充报销交互简化真实主流程 UX 验收
  - Done when:
    - `tests/ux/real-user-flows.spec.mjs` 或新的 UX 测试覆盖成员批量上传后只处理系统列出的待办
    - 测试覆盖多候选附件人工选择归票、成员批量提交、管理员查看就绪度和下载完整材料包
    - `UX_TEST_REPORT.md` 记录复跑结果、仍未覆盖的真实外部依赖和截图路径
    - 不把未配置真实 AI Provider 或外部通知渠道伪装成已完成能力

## 临时任务 - 2026-04-29 UX 实测

- [x] 使用真实数据执行端到端 UX 测试并输出报告
  - Done when:
    - 已使用 `/home/gsh/财务/ICPC区域赛/区域赛报销` 的真实材料副本完成无头浏览器实测
    - `UX_TEST_REPORT.md` 已记录角色流程、问题清单、截图路径和复现方式
    - 必要的低风险 UX 修复已落地并单独说明

- [x] 按 UX 实测收口成员工作台识别阻塞提示
  - Done when:
    - 成员工作台不再把识别排队中的材料显示成“暂无识别记录”
    - 识别服务未配置时，成员工作台给出明确阻塞说明和补救路径
    - 前端测试覆盖识别排队中与识别服务未配置两类状态

- [x] 按 UX 实测明确任务创建页成员名单填写语义
  - Done when:
    - 创建任务页成员名单输入框明确提示填写“成员姓名或学号”
    - 页面文案显式说明当前阶段不要填写内部数据库 ID
    - 前端测试覆盖新提示文案

- [x] 按 UX 实测强调补材料提醒只保存内部记录
  - Done when:
    - 提醒表单在提交按钮附近明确说明不会自动发送消息
    - 提交成功反馈明确说明只保存内部提醒记录
    - 前端测试覆盖新的边界文案

- [x] 按 UX 实测增加上传前文件体积预检查
  - Done when:
    - 成员专项上传页在提交前拦截超过当前上传上限的文件
    - 成员工作台上传区在提交前拦截超过当前上传上限的文件
    - 前端测试覆盖专项上传页与工作台上传区两条路径

- [x] 按 UX 实测恢复成员端手动补录发票信息与重新识别入口
  - Done when:
    - 成员能在当前主工作台路径下对本人材料手动填写或更正发票关键信息，而不是只能被动等待识别结果
    - 成员能对本人材料主动触发重新识别，并清楚看到“重新识别中 / 成功 / 失败”的反馈
    - 该入口不放宽为修改他人材料或直接写入任意识别结果
    - 前端测试覆盖本人可操作、非本人不可操作和重新识别反馈三类路径

## 临时任务 - 2026-04-29 运行时修复

- [x] 修复 worker 日志不可见与 DeepSeek 识别结果规范化
  - Done when:
    - `uv run python -m trms_backend worker` 启动后能直接看到 worker 启动/轮询日志
    - DeepSeek 返回 `confidence: "high"`、中文 `material_type`、非枚举 `expense_type` 时，不会让整次识别无条件失败
    - 测试覆盖 worker 日志初始化与 DeepSeek 风格输出规范化

## 临时任务 - 2026-04-29 成员简化上传流程

- [x] 按简化流程收口成员上传入口并修复发票队列溢出
  - Done when:
    - 成员工作台和专项上传页上传文件时不再要求成员先选择材料类型，而是直接上传后等待 AI 识别
    - 后端任务上传接口允许省略 `material_type`，并以保守默认值接收材料；当识别结果明确给出材料类型时，系统会自动把默认值收敛为识别类型
    - 成员工作台左侧发票选择卡片不再因为长发票号、状态摘要和元数据网格出现异常挤压或竖排溢出
    - 前后端测试覆盖“免选类型上传”和“识别后自动收敛材料类型”的主路径

## 临时任务 - 2026-04-29 识别 Provider 分离配置

- [x] 支持区分文本 LLM / VLM 的系统级优先配置
  - Done when:
    - 运行时配置明确区分纯文本材料使用的 text LLM 与扫描 PDF / 图片使用的 VLM，不再只能共用一套 `TRMS_LLM_*`
    - 系统管理员可在系统管理页保存 text LLM / VLM 覆盖项，且系统配置优先，缺失字段再 fallback 到 `.env`
    - 识别执行链会按输入类型动态选择 text LLM 或 VLM，并在保存系统配置后无需重启即可生效
    - 前后端测试覆盖环境变量分离、系统配置覆盖和系统管理员配置入口

## 临时任务 - 2026-04-29 PDF 渲染回退识别

- [x] 统一用 PyMuPDF 处理 PDF 并在文本失败后回退到 VLM
  - Done when:
    - PDF 文本提取与 PDF 页面渲染统一使用同一个 Python PDF 库，而不是继续混用“文本抽取”和“扫描件直传 file”
    - 扫描 PDF 不再作为 `file` 发给 VLM，而是先渲染成图片后按 `image_url` 路径识别
    - 纯文本 PDF 在 text LLM 首次失败后，会自动渲染为图片并再走一次 VLM
    - 测试覆盖扫描 PDF -> image_file、文本 PDF 失败后回退 VLM，以及相关失败原因与原始上下文

## 临时任务 - 2026-04-29 统一识别响应格式

- [x] 统一对主流 provider 使用 json_object 响应格式
  - Done when:
    - OpenAI / DeepSeek 与当前火山 Ark VLM 路径都确认可接受 `response_format: { type: \"json_object\" }`
    - 仓库识别客户端不再对单个 provider 维持额外的 `json_schema` / `json_object` 分支判断
    - 继续保留服务端对模型输出的 JSON 解析和 Pydantic 结构化校验，不因为切到 `json_object` 放松约束

## 临时任务 - 2026-04-29 两阶段识别设计

- [x] 建立材料两阶段识别总流程
  - Done when:
    - 识别链路显式拆分为“第一阶段分类”和“第二阶段按类别提取元数据”，而不是继续用单次 prompt 同时判断类别和字段
    - 第一阶段输出至少包含 `document_family`、`material_type`、`expense_type_candidate`、`is_reimbursement_voucher` 和分类置信度
    - 第二阶段会根据第一阶段分类结果选择对应 schema，而不是让所有材料共用同一套字段集合
    - `WORKLOG.md` 已记录当前规则与字段映射边界

- [x] 固化“税局盖章材料归发票类别”的分类规则
  - Done when:
    - 第一阶段分类明确遵循：只要材料上存在税局盖章或等价税务监制特征，就归入发票类别
    - 铁路电子客票、铁路电子行程单、航空电子客票报销凭证等可直接报销凭证按发票类别处理，而不是误归为辅助材料
    - 该规则在测试或识别提示词中有显式覆盖，不只停留在注释或口头约定

- [x] 建立按类别提取字段的 schema 清单
  - Done when:
    - 发票类、比赛通知、支付凭证、订单截图、行程单、其他附件各自有独立字段清单
    - 字段清单能覆盖当前 `invoice_validation.py` 已实现规则所需信息，而不是只覆盖现有发票元数据
    - `WORKLOG.md` 已明确记录“规则 -> 材料类别 -> 所需识别字段”的映射

- [x] 拆分成员侧辅助材料自动归票与待关联提示任务
  - Done when:
    - 原任务已拆成“后端单候选自动归票规则”“待关联摘要接口”“成员工作台待处理提示”三个独立交付面
    - 拆分后每个子任务都能在单轮内独立验证，而不是继续把绑定规则、数据模型和前端提示混在一起
    - `WORKLOG.md` 已记录当前代码缺口与拆分依据

- [x] 建立辅助材料单候选自动归票后端规则
  - Done when:
    - “同批上传”与“发票后补传但仅存在一个安全候选发票”两类场景下，系统会自动把辅助材料绑定到该发票
    - 自动绑定只依赖明确边界，例如同任务、同提交人、单候选，不通过模糊猜测跨成员乱绑
    - 有后端测试覆盖自动绑定成功、无候选不绑定和多候选不绑定三类路径

- [x] 建立辅助材料多候选待关联摘要接口
  - Done when:
    - 未自动绑定的辅助材料能被后端稳定识别为“待关联”
    - 成员侧或管理员侧能读取每份待关联材料的候选发票摘要，而不是只看到它静默悬空
    - 有 API 测试覆盖无候选、单候选已自动绑定和多候选待人工处理三类状态

- [x] 在成员工作台展示待关联辅助材料与处理入口
  - Done when:
    - 成员工作台会显式展示“哪些辅助材料还未关联到发票、为什么没自动关联、下一步怎么处理”
    - 对已自动归票的材料和待关联材料有清晰区分，不把它们混成普通缺失材料提示
    - 前端测试覆盖待关联列表、候选摘要和处理入口文案

- [x] 拆分成员侧发票批量提交与撤回区任务
  - Done when:
    - 原任务已拆成“后端提交状态模型与 API”“后端撤回规则”“成员工作台批量区块”三个独立交付面
    - 拆分后每个子任务都能单轮验证，而不是把状态模型、权限规则和前端交互混在一起
    - `WORKLOG.md` 已记录当前代码缺口与拆分依据

- [x] 建立成员侧发票提交状态模型与批量提交 API
  - Done when:
    - 发票存在明确的成员侧“已提交/未提交”状态，而不是继续把“已确认费用”误当作“已提交管理员”
    - 支持批量提交若干发票，并显式拒绝不满足前置条件的发票
    - 有后端测试覆盖批量提交成功、部分不满足条件失败和越权失败路径

- [x] 建立成员侧发票撤回规则与批量撤回 API
  - Done when:
    - 已提交但尚未进入不允许撤回阶段的发票可以批量撤回
    - 撤回不会绕过已有任务状态、确认状态或管理员后续处理边界
    - 有后端测试覆盖可撤回、不可撤回和越权撤回三类路径

## P0 - 框架、盘点与基础设施

- [x] 搭建 Codex 自动化工作框架
  - Done when:
    - `AGENTS.md`、`TASKS.md`、`WORKLOG.md`、`BLOCKERS.md` 已建立
    - `scripts/verify.sh` 和 `scripts/codex-nightly.sh` 已建立
    - 基础脚本语法检查和 `git diff --check` 已纳入验证

- [x] 确认项目技术栈和启动方式
  - Done when:
    - `WORKLOG.md` 记录当前识别到的技术栈、入口和测试命令
    - `scripts/verify.sh` 能在本地执行完整基础验证
    - 不要求实现新业务功能

- [x] 固化后端健康检查验证
  - Done when:
    - 已有 `/health` 接口有明确测试覆盖
    - 验证脚本能运行相关测试
    - `WORKLOG.md` 记录后端启动命令

- [x] 梳理当前 API 能力清单
  - Done when:
    - `WORKLOG.md` 记录当前已实现的任务、材料、发票、分摊、确认接口
    - 标注与需求文档 FR-001 至 FR-015 的覆盖关系
    - 不改动业务逻辑

- [x] 整理第一阶段验收映射
  - Done when:
    - `TASKS.md` 或独立文档映射 AC-001 至 AC-018 的实现状态
    - 已完成、部分完成、未开始状态清晰
    - 下一批任务可从映射中继续拆分

- [x] 增加数据库迁移策略说明
  - Done when:
    - `WORKLOG.md` 或专门文档记录当前 `create_all` 策略的限制
    - 明确是否引入 Alembic 以及触发条件
    - 不要求立即迁移已有数据库

- [x] 明确第一阶段 Won't-have 边界
  - Done when:
    - `WORKLOG.md` 或文档记录 FR-011 Browser Use 自动录入、财务 API 对接、财务审批同步等第一阶段不实现
    - 说明后续如需启用 Browser Use 必须人工确认提交
    - 不实现任何浏览器自动化

## P0 - Review 发现对齐修复

- [x] 收口任务创建与匿名任务查询权限
  - Done when:
    - 创建报销任务必须要求已登录且具备管理员权限，匿名请求不能再直接创建任务
    - 任务列表、任务详情、任务成员和任务材料等接口不再把匿名请求当作可访问范围
    - 有 API 测试覆盖匿名、普通成员、任务管理员和无关管理员四类路径

- [x] 禁止成员侧接口通过匿名自报 `actor_id` 越权读取
  - Done when:
    - 费用明细、成员状态、缺失材料等成员侧接口必须依赖 bearer 身份上下文，而不是匿名请求自报 `actor_id`
    - bearer 身份与显式 `actor_id` / `member_id` 不一致时显式拒绝
    - 回归测试覆盖“知道成员编号但未登录”与“登录后冒充他人”两类失败路径

- [x] 收口 Telegram 绑定与提交身份边界
  - Done when:
    - Telegram 账号绑定不能再由匿名调用方随意为任意 `telegram_user_id` 建立绑定
    - Telegram 入站材料提交不再直接信任表单里的 `telegram_user_id` 作为真实身份来源
    - 未完成可信绑定或可信入站校验时，材料进入待归属而不是直接写入成员主链路

- [x] 收口格式化邮件提交成员身份解析边界
  - Done when:
    - 邮件入站不再接受匿名调用方直接指定 `resolved_member_id` 作为已确认成员身份
    - 成员身份只能来自受控绑定关系、受信任入站器或明确的待归属流程
    - 测试覆盖邮件伪造成员身份失败和未绑定进入待归属路径

- [x] 接入基于 VLM API 的扫描 PDF / 图片直提识别链路
  - Done when:
    - 图片、截图和纯扫描 PDF 不再统一停在 `ocr_not_configured`
    - 支持图像输入的 VLM API 可直接输出结构化识别结果，不要求先落传统 OCR 中间文本
    - VLM 输出继续经过结构化校验、字段置信度标注和失败显式暴露，不把模型结果当最终事实
    - 测试覆盖文本 PDF、扫描 PDF、图片、VLM 提取失败和空白文件路径

- [x] 实现真实合并打印 PDF 导出
  - Done when:
    - `merged_pdf` 导出生成真实 PDF 产物，而不是只返回计划占位 JSON
    - 异步导出任务、下载接口和前端页面都能展示真实产物状态与下载入口
    - 遇到损坏、加密或不可读材料时返回具体材料编号和失败原因，不静默跳过

- [x] 拆分成员发票工作台重构任务
  - Done when:
    - 原“重构成员发票工作台并补齐自助元数据管理”任务已拆成可单轮验证的子任务
    - 拆分结果覆盖“汇总视图、材料类型编辑、分摊对象编辑、入口收口”四个独立交付面
    - `WORKLOG.md` 已记录当前代码缺口与拆分依据

- [x] 建立成员发票工作台单任务汇总视图
  - Done when:
    - 成员可在单一任务上下文查看本人上传发票的识别字段、人工更正值、材料类型、当前分摊方案和确认状态
    - 页面按“待处理事项、异常原因、下一步动作”组织信息，不再要求用户在材料状态页和费用确认页之间自行拼接上下文
    - 有前端测试覆盖任务切换、摘要展示和关键异常提示

- [x] 增加成员侧材料类型自助更正能力
  - Done when:
    - 成员可在本人材料范围内修改发票或辅助材料的 `material_type`
    - 越权访问、非法类型和不允许修改的状态都会返回明确错误
    - 有 API 测试覆盖本人成功、越权失败和校验刷新边界

- [x] 增加成员侧金额分配对象自助调整能力
  - Done when:
    - 成员可在权限允许范围内调整本人发票的金额分配对象和分摊备注，且总额约束保持成立
    - 分摊调整后的确认状态刷新规则和失败原因在成员端可见
    - 有测试覆盖本人调整成功、无关成员被拒绝和确认状态重置

- [x] 收口成员发票工作台入口与下一步动作
  - Done when:
    - 成员任务列表和相关页面优先进入单任务发票工作台，而不是把材料状态、缺失材料、费用确认分散成并列主入口
    - 工作台内可明确跳转到上传、补材料、确认费用等下一步动作
    - 有前端测试覆盖主入口跳转和关键下一步动作入口

- [x] 调整任务内成员共享发票可见性策略
  - Done when:
    - 同一比赛任务内成员可查看当前已上传发票的基础元数据、当前分摊去向和必要附件摘要
    - 对“共享发票可见”与“敏感原始文件下载、支付截图全文、无关账号信息”之间的边界有明确规则和测试
    - `WORKLOG.md` 明确记录该任务属于相对原需求文档的产品变更，并同步更新相关权限测试假设

- [x] 补充原型图对照与 UI 规范文档
  - Done when:
    - 已读取 `docs/原型图.png` 并提炼出当前阶段必须遵循的基础信息架构和交互规范
    - 文档明确指出当前 UI 的主要不合理之处，而不是笼统写成“与原型不一致”
    - 文档明确“不要求完全复刻原型图，但必须遵循其基本规范”

- [x] 按原型图收口管理员端导航与任务推进信息架构
  - Done when:
    - 管理员端形成稳定的模块导航和当前任务上下文，不再主要依赖页面内部一组跳转按钮
    - 管理员首页优先展示任务推进阶段、异常数量和下一步动作，而不是泛化工作台说明
    - 有前端测试覆盖管理员任务入口、当前模块高亮和关键任务动作入口

- [x] 按原型图建立材料审核列表-详情联动视图
  - Done when:
    - 材料审核或复核主界面采用“材料列表 + 当前材料详情”联动结构
    - 同页可查看原始票据预览、识别字段、校验异常、分摊去向和处理动作
    - 不再要求管理员在复核、发票录入、分摊编辑之间频繁跳页才能完成单张发票处理

- [x] 按原型图收口成员端单任务处理闭环
  - Done when:
    - 成员可在单一任务上下文中连续完成上传材料、查看识别结果、查看缺失项、查看分摊去向和提交确认
    - 发票元数据、材料类型、关联附件和金额分配对象在成员侧有稳定可见入口
    - 页面不再把主要流程拆成多个弱关联页面要求用户自行判断下一步

## P1 - 报销任务与成员名单

- [x] 建立全局发票抬头和税号配置边界
  - Done when:
    - 存在可读取的全局抬头、税号配置模型或服务边界
    - 新建任务可默认继承全局抬头和税号
    - 有测试覆盖默认继承和任务级覆盖

- [x] 完善报销任务发布前校验
  - Done when:
    - 缺少成员名单、费用类别、项目信息、报销人信息时不能发布任务
    - 对应 API 测试覆盖成功和失败路径
    - `./scripts/verify.sh` 通过

- [x] 增加任务成员管理接口
  - Done when:
    - 可查询任务成员名单
    - 可在草稿状态添加、移除或更新任务成员
    - 开放提交后成员变更规则有测试或明确限制

- [x] 增加任务费用类别约束
  - Done when:
    - 任务只允许配置受支持的费用类别
    - 发票费用类型必须属于任务允许类别
    - 非法费用类别返回明确错误

- [x] 增加任务截止时间状态约束
  - Done when:
    - 超过截止时间的任务不能由成员继续提交材料
    - 管理员路径是否允许补交有明确规则或记录假设
    - 对应测试覆盖边界时间

- [x] 建立任务自动关闭检查边界
  - Done when:
    - 存在可调用的任务截止检查服务或命令
    - 到期开放任务可进入关闭状态
    - 不要求引入真实调度器

- [x] 增加任务状态流转条件检查
  - Done when:
    - `ready_to_export` 只能在复核条件满足时进入
    - `completed` 只能在导出完成后进入
    - 失败路径返回明确错误

## P1 - 材料提交与归档

- [x] 校验材料提交人必须属于任务成员
  - Done when:
    - 非任务成员不能向任务提交材料
    - Web、CLI、Telegram、Email 渠道统一经过该校验
    - 对应 API 测试覆盖

- [x] 增加材料类型与附件类型字段
  - Done when:
    - 材料可标记为发票、支付记录、比赛通知、行程单、订单截图或其他附件
    - API 能保存和返回材料类型
    - 非法类型返回明确错误

- [x] 建立材料文件保存抽象
  - Done when:
    - 原始上传文件有可替换的存储接口
    - 默认本地存储不会覆盖同名文件
    - 测试覆盖重复上传和文件元数据记录

- [x] 保存原始文件存储位置
  - Done when:
    - 材料记录保存不可变 storage key 或路径
    - 数据库不保存完整文件内容
    - 测试覆盖文件可由 storage key 定位

- [x] 增加材料上传文件校验规则
  - Done when:
    - 空文件、缺少文件名、不支持内容类型或超出大小限制时返回明确错误
    - 支持的文件类型在代码或配置中可查
    - 不吞掉上传失败原因

- [x] 支持批量上传部分成功结果
  - Done when:
    - 一次上传多个文件时返回逐文件成功或失败结果
    - 单个文件失败不伪装为全部成功
    - 测试覆盖部分成功和全部失败

- [x] 增加待归属材料状态
  - Done when:
    - 无法确定任务或提交人的材料可进入待归属状态
    - 普通成员不能访问待归属材料
    - 管理员后续处理边界有测试或明确 TODO

- [x] 建立待归属材料认领流程
  - Done when:
    - 管理员可将待归属材料绑定到任务和提交人
    - 认领操作记录操作者和时间
    - 普通成员不能认领无关材料

- [x] 增加跨渠道重复文件检测
  - Done when:
    - 同一任务内不同渠道提交相同文件 hash 时标记重复
    - 重复结果不依赖文件名
    - 测试覆盖 Web 与 CLI 或其他渠道重复场景

- [x] 建立发票与辅助材料关联模型
  - Done when:
    - 一张发票可关联支付记录、比赛通知、行程单、订单截图等材料
    - 一个附件是否可关联多张发票有明确规则或记录假设
    - 有最小测试覆盖关联和取消关联

## P1 - 发票、识别与人工更正

- [x] 建立 AI 识别任务占位模型
  - Done when:
    - 存在识别任务记录或领域模型，能表达 pending、succeeded、failed、needs_confirmation
    - AI 输出明确不是最终事实来源
    - 不接入真实外部 AI 服务

- [x] 保存识别原始结果和字段置信度
  - Done when:
    - 识别结果能保存原始响应、字段值、字段来源和置信度
    - 低置信度字段可标记为待确认
    - 测试覆盖低置信度路径

- [x] 建立识别任务触发边界
  - Done when:
    - 材料提交后能创建或排队识别任务占位
    - 不阻塞上传响应
    - 不调用真实 OCR 或外部 AI

- [x] 建立人工录入发票字段边界
  - Done when:
    - 管理员或成员可对材料录入发票号码、金额、抬头、税号、交易时间和费用类型
    - 录入后触发基础校验
    - 失败路径有测试

- [x] 增加人工更正识别字段记录
  - Done when:
    - 人工更正能记录字段来源和修改时间
    - 更正前后差异可追溯
    - 关键字段更正后会触发重新校验或记录待触发状态

- [x] 支持识别失败显式暴露
  - Done when:
    - OCR/PDF/AI 失败可记录失败原因
    - API 能返回材料识别失败状态
    - 失败不会被静默当作识别成功

- [x] 支持多次识别历史
  - Done when:
    - 同一材料可以保留多次识别尝试
    - 能查询最新有效结果和历史记录
    - 旧结果不会覆盖审计历史

## P1 - 校验规则与缺失材料

- [x] 扩展发票校验规则结果
  - Done when:
    - 校验结果包含 `rule_code`、目标对象、严重级别、状态、消息和结构化证据
    - 现有抬头、税号、重复发票规则继续通过
    - 对应测试覆盖

- [x] 未识别抬头或税号时输出待确认校验
  - Done when:
    - 抬头缺失时结果为 pending 或 failed，规则语义明确
    - 税号缺失时结果为 pending 或 failed，规则语义明确
    - 不把缺失字段静默当作通过

- [x] 实现金额超过阈值需要支付记录的校验骨架
  - Done when:
    - 单张发票金额达到配置阈值时输出缺失支付记录结果
    - 阈值默认值和配置来源有记录
    - 对应测试覆盖通过和失败路径

- [x] 实现支付记录金额匹配校验
  - Done when:
    - 已关联支付记录时可比较支付金额与发票金额
    - 金额缺失或不一致时输出明确校验结果
    - 是否要求精确匹配有记录或配置

- [x] 实现参赛费比赛通知校验
  - Done when:
    - 参赛费缺少比赛通知时输出 blocker 结果
    - 已关联比赛通知时通过
    - 规则不依赖文件名猜测

- [x] 实现航空费用附件完整性校验
  - Done when:
    - 航空费用缺少发票或行程单时输出缺失材料结果
    - 缺少舱位信息且无订单截图时输出待确认或失败
    - 测试覆盖主要分支

- [x] 实现网约车行程信息校验
  - Done when:
    - 市内交通为网约车且缺少行程信息时输出缺失材料结果
    - 无法判断是否网约车时输出待确认
    - 测试覆盖缺失和通过路径

- [x] 实现比赛时间范围校验
  - Done when:
    - 优先使用交易时间而不是开票时间
    - 默认比赛前后合理缓冲规则有记录
    - 缺少交易时间时输出待确认而非静默通过

- [x] 实现比赛地点范围校验
  - Done when:
    - 地点能与比赛城市、出发地、到达地或往返路径做基础匹配
    - 地点缺失时输出待确认
    - 测试覆盖匹配、不匹配和未知路径

- [x] 建立缺失材料清单模型
  - Done when:
    - 校验结果可聚合为成员或任务维度的缺失材料清单
    - 至少覆盖支付记录和比赛通知两类缺失项
    - 有测试覆盖聚合逻辑

- [x] 支持材料补充后重新校验
  - Done when:
    - 新增或关联附件后相关发票校验结果可重新计算
    - 旧校验结果不会被静默误用
    - 测试覆盖从失败变为通过

## P1 - 费用分摊与成员确认

- [x] 完善费用分摊提交权限
  - Done when:
    - 只有发票提交人、归属成员或管理员中的允许角色可提交分摊
    - 未授权成员不能修改无关发票分摊
    - 测试覆盖越权路径

- [x] 建立个人费用明细查询接口
  - Done when:
    - 成员可查询本人相关费用明细
    - 管理员可查询任务内全部费用明细
    - 普通成员不能查看无关成员明细

- [x] 支持成员费用异议处理状态
  - Done when:
    - 成员可提交异议说明
    - 异议状态能被管理员查询
    - 管理员处理异议后重新进入待确认有明确边界

- [x] 实现费用分摊确认失效规则
  - Done when:
    - 管理员或系统修改分摊金额后，相关成员确认状态失效
    - 旧确认记录不被静默当作有效确认
    - 对应回归测试覆盖

- [x] 引入费用明细版本号
  - Done when:
    - 成员确认绑定到具体费用明细版本
    - 金额或归属变化会生成新版本或递增版本
    - 查询接口能区分当前有效确认与历史确认

- [x] 支持成员逾期未确认识别
  - Done when:
    - 可查询任务内逾期未确认成员
    - 逾期判断基于任务截止时间或确认截止规则
    - 不静默把未确认当作已确认

- [x] 禁止管理员代确认默认路径
  - Done when:
    - 第一阶段默认不允许管理员代成员确认
    - 如果保留代确认接口占位，必须要求原因并审计
    - 有测试覆盖默认禁止

## P1 - 管理员复核与状态流转

- [x] 建立管理员复核状态流转
  - Done when:
    - 任务可从关闭进入复核，并在 Must 级问题清零后进入可导出状态
    - 有失败校验时不能最终确认
    - 对应测试覆盖主要状态流转

- [x] 建立复核汇总查询接口
  - Done when:
    - 管理员可查看任务内材料、识别结果、校验结果、分摊和确认状态聚合
    - 普通成员不可调用管理员复核视图
    - 有最小 API 测试

- [x] 阻止存在待归属材料的最终确认
  - Done when:
    - 任务存在待归属材料时不能进入可导出状态
    - 错误响应指出待处理材料数量或编号
    - 测试覆盖

- [x] 阻止存在未确认成员的最终确认
  - Done when:
    - 任一相关成员未确认当前费用明细时不能最终确认
    - 异议状态不能被静默当作确认
    - 测试覆盖未确认和异议路径

- [x] 支持管理员补材料提醒记录
  - Done when:
    - 管理员可记录对成员的补材料提醒
    - 提醒内容、目标成员、任务和时间可查询
    - 不要求接入真实短信、邮件或 Telegram 发送

- [x] 建立系统自动提醒占位
  - Done when:
    - 可根据缺失材料和未确认状态生成提醒任务占位
    - 生成逻辑幂等
    - 不调用真实外部通知渠道

## P1 - 导出与打印材料

- [x] 建立导出模块边界骨架
  - Done when:
    - 代码中存在导出模块的清晰占位边界
    - 不生成真实财务材料
    - 有最小测试确认接口或服务边界可调用

- [x] 建立导出任务模型
  - Done when:
    - 导出任务能表达 pending、running、succeeded、failed
    - 记录导出类型、任务编号、操作者、生成参数和时间
    - 有最小持久化测试

- [x] 导出报销汇总表
  - Done when:
    - 可按任务生成费用类型和总金额汇总
    - 输出格式优先 CSV 或 XLSX 边界明确
    - 测试覆盖汇总金额正确

- [x] 导出成员报销明细表
  - Done when:
    - 可按成员列出费用明细和金额
    - 只使用当前有效费用版本
    - 测试覆盖多人分摊

- [x] 导出发票明细表
  - Done when:
    - 可导出发票号码、金额、费用类型、提交人、校验状态
    - 重复发票和异常校验状态可见
    - 测试覆盖基础字段

- [x] 导出缺失材料清单
  - Done when:
    - 可按成员和费用列出缺失材料
    - 至少覆盖支付记录、比赛通知、行程信息
    - 测试覆盖空清单和非空清单

- [x] 生成财务填报草稿
  - Done when:
    - 可输出项目、报销人、总金额、发票明细等财务录入辅助信息
    - 输出不包含敏感 token 或本地私有路径
    - 测试覆盖字段完整性

- [x] 合并打印 PDF 占位
  - Done when:
    - 存在合并打印 PDF 的服务边界
    - 默认顺序按架构文档记录
    - 遇到损坏或加密 PDF 时返回具体材料编号，不静默跳过

- [x] 绑定导出结果到任务版本
  - Done when:
    - 导出文件记录生成时任务状态或数据版本
    - 数据变化后旧导出不会被当作最新
    - 测试覆盖导出后修改数据路径

## P1 - Web 前端与管理员后台

- [x] 确认 Web 前端技术栈和工程边界
  - Done when:
    - `WORKLOG.md` 记录是否采用架构文档建议的 React、TypeScript、Vite 和管理后台组件库
    - 明确前端目录、启动命令、构建命令和测试命令
    - 不实现业务页面

- [x] 建立 Web 前端项目骨架
  - Done when:
    - 存在独立前端入口、路由边界和 API 客户端边界
    - `./scripts/verify.sh` 能在存在前端工程时运行 lint/test/build
    - README 或 WORKLOG 记录本地启动方式

- [x] 建立前端 API 类型与错误处理边界
  - Done when:
    - 前端有任务、材料、发票、分摊、确认、校验和导出的基础类型定义
    - API 错误能以统一结构展示给用户
    - 不在前端吞掉服务端错误

- [x] 建立 Web 登录和角色入口占位
  - Done when:
    - 前端能表达成员、管理员和系统管理员三类角色入口
    - 未登录用户不能进入业务页面
    - 不接入真实 OAuth 时有明确 mock 或占位边界

- [x] 实现管理员任务列表页面
  - Done when:
    - 管理员可查看任务编号、比赛名称、状态、截止时间和异常摘要
    - 支持按状态筛选或基础搜索
    - 页面有加载、空状态和错误状态

- [x] 实现管理员任务创建页面
  - Done when:
    - 表单覆盖比赛名称、地点、起止时间、成员名单、费用类别、截止时间、管理员、项目、报销人、抬头和税号
    - 前端校验必填、日期顺序和空成员项
    - 提交失败展示服务端错误

- [x] 实现任务详情与状态操作页面
  - Done when:
    - 管理员可查看任务基础信息、成员名单、费用类别和当前状态
    - 可执行允许的状态流转操作
    - 非法流转错误清晰展示

- [x] 实现成员 Web 可提交任务页面
  - Done when:
    - 成员只能看到本人可参与或已参与任务
    - 显示比赛名称、比赛时间、任务状态和截止时间
    - 无可见任务时有明确空状态

- [x] 实现成员 Web 材料上传页面
  - Done when:
    - 成员可选择任务并上传一个或多个发票或附件文件
    - 可选择材料类型和提交渠道为 Web
    - 上传结果展示材料编号、重复状态和失败原因

- [x] 实现成员材料状态页面
  - Done when:
    - 成员可查看本人材料的识别状态、校验状态和缺失材料提示
    - 不展示无关成员材料详情
    - 支持加载、失败和空列表状态

- [x] 收口登录态入口并开放成员自助识别处理
  - Done when:
    - 未登录用户首页和导航不再暴露成员、管理员或系统管理功能板块
    - 登录用户首页和导航只展示当前账号可进入的工作台入口
    - 成员可在材料状态页查看识别状态、发起重新识别，并对本人发票执行人工填写或更正
    - 成员重识别只允许作用于本人材料，不能放宽为任意识别结果状态写入权限

- [x] 实现发票人工录入和更正页面
  - Done when:
    - 可录入或更正发票号码、金额、抬头、税号、交易时间、费用类型
    - 展示字段来源、置信度和待确认提示
    - 保存后能触发或提示重新校验

- [x] 实现费用分摊编辑页面
  - Done when:
    - 可为一张发票添加一个或多个归属成员和金额
    - 前端显示分摊合计与发票金额差额
    - 服务端拒绝时展示具体错误，不在前端伪造成功

- [x] 实现成员费用确认页面
  - Done when:
    - 成员可查看本人费用明细、关联发票和附件摘要
    - 成员可确认或提交异议原因
    - 明细版本过旧或已失效时提示刷新

- [x] 实现管理员复核总览页面
  - Done when:
    - 管理员可聚合查看任务内材料、识别结果、校验结果、分摊和确认状态
    - Must 级失败、待归属材料、未确认成员和异议状态突出显示
    - 普通成员不能访问复核页面

- [x] 实现缺失材料清单页面
  - Done when:
    - 管理员可按成员、发票或费用类型查看缺失材料
    - 成员可查看本人缺失材料
    - 支持空清单状态

- [x] 实现管理员人工更正与提醒页面
  - Done when:
    - 管理员可从复核页进入识别字段更正和金额更正
    - 管理员可记录补材料提醒
    - 更正和提醒操作失败时不伪装成功

- [x] 实现导出任务页面
  - Done when:
    - 管理员可创建汇总表、成员明细表、发票明细表、缺失材料清单、财务草稿和 PDF 合并导出任务
    - 页面展示导出任务状态、失败原因和真实下载入口
    - 未满足最终确认条件时不能静默导出

- [x] 建立前端权限可见性测试
  - Done when:
    - 成员页面不渲染管理员操作
    - 管理员页面不泄露无关长期凭证或敏感配置
    - 测试覆盖无权限、加载中、错误状态

- [x] 建立前端表单和上传组件测试
  - Done when:
    - 任务创建表单、材料上传、分摊编辑和确认操作有组件或集成测试
    - 覆盖服务端错误展示
    - 不通过弱化断言制造通过

- [x] 建立前端主流程 E2E 占位
  - Done when:
    - E2E 测试覆盖管理员创建任务、成员上传材料、录入发票、分摊、确认、管理员复核和导出入口
    - 可使用 mock API 或本地后端，边界记录清楚
    - 不接入真实外部 AI、Telegram、邮件或对象存储

- [x] 重构 Web 首页与角色工作台信息架构
  - Done when:
    - 首页、管理员任务入口和成员任务入口不再以大段边界说明为主，而是优先展示当前阶段、异常摘要和下一步动作
    - 前端存在统一的工作台壳层、导航和阶段化工作流视觉元素，桌面与移动端都可用
    - 相关前端测试覆盖首页与两类角色入口的关键工作流文案和主操作入口

- [x] 统一业务文案映射并重构后台工作台视觉
  - Done when:
    - 首页、成员页、管理员页和系统管理员页不再展示 API、路由、字段名、状态码或内部实现描述
    - 前端存在统一的业务文案映射层、错误提示组件和后台风格工作台组件
    - 管理员首页以任务表格为主视觉区域，成员页与系统管理员页也以实际操作入口和状态信息为主
    - 相关前端测试覆盖首页、管理员页、成员页和错误提示的关键业务文案

## P2 - CLI 客户端与 CLI API

- [x] 建立 CLI 项目骨架
  - Done when:
    - CLI 有独立入口或模块边界
    - 至少支持健康检查或任务列表调用的占位命令
    - README 或 WORKLOG 记录本地运行方式

- [x] 定义 CLI JSON 输出规范
  - Done when:
    - CLI JSON 输出包含 schema version
    - 成功和失败输出都是合法 JSON
    - 测试覆盖 `--json` 成功和错误路径

- [x] 建立 CLI 登录和 Token 存储占位
  - Done when:
    - CLI 有登录或绑定身份命令边界
    - Token 不打印到日志
    - 本地存储策略和权限要求有记录

- [x] 增加 CLI 任务查询能力
  - Done when:
    - CLI 可列出当前可提交任务
    - 输出包含任务编号、比赛名称、状态和截止时间
    - 有最小测试或可验证命令

- [x] 增加 CLI 可见任务权限过滤
  - Done when:
    - CLI API 只返回当前成员可参与任务
    - 不返回无关比赛任务
    - 测试覆盖无可见任务和有可见任务

- [x] 增加 CLI 材料提交占位流程
  - Done when:
    - CLI 能接收本地文件路径并调用后端材料提交接口
    - 上传失败能显示明确原因
    - 不在日志中输出敏感 token

- [x] 增加 CLI 批量上传逐文件结果
  - Done when:
    - 多文件提交返回每个文件的成功或失败状态
    - 部分成功时退出码和输出语义明确
    - 测试覆盖部分成功

- [x] 增加 CLI 本地预检查
  - Done when:
    - 提交前检查本地文件存在、可读、大小和基础类型
    - 预检查失败不发起上传
    - 错误信息包含具体文件路径

- [x] 增加 CLI 状态查询能力
  - Done when:
    - CLI 可查询本人相关材料识别、校验、缺失材料和确认状态
    - 文本和 JSON 输出都可用
    - 不返回无关成员材料详情

- [x] 增加 CLI 缺失材料查询能力
  - Done when:
    - CLI 可按任务输出本人需要补充的材料
    - `--json` 输出结构化列表
    - 测试覆盖无缺失和有缺失

- [x] 增加 CLI 分摊提交能力
  - Done when:
    - CLI 可指定发票、成员和金额分摊
    - 金额合计不等于发票金额时显示服务端错误
    - 不在 CLI 复制服务端业务规则

- [x] 增加 CLI 个人费用确认能力
  - Done when:
    - CLI 可展示本人费用明细并提交确认
    - CLI 可提交异议说明
    - 费用明细版本过旧时提示重新拉取

- [x] 记录 CLI 版本兼容策略
  - Done when:
    - CLI 请求包含客户端版本或能力标识
    - 服务端可返回版本过旧错误
    - JSON 输出破坏性变更策略有记录

- [x] 评估 CLI 目录递归上传
  - Done when:
    - `WORKLOG.md` 记录是否作为第一阶段 Could 功能保留
    - 若保留，拆出独立实现任务
    - 不在本任务中实现递归上传

## P2 - Telegram 与邮件渠道

- [x] 建立渠道提交统一入口边界
  - Done when:
    - Web、CLI、Telegram、邮件都复用统一材料提交服务
    - 渠道层不实现独立业务规则
    - 有测试或文档说明调用边界

- [x] 建立 Telegram 账号绑定模型
  - Done when:
    - Telegram 账号可绑定成员身份
    - 未绑定账号提交材料进入待归属
    - 不记录 Telegram token 到仓库或日志

- [x] 增加 Telegram 材料提交接入占位
  - Done when:
    - 存在 Telegram 入站处理模块或接口边界
    - 可将已绑定账号材料转入统一材料流程
    - 不接入真实 Telegram Bot

- [x] 定义格式化邮件提交规范
  - Done when:
    - 文档记录邮件主题、任务编号、正文元数据和附件规则
    - 格式错误时应返回或记录明确失败原因
    - 不实现真实邮件收取

- [x] 增加邮件材料提交接入占位
  - Done when:
    - 存在邮件入站处理模块或接口边界
    - 可将格式化邮件附件转入统一材料流程
    - 格式不合规材料不会静默丢失

## P3 - 生产配置、真实识别与部署补齐

- [x] 刷新需求验收映射和生产就绪差距清单
  - Done when:
    - `docs/第一阶段验收映射.md` 基于当前代码、测试和 `TASKS.md` 重新标注 FR-001 至 FR-015、AC-001 至 AC-018 的状态
    - 明确区分“占位/骨架完成”和“可生产使用完成”
    - 列出上线阻断项，且不把未接入的外部 AI、对象存储、队列、迁移和权限审计描述为已完成

- [x] 建立统一后端运行配置模型
  - Done when:
    - 后端集中读取并校验 `DATABASE_URL`、`MATERIAL_STORAGE_DIR`、CORS 允许源、公开 API base URL、后端监听 host/port 的运行配置
    - 后端端口可通过环境变量或启动脚本参数配置，README 给出本地和生产示例
    - 配置校验失败时显式报错，不静默回退到不安全生产默认值
    - 测试覆盖默认开发配置、生产必填配置缺失和非法端口

- [x] 建立前端运行端口和 API 地址配置边界
  - Done when:
    - Vite 开发服务 host/port 可通过环境变量配置，且不会影响生产构建产物
    - 前端 API base URL 配置文档覆盖同源 `/api`、本地跨端口联调和生产反向代理三种场景
    - 前端不会在页面或日志中展示 LLM API key、后端 secret 或长期 token
    - 有最小测试或构建验证覆盖默认 API base URL 行为

- [x] 增加 OpenAI 兼容 LLM Provider 配置
  - Done when:
    - 后端支持通过环境变量配置 OpenAI 兼容接口的 `api_key`、`base_url`、`model`、超时和重试上限
    - API key 只从环境变量或密钥管理读取，不入库、不写日志、不返回前端
    - 未配置 LLM 时识别任务明确进入 `failed` 或 `disabled` 状态，而不是伪造识别成功
    - 测试覆盖配置读取、缺失密钥、base URL 规范化和日志脱敏

- [x] 实现真实 PDF 文本提取和识别输入构建
  - Done when:
    - 文本 PDF 可提取可复制文本并进入识别输入，不直接依赖 LLM 猜测文件内容
    - 图片或扫描 PDF 在 VLM 图像直提链路未接入前，必须显式标记 `ocr_not_configured` 或同类失败原因
    - 损坏、加密、空白或不可解析文件会记录具体材料编号和失败原因
    - 测试使用本地样例文件覆盖成功提取和失败路径

- [x] 接入 OpenAI 兼容 LLM 结构化识别最小闭环
  - Done when:
    - 识别服务调用可替换的 OpenAI 兼容 LLM 客户端，输出通过 Pydantic/JSON Schema 校验后才写入识别结果
    - 至少覆盖发票号码、金额、抬头、税号、交易时间、地点、费用类型和附件类型的结构化提取
    - LLM 超时、非 JSON、字段缺失或置信度不足时显式暴露失败或待确认状态
    - 单元测试使用 fake LLM provider，不依赖真实网络或真实 API key

- [x] 拆分异步识别和导出任务执行机制
  - Done when:
    - 原“建立异步识别和导出任务执行机制”已拆成共享运行模式、识别执行链、导出执行链三个子任务
    - 每个子任务都有独立的可验证 Done when，避免单轮同时改动识别、导出、worker 和配置
    - 本轮不改动业务代码，只调整任务边界

- [x] 建立异步任务共享运行模式与执行入口
  - Done when:
    - 识别任务和导出任务共享的异步执行模式有明确配置边界，例如 `in_process` 与 `worker`
    - 存在可运行的 worker 或等价命令入口，用于消费后续识别/导出任务
    - 开发环境可显式启用 in-process 模式，生产环境默认要求外部 worker 或明确拒绝同步执行
    - 测试覆盖运行模式配置解析和非法配置失败路径

- [x] 实现识别任务异步执行与重试可观测性
  - Done when:
    - 材料上传接口只负责创建识别任务，不在请求内同步执行 PDF/LLM 识别
    - 识别任务可由 worker 或等价执行入口消费，状态、失败原因、重试次数可查询
    - 重复投递同一识别任务时不产生重复业务结果，失败记录和幂等边界有测试覆盖
    - 保留当前显式手动触发接口作为开发/排障入口，或明确记录其替代关系

- [x] 实现导出任务异步执行与产物状态查询
  - Done when:
    - 导出 API 只创建导出任务，不在请求内同步生成耗时导出结果
    - 导出任务可由 worker 或等价执行入口消费，状态、失败原因、重试次数可查询
    - 重复投递同一导出任务时不产生重复业务结果，失败记录和幂等边界有测试覆盖
    - 导出产物就绪前后状态和访问边界有最小测试覆盖

- [x] 生产化数据库迁移机制
  - Done when:
    - 引入 Alembic 或等价迁移工具并建立当前 schema 基线
    - 共享环境不再依赖 `create_all` 自动演进 schema
    - README 记录迁移、回滚和本地旧库处理方式
    - CI/验证脚本覆盖迁移脚本可执行性

- [x] 生产化对象存储和导出文件访问
  - Done when:
    - 原始材料和导出文件可配置使用 S3 兼容对象存储，默认本地存储仅作为开发模式
    - 下载必须经过后端鉴权或短期签名链接，不能暴露长期公开 URL
    - 存储 key、bucket、endpoint 和凭据通过配置注入且脱敏
    - 测试覆盖本地存储适配器和对象存储适配器的接口契约

- [x] 收口生产账号初始化和注册策略
  - Done when:
    - 生产环境禁止任意用户自选 `admin` 或 `system_admin` 角色注册
    - 管理员账号初始化、邀请或审批边界明确，并有审计记录
    - 开发调试角色入口可通过配置关闭，生产构建或生产配置下不可见不可用
    - 测试覆盖生产模式越权注册失败

- [x] 建立生产部署清单和 Docker Compose 基线
  - Done when:
    - 提供可运行的 api、worker、web、postgres、redis、minio、reverse-proxy 组合部署基线或等价部署说明
    - 后端端口、前端端口、API base URL、数据库、对象存储、LLM base URL 和 LLM API key 均可通过环境变量配置
    - `.env.example` 只包含占位变量，不包含真实 secret
    - 部署说明包含健康检查、日志位置、迁移命令和初始管理员创建方式

- [x] 统一根目录 `.env` 配置入口
  - Done when:
    - 后端 API/worker、本地前端开发服务和 Docker Compose 统一从仓库根目录 `.env` 读取配置
    - 根目录 `.env.example` 覆盖后端、前端和部署所需占位变量
    - `README.md` 或部署文档明确 `.env` 的使用方式与优先级

- [x] 调整默认开发端口并补充开发环境模板
  - Done when:
    - 后端默认开发端口改为 `9876`，相关 CLI 默认地址、容器默认端口和部署健康检查保持一致
    - 根目录新增开发环境模板文件，覆盖本地直跑 API 和 Web 联调所需变量
    - 文档明确生产模板与开发模板的区别

- [x] 补齐本地 `uv` 启动的项目打包配置
  - Done when:
    - `uv sync` 会把当前仓库作为本地项目安装到 `.venv`
    - `uv run python -m trms_backend` 与 `uv run python -m trms_cli` 不再因 `src/` 布局缺少打包配置而报 `No module named ...`
    - 至少完成一次相关导入或帮助命令验证

- [x] 兼容 DeepSeek 结构化识别响应格式
  - Done when:
    - DeepSeek Provider 不再发送当前不支持的 `response_format.type=json_schema`
    - 对 DeepSeek 返回的 `json_object` 响应可兼容顶层 `output` 包装和直接字段对象两种形态
    - 有单元测试覆盖 OpenAI 分支和 DeepSeek 分支

- [x] 拆分上线前安全与恢复演练任务
  - Done when:
    - 原任务已拆分到权限收口、安全回归、备份恢复演练和主流程演练等更小任务
    - `WORKLOG.md` 记录拆分原因、前置依赖和后续执行顺序

## P3 - 权限、安全、审计与可观测性

- [x] 实现用户名密码注册登录基础闭环
  - Done when:
    - 后端提供 `/api/auth/register`、`/api/auth/login`、`/api/auth/me`、`/api/auth/logout`
    - 密码只保存哈希，不以明文落库或返回
    - Web 前端可注册、登录、保存会话并进入成员、管理员或系统管理员入口
    - 保留现有业务 API 的 `actor_id` / `submitter_id` 迁移边界，不伪装为已完成全量权限收口

- [x] 支持单账号多角色绑定与切换
  - Done when:
    - 后端用户模型可为同一账号保存多个角色，而不是单个 `role`
    - 登录态与 bearer 身份上下文能表达“当前激活角色”和“可切换角色集合”
    - 前端可在一个账号内切换成员、管理员或系统管理员工作台，而不暴露无权限板块
    - 相关鉴权和回归测试覆盖多角色账号的可见范围与越权失败路径

- [x] 建立最小请求身份上下文占位
  - Done when:
    - API 有统一方式表达当前用户、角色和成员编号
    - 现有直接传 `submitter_id` 或 `member_id` 的路径有迁移边界
    - 不要求接入真实 OAuth

- [x] 将 Web 业务 API 迁移到 bearer 身份上下文
  - Done when:
    - Web 前端业务请求携带登录返回的 bearer token
    - 后端从 token 解析当前用户，替代关键路径中的前端自报 `actor_id`、`submitter_id`、`member_id`
    - 保留 CLI、Telegram、邮件接入器的独立身份边界，不把渠道身份混入 Web 会话

- [x] 拆分基础权限控制任务
  - Done when:
    - 原“增加基础权限控制”任务已按成员侧、管理员侧和导出/异步作业边界拆成更小任务
    - `WORKLOG.md` 记录拆分原因、当前匿名旧契约范围和后续收口顺序

- [x] 收口成员侧业务 API 身份上下文与可见范围
  - Done when:
    - 成员侧查询、确认和材料可见性相关接口统一消费请求身份上下文
    - bearer 身份与显式 `actor_id` / `member_id` / `submitter_id` 不一致时显式拒绝
    - 成员只能访问本人相关材料、费用和确认记录

- [x] 收口管理员侧任务管理与复核权限
  - Done when:
    - 任务详情、成员管理、状态流转、提醒和复核摘要等管理员接口统一消费请求身份上下文
    - 非任务负责人不能管理无关任务
    - 匿名或身份不一致请求不能伪装管理员执行任务管理操作

- [x] 收口导出与异步作业权限边界
  - Done when:
    - 导出任务、导出文件下载、识别/导出异步作业管理接口统一消费请求身份上下文
    - 管理员只能管理自己负责任务的导出和相关异步作业
    - 为后续“增加导出文件访问控制测试”提供稳定前置边界

- [x] 增加导出文件访问控制测试
  - Done when:
    - 成员不能访问无关导出文件
    - 管理员能访问所负责任务的导出文件
    - 未实现真实导出时有明确边界测试

- [x] 增加 API 错误响应格式约束
  - Done when:
    - 常见 400、403、404、409 错误有一致响应结构
    - 现有测试更新为校验错误语义而非脆弱文本
    - 不吞掉真实异常

- [x] 增加审计日志模型骨架
  - Done when:
    - 关键操作可记录操作者、对象、动作、时间和结果
    - 不记录 secret 或完整敏感材料内容
    - 有最小单元测试

- [x] 记录材料提交和待归属认领审计
  - Done when:
    - 材料提交和待归属认领写入审计日志
    - 审计日志包含成功结果或失败原因
    - 测试覆盖至少一种成功操作

- [x] 建立材料删除标记边界
  - Done when:
    - 材料删除标记有明确领域语义和最小 API 或服务边界
    - 删除标记后的可见性和权限规则有测试或明确记录
    - 不直接物理删除原始材料和审计历史

- [x] 记录材料删除标记审计
  - Done when:
    - 材料删除标记写入审计日志
    - 审计日志包含操作者、目标材料和结果
    - 测试覆盖至少一种成功操作

- [x] 记录识别和人工更正审计
  - Done when:
    - 识别结果生成和人工更正字段写入审计日志
    - 更正记录包含修改前后差异摘要
    - 不记录完整敏感文件内容

- [x] 记录分摊和确认审计
  - Done when:
    - 分摊金额变更、成员确认和异议写入审计日志
    - 管理员处理异议或代确认路径若存在必须审计
    - 测试覆盖分摊变更

- [x] 记录导出和下载审计
  - Done when:
    - 导出任务创建、成功、失败和下载写入审计日志
    - 失败日志包含导出类型和失败原因
    - 不记录完整文件 URL 或长期访问凭证

- [x] 增加敏感信息日志脱敏规则
  - Done when:
    - Token、OAuth 密钥、Telegram Bot Token、邮件密码不会出现在日志
    - 文件 URL 或本地路径按规则脱敏
    - 测试或静态检查覆盖关键日志辅助函数

- [x] 建立请求 ID 日志上下文
  - Done when:
    - API 请求可生成或透传 request_id
    - 错误日志包含 request_id
    - 不要求接入完整 APM

- [x] 增加基础指标边界
  - Done when:
    - 上传成功率、识别任务状态、校验失败类型、导出任务状态有可扩展指标边界
    - 不引入不必要的新监控依赖
    - WORKLOG 记录当前取舍

- [x] 增加备份和恢复策略说明
  - Done when:
    - 文档记录数据库、对象存储和原始材料备份策略建议
    - 明确上线前需要恢复演练
    - 不实现真实备份任务

- [x] 执行数据库与对象存储备份恢复演练
  - Done when:
    - 已按当前部署基线执行一次数据库备份、恢复和结果核对
    - 已按当前对象存储基线执行一次原始材料或导出产物备份、恢复和可读性核对
    - `WORKLOG.md` 记录演练命令、结果、未覆盖风险和后续改进点

## P3 - 测试、质量与端到端验收

- [x] 增加规则层单元测试覆盖矩阵
  - Done when:
    - 抬头税号、大额支付记录、附件完整性、比赛范围、重复发票规则有测试矩阵
    - 每条规则覆盖通过、失败、待确认路径
    - 不通过删减断言制造通过

- [x] 增加权限越权回归测试
  - Done when:
    - 成员不能访问无关材料、费用、确认记录和导出
    - 非管理员不能进入管理员复核路径
    - 测试覆盖 403 而不是只覆盖 404

- [x] 增加材料上传集成测试
  - Done when:
    - 覆盖文件保存、hash、重复检测、批量部分成功
    - 覆盖不同渠道进入统一流程
    - 不依赖外部对象存储

- [x] 增加导出任务集成测试
  - Done when:
    - 覆盖导出任务创建、状态变化、失败原因
    - 覆盖损坏 PDF 报告材料编号
    - 不要求生成真实财务可用材料

- [x] 增加 CLI 参数解析测试
  - Done when:
    - 覆盖 login、list-tasks、submit、status、missing、split、confirm 命令
    - 覆盖 `--json` 输出
    - 覆盖本地文件预检查失败

- [x] 建立主流程 E2E 测试骨架
  - Done when:
    - 测试覆盖创建任务、开放、提交材料、录入发票、校验、分摊、确认、复核、导出占位
    - 可在本地 `./scripts/verify.sh` 中运行
    - 不接入真实外部 AI、Telegram、邮件或对象存储

- [x] 增加上线前安全回归验证
  - Done when:
    - 已完成权限越权、导出下载、日志脱敏、CORS 配置和生产注册策略的回归验证
    - 相关验证入口已纳入现有测试或验证脚本，并在 `WORKLOG.md` 记录执行结果
    - 不把未覆盖的外部依赖或人工步骤伪装成自动化通过

- [x] 执行上线前主流程 E2E 演练并记录风险
  - Done when:
    - 已执行主流程 E2E：创建任务、提交材料、真实或 fake LLM 识别、校验、分摊、确认、复核、导出
    - `WORKLOG.md` 明确记录仍未验证的外部依赖和上线风险
    - 不把 Telegram、邮件、真实 OCR 或 Browser Use 的未接入能力伪装为已演练

- [x] 更新 README 的第一阶段运行说明
  - Done when:
    - README 记录后端启动、测试、CLI 占位、当前未实现外部依赖
    - 不夸大未实现功能
    - 与 `TASKS.md` 当前阶段一致

## P4 - Could 与后续增强评估

- [x] 评估历史成员信息复用
  - Done when:
    - 记录是否进入第一阶段或后续阶段
    - 若进入第一阶段，拆出成员模板任务
    - 不直接实现

- [x] 评估常见比赛模板
  - Done when:
    - 记录模板包含字段和使用场景
    - 明确是否会污染当前任务创建主路径
    - 不直接实现

- [x] 评估 CLI 离线暂存后同步
  - Done when:
    - 记录离线暂存的数据安全风险和同步冲突风险
    - 明确是否推迟到后续阶段
    - 不直接实现

- [x] 实现 CLI 目录递归上传
  - Done when:
    - `trms-cli submit` 支持显式目录参数的递归展开，并保持现有逐文件结果语义
    - 递归阶段的本地失败可并入现有 `partial_success` / `failed` 输出
    - 明确并测试目录遍历顺序、是否跟随符号链接以及对不支持文件类型的处理

- [x] 评估自动生成成员补材料消息
  - Done when:
    - 记录消息模板、渠道和审计要求
    - 明确是否依赖通知模块
    - 不直接实现

- [x] 评估更复杂财务规则引擎
  - Done when:
    - 记录当前规则模块何时需要迁移到配置化或 DSL
    - 明确迁移触发条件
    - 不引入新依赖

- [x] 评估 Browser Use 后续阶段方案
  - Done when:
    - 记录 Browser Use 自动填报的风险、人工确认点和退出条件
    - 明确第一阶段仍不实现
    - 不保存财务系统登录态

## P5 - 前端 Material 3 重写

本批任务将 Web 前端整体迁移到 Material Design 3 设计体系，并同步优化用户交互逻辑（任务驱动总览、列表-详情联动、单任务闭环、统一反馈）。每条都是单轮可验证的最小任务。

- [x] 评估并确认 Material 3 React 落地方案
  - Done when:
    - `WORKLOG.md` 记录最终采用的 M3 库（MUI v7 / sixui / 其它）和拒绝其它方案的理由
    - 记录引入新依赖的范围、bundle 体积影响和测试改动面预估
    - 不实际安装任何依赖，不改动业务代码

- [x] 引入 Material 3 主题与基线依赖
  - Done when:
    - `web/` 引入 MUI v7（`@mui/material`、`@emotion/react`、`@emotion/styled`、`@mui/icons-material`）
    - 建立 `src/theme/m3-theme.ts`，含完整 M3 颜色 token、字体、形状、tonal elevation
    - `main.tsx` 包裹 ThemeProvider + CssBaseline，支持亮/暗模式跟随系统
    - 现有 `styles.css` 暂时保留，后续按页迁移
    - `npm run lint` / `npm test` / `npm run build` 全部通过

- [x] 重构应用骨架：Top App Bar + Navigation Rail + Snackbar 容器
  - Done when:
    - `RootLayout` 改为 M3 Top App Bar（左：菜单 + 品牌；右：账号头像菜单）
    - 桌面端使用 Navigation Rail，移动端使用 Bottom Navigation
    - 全局加入 SnackbarProvider 与可全局调用的 `useSnackbar` hook
    - 顶部菜单含主题切换（亮/暗）、退出登录、角色切换
    - 既有路由全部沿用，对应 RootLayout 测试同步更新

- [x] 重写登录/注册页交互
  - Done when:
    - 登录与注册分两个 Tab（M3 Tabs 或 SegmentedButton）
    - 已登录态不再渲染表单，改为显示已登录卡片 + 进入入口
    - 开发 mock 入口收口为页内可折叠区域，仅 dev 环境显示
    - 错误提示统一用 Snackbar，不再用页面级红色卡片
    - `auth.tsx` 与登录相关测试同步更新

- [x] 重写首页：任务驱动总览
  - Done when:
    - 移除"页面边界已收口/可见板块/页面边界"等实现性卡片
    - 成员首页：KPI 卡（待提交、待确认、已归档）+ "继续上次任务" + 任务列表
    - 管理员首页：KPI 卡（异常材料、未确认成员、待审核任务、待导出）+ "今日最紧急任务" + 全部任务表
    - 首页相关测试同步更新

  > 实现说明：当前阶段成员首页 KPI 与全部任务表已经在 `/member` 任务列表页（Round 6 范围）；管理员首页同样位于 `/admin` 任务列表（已存在）。本轮 `/` 总览页只负责把"未登录"和"已登录后选择工作台"的体验改为 M3 任务驱动卡片视图，不重复实现 KPI 数据计算；KPI 重写在对应角色任务列表轮次完成。

- [x] 重写成员端任务列表（M3 Material 视觉）
  - Done when:
    - 任务列表 `/member` 改为 MUI v7 Stack + KPI Card 网格 + Card 包裹的 MUI Table，移动端可用
    - 状态使用 MUI Chip 颜色映射，下一步动作和"进入工作台"按钮保持现有路由语义不变
    - 加载、空态、错误三种状态都有清晰的 M3 视觉
    - `member-task-list.test.tsx` 全部通过，断言语义不弱化

- [x] 重写成员单任务工作台（Tabs 化）
  - Done when:
    - `/member/invoices/workbench` 主体框架改为 M3 Tabs（发票 / 缺失材料 / 费用确认）
    - 顶部展示任务上下文卡（比赛名、状态、截止时间、当前成员），并支持任务切换
    - 列表项与详情仍保持现有业务行为，本轮不改后端接口
    - 现有 `member-invoice-workbench.test.tsx` 测试通过或同步更新

- [x] 让 dashboard 公共组件视觉对齐 MUI Material 3
  - Done when:
    - `web/src/components/dashboard.tsx` 中 `PageHeader` / `SectionCard` / `StatCard` / `EmptyState` / `ErrorMessage` / `RoleWorkspace` / `TaskTable` / `StatusBadge` 内部全部改为基于 MUI v7 实现
    - 对外 API（props 名、children、heading 角色、aria-label）保持不变，不破坏现有 30+ 个测试和业务页面
    - 视觉与新 M3 主题对齐：surface、elevation、Chip 状态色、Card 圆角、Typography scale
    - `npm run lint` / `npm test` / `npm run build` 全部通过

- [x] 重写成员材料上传交互（拖拽 Dialog + Snackbar）
  - Done when:
    - `/member/materials/upload` 改为 M3 拖拽上传卡 + 单文件状态显示
    - 上传成功/失败反馈通过 Snackbar 推送，不再用页面级红色卡片
    - 上传结果保留逐文件结果显示
    - `member-material-upload.test.tsx` 测试通过

- [x] 重写成员材料状态页（M3 列表 + 详情视图）
  - Done when:
    - `/member/materials/status` 改为 M3 列表 + 详情或 Card 网格视图
    - 识别状态、校验异常、缺失材料用统一 Chip 颜色表达
    - 重新识别和状态刷新动作走 Snackbar 反馈
    - `member-material-status.test.tsx` 测试通过

- [x] 重写成员费用确认页与缺失材料页
  - Done when:
    - `/member/expenses/confirm` 改为 M3 表单 + Stepper 或分卡组织
    - `/member/materials/missing` 改为 M3 列表
    - 确认与异议提交走 Snackbar 反馈
    - 相关测试通过

- [x] 收口成员端旧二级路由为工作台跳转
  - Done when:
    - `/member/materials/upload` `status` `missing` `expenses/confirm` 在工作台 Tabs 完成后改为重定向到 `/member/invoices/workbench` 对应 tab
    - 不破坏现有外部链接（旧 URL 仍可访问，自动跳转）
    - 相关测试更新

- [x] 重写管理员侧 workspace shell（Material 3 化导航与任务上下文）
  - Done when:
    - `web/src/app/admin-workspace-shell.tsx` 改为基于 MUI 的 List + ListItemButton + Card 实现
    - 模块导航使用 ListItemIcon + selected 高亮 + 图标，每项有标题与说明
    - 当前任务上下文卡用 Card + Stack + Chip + dl/dt/dd 网格 + 快捷入口按钮组
    - aria-label "管理员模块导航"、"当前任务上下文" 与 "当前任务快捷入口" 全部保留
    - 现有 admin-task-list / admin-task-detail / admin-export-tasks / admin-review-overview 等测试通过

- [x] 完善管理员任务详情：列表+详情联动深度优化
  - Done when:
    - `/admin/tasks/:id/review` 与 `/admin/tasks/:id/invoices` 在已是列表+详情的基础上，进一步用 M3 Tabs 区分识别字段、校验异常、附件预览
    - 单张发票预览、识别字段、校验异常、处理动作集中在右侧详情面板
    - 相关测试更新

- [x] 准备 Material 3 表单与上传基础组件
  - Done when:
    - 登录/注册页已采用 MUI TextField / Select（Round 4 完成）
    - dashboard 公共组件 `ErrorMessage` 改用 MUI Alert，错误展示语义统一
    - 新增 `web/src/components/FileDropZone.tsx`：M3 拖拽上传卡，支持点击选择、拖拽、单文件移除、文件类型图标、可选 hint，对外暴露 `files`、`onChange`、`accept`、`ariaLabel`、`fileListAriaLabel` 等 props，后续业务页面可直接消费
    - `npm run lint` / `npm test` / `npm run build` 全部通过

- [x] 拆分成员/管理员业务表单整体迁移任务
  - Done when:
    - 原“把成员/管理员业务表单整体迁移到 MUI TextField / Select / Autocomplete”已拆成按页面收口的独立子任务
    - 子任务至少覆盖任务创建、管理员发票录入、管理员分摊编辑、缺失材料筛选四个页面级表单
    - 上传区改造继续保留在独立的 `FileDropZone` 任务中，避免把文件输入和业务表单迁移混在同一轮

- [x] 迁移任务创建表单到 MUI TextField / Autocomplete / Checkbox 体系
  - Done when:
    - `web/src/app/admin-task-create.tsx` 的比赛信息、管理员与报销信息、发票抬头税号字段改用 MUI `TextField`
    - 成员名单行与费用类别选择改为更贴近 M3 的输入控件，并统一通过 `helperText` / `error` 表达校验
    - 相关测试同步更新

- [x] 迁移管理员发票录入表单到 MUI TextField / Select
  - Done when:
    - `web/src/app/admin-invoice-editor.tsx` 中发票编辑表单改用 MUI `TextField` / `Select`
    - 校验错误不再依赖自造 `.field-error`
    - 相关测试同步更新

- [x] 迁移管理员分摊编辑表单到 MUI TextField / Select
  - Done when:
    - `web/src/app/admin-split-editor.tsx` 的成员选择、金额和备注输入改用 MUI 表单控件
    - 行级校验通过 `helperText` / `error` 呈现，而不是继续手写错误标签
    - 相关测试同步更新

## 临时任务 - 2026-04-29 用户反馈修复

- [x] 让系统管理面板接入真实配置与巡检能力
  - Done when:
    - `/system` 不再只是静态占位卡片，而是能读取并更新至少一项真实系统级配置
    - 页面能展示可验证的运行态信息，而不是只显示写死的统计数字
    - 有前后端测试覆盖系统管理员可用、普通管理员/成员不可用和保存失败提示路径

- [x] 把开发环境快捷角色入口改成真实 dev 会话登录
  - Done when:
    - 登录页中的“以成员/管理员/系统管理员进入”不再只创建前端 mock 会话
    - 开发环境快捷入口会建立后端可识别的真实 bearer 会话，进入页面后的接口请求不再因匿名而失败
    - 有前端测试覆盖首次进入、已存在账号再次进入和失败提示路径

- [x] 允许管理员更新已创建任务的基础配置
  - Done when:
    - 后端存在受权限约束的任务基础信息更新接口，而不只是成员名单/状态更新
    - 管理员任务详情页存在可保存的编辑入口，至少覆盖比赛信息、成员名单、费用类别、项目/报销人信息和发票抬头税号
    - 有前后端测试覆盖草稿任务更新成功、非负责人拒绝和非草稿限制

- [x] 将“前端仍存在大量风格不协调界面”对齐到剩余 P5 收尾任务
  - Done when:
    - 已确认该问题当前由以下未完成任务覆盖：`迁移缺失材料筛选表单到 MUI Select`、`把材料上传场景接入 FileDropZone`、`把破坏性业务动作接入 ConfirmDialog`、`进一步收缩 styles.css 死代码`
    - 后续继续按这些最小任务逐项收口，不再新增一个过大的“统一重写前端风格”任务

- [x] 让 `scripts/codex-nightly.sh` 按未完成任务自动停止
  - Done when:
    - 脚本不再依赖 `MAX_ROUNDS` 作为停止条件
    - 每轮开始前和每轮结束后都通过 `TASKS.md` 中是否还存在 `- [ ]` 判断是否继续
    - 保留 `.codex-nightly/STOP` 的人工停止能力和现有 dirty working tree 防护

- [x] 迁移缺失材料筛选表单到 MUI Select
  - Done when:
    - `web/src/app/task-missing-materials.tsx` 中任务选择与查看维度筛选改用 MUI `Select`
    - 筛选表单保持现有成员侧权限与跳转语义，不顺带改动列表展示结构
    - 相关测试同步更新

- [x] 把材料上传场景接入 FileDropZone
  - Done when:
    - `/member/materials/upload`、`/member/invoices/workbench` 上传区使用 FileDropZone
    - 上传中通过 LinearProgress 表达进度，结果通过 Snackbar 反馈
    - 相关测试同步更新

- [x] 引入 ConfirmDialog 全局基础设施
  - Done when:
    - 新增 `web/src/components/ConfirmDialog.tsx` 提供 `ConfirmDialogProvider`
    - 新增 `useConfirmDialog()` hook，返回 `confirm(options): Promise<boolean>`
    - 支持 `tone`、`destructive`、`requireTyping`、`confirmLabel`、`cancelLabel` 等 M3 选项
    - `RootLayout` 包裹 `ConfirmDialogProvider`，业务页面无需改动即可使用

- [x] 把破坏性业务动作接入 ConfirmDialog
  - Done when:
    - 任务状态流转、删除、代确认、强制导出等动作前调用 `useConfirmDialog().confirm(...)`
    - 取消时不发起请求，确认时附带操作上下文记录
    - 测试覆盖确认与取消路径

- [x] 清理 `styles.css` 中与 MUI 主题冲突的全局 token
  - Done when:
    - 删除两个冲突的 `:root` 块、body 强制颜色与背景
    - 删除已被 MUI AppShell 完全替代的 `.topbar*`、`.workspace-shell`、`.workspace-header`、`.workspace-hero*`、`.workspace-nav*`、`.brand-mark`、`.session-pill` 等旧顶栏/Hero 类
    - 由 MUI CssBaseline 统一接管 body 字体、背景、字色
    - `npm run lint` / `npm run build` / `npm test` 全部通过

- [x] 进一步收缩 `styles.css` 死代码
  - Done when:
    - 排查并删除 `.workflow-*`、`.kpi-*`、`.dashboard-grid`、`.workspace-meta-grid`、`.task-insight*`、`.task-stage-line`、`.anomaly-chip*`、`.task-workflow*` 等当前 .tsx 中未被引用的死类
    - 保留仍被业务页 `<article className="task-card">` 等引用的辅助类
    - `npm run lint` / `npm run build` / `npm test` 全部通过

## 临时任务 - 2026-04-29 当前系统 review 收口

- [x] 让发票附件可点击进入附件详情页，并允许成员编辑附件识别字段
  - Done when:
    - 成员在发票详情页看到的已关联附件改为可点击卡片，点击后进入对应附件详情页
    - 附件详情页的识别字段改为可编辑表单，交互口径与发票页手工更正保持一致
    - 保存附件识别字段后，会刷新该材料关联发票的相关校验结果，不出现“只改前端展示不改后端状态”
    - 前后端测试覆盖附件卡片跳转、成员编辑成功、越权拒绝和校验刷新

- [x] 修正前端通用 API 错误解析，优先展示后端真实失败原因
  - Done when:
    - `web/src/lib/api/errors.ts` 与 `web/src/lib/ui-text.ts` 不再把大多数 `4xx` 统一折叠成“当前操作未完成，请检查填写内容后重试”
    - 登录/注册、任务表单、上传、分摊、确认等页面能展示后端 `detail`、`message` 与字段级校验信息
    - 前端测试覆盖登录失败、`409` 冲突、`403` 越权、`422` 字段校验至少四类路径

- [x] 收口成员专项上传页剩余非 M3 表单与操作控件
  - Done when:
    - `web/src/app/member-material-upload.tsx` 中任务选择、材料类型、提交按钮、结果状态标签与链接入口全部切换为 MUI / Material 3 组件
    - 页面不再依赖 `route-link`、原生 `select`、手写 `status-chip`
    - 相关测试同步更新

- [x] 重构成员发票工作台为“左侧发票列表 + 右侧详情面板”
  - Done when:
    - `web/src/app/member-invoice-workbench.tsx` 的发票 Tab 不再把全部发票详情纵向铺满
    - 左侧可选择当前任务下的本人发票/共享发票，右侧在单一上下文中展示识别、补录、分摊、附件和下一步动作
    - 桌面端主要操作尽量无需整页滚动；移动端保留可用降级布局
    - 前端测试覆盖发票切换、详情刷新和关键操作入口

- [x] 收口成员发票工作台剩余非 M3 表单与状态控件
  - Done when:
    - `member-invoice-workbench.tsx` 中材料类型、手动补录、分摊编辑、确认原因等原生 `input` / `select` / `textarea` / `button` 改为 MUI 表单与操作组件
    - 旧 `route-link` / `button-*` / `status-chip` 类不再承担核心交互
    - 相关测试同步更新

- [x] 收口成员材料状态页与费用确认页剩余非 M3 控件
  - Done when:
    - `member-material-status.tsx` 与 `member-expense-confirmation.tsx` 中残余原生表单、按钮、状态标签与链接动作统一替换为 MUI 组件
    - 页面只保留必要布局辅助类，不再依赖 legacy 操作样式类
    - 相关测试同步更新

- [x] 收口管理员复核、导出与提醒页剩余非 M3 控件
  - Done when:
    - `admin-review-overview.tsx`、`admin-export-tasks.tsx`、`admin-corrections-reminders.tsx` 中残余 `route-link`、手写 `status-chip`、原生表单控件替换为 MUI 组件
    - 复核列表/详情联动结构保持不变，但视觉、状态与操作反馈统一到 Material 3
    - 相关测试同步更新

- [x] 收口管理员任务列表/详情/发票录入/分摊编辑剩余非 M3 控件
  - Done when:
    - `admin-task-list.tsx`、`admin-task-detail.tsx`、`admin-invoice-editor.tsx`、`admin-split-editor.tsx` 的残余 legacy 按钮、状态标签、原生输入完全切换为 MUI
    - 页面仅保留必要布局辅助类，不再依赖 `.button-*`、`.route-link`、`.status-chip` 等旧交互样式
    - 相关测试同步更新

- [x] 补齐上传后识别调度闭环，消除 `in_process` / worker 语义错位
  - Done when:
    - `TRMS_ASYNC_JOB_MODE=in_process` 时，材料上传或显式重试能在请求内触发识别执行，而不是只停留在 `pending`
    - `TRMS_ASYNC_JOB_MODE=worker` 时，接口与前端明确提示“已入队等待 worker 消费”，不把排队误显示成系统异常
    - 测试覆盖 `in_process` 自动执行、`worker` 排队等待和无 worker 场景提示

- [x] 强化发票识别提示词与失败可诊断信息
  - Done when:
    - `recognition_llm.py` 的 system / user prompt 明确覆盖中文电子发票/纸票常见字段、金额单位、税号/抬头/时间抽取规则和“缺失不瞎猜”边界
    - 识别失败时保留足够的安全可审计上下文，便于区分“提示词未抽出字段”“模型输出不合法”“文件准备阶段失败”
    - 测试覆盖中文发票样例、字段缺失样例和非法 JSON / schema 失败样例

- [x] 为独立 worker 增加启动、轮询与任务结果结构化日志
  - Done when:
    - `uv run python -m trms_backend worker` 启动时输出运行模式、轮询间隔、已注册处理器和关键配置摘要（脱敏）
    - 每轮轮询、任务成功、任务失败、空闲等待都输出结构化日志，能定位 recognition/export job id、material/task id、failure reason
    - worker 日志不泄露 token / API key，相关测试覆盖日志脱敏和关键事件

- [x] 稳定 `main-flow-e2e-placeholder` 全量前端回归测试时序
  - Done when:
    - `cd web && npm test` 全量运行时，`src/app/main-flow-e2e-placeholder.test.tsx` 不再因默认 `5000ms` 超时而偶发失败
    - 已确认根因是测试本身时序/等待策略问题还是页面实现性能问题，而不是只靠盲目放大超时时间掩盖
    - `./scripts/verify.sh` 可稳定通过至少一次
