# TASKS

本任务队列根据 `README.md`、需求分析文档 V0.2、架构设计文档 V0.1 和当前代码状态生成。后续 Codex 每轮只应完成一个未完成且未阻塞的最小任务。

任务覆盖范围：

- FR-001 至 FR-015。
- 需求文档第 7 至 16 节中的 CLI、非功能、数据、权限、异常场景、优先级和第一阶段交付物。
- Web 前端成员提交入口、管理员后台、复核与导出操作界面。
- 架构文档中的模块边界、安全审计、可观测性、测试策略和验收映射。
- FR-011 财务系统 Browser Use 自动录入属于第一阶段 Won't have，只保留边界说明任务，不实现自动录入。
- 生产上线前必须补齐运行配置、端口配置、OpenAI 兼容 LLM Provider 配置、真实识别流水线、部署、迁移、对象存储、权限审计和备份恢复边界。

补充说明：

- 2026-04-28 需求文档对照 review 已发现高优先级偏差：匿名可读任务/材料数据、成员侧接口仍允许匿名自报 `actor_id`、Telegram/邮件渠道身份可伪造、扫描 PDF/图片 OCR 缺失、真实合并 PDF 导出缺失。
- 用户新增产品要求：同一比赛任务内的成员之间应可互相查看当前已上传发票。该要求与需求分析文档 V0.2 中“成员只能查看本人相关费用和材料状态”的旧表述冲突；后续任务按需求变更处理，并要求在 `WORKLOG.md` 中持续记录影响面。

## 临时任务 - 2026-04-29 UX 实测

- [x] 使用真实数据执行端到端 UX 测试并输出报告
  - Done when:
    - 已使用 `/home/gsh/财务/ICPC区域赛/区域赛报销` 的真实材料副本完成无头浏览器实测
    - `UX_TEST_REPORT.md` 已记录角色流程、问题清单、截图路径和复现方式
    - 必要的低风险 UX 修复已落地并单独说明

- [x] 按 UX 实测收口成员工作台识别阻塞提示
  - Done when:
    - 成员工作台不再把识别排队中的材料显示成“暂无识别记录”
    - 识别服务未配置时，成员工作台给出明确阻塞说明和补救路径
    - 前端测试覆盖识别排队中与识别服务未配置两类状态

- [x] 按 UX 实测明确任务创建页成员名单填写语义
  - Done when:
    - 创建任务页成员名单输入框明确提示填写“成员姓名或学号”
    - 页面文案显式说明当前阶段不要填写内部数据库 ID
    - 前端测试覆盖新提示文案

- [x] 按 UX 实测强调补材料提醒只保存内部记录
  - Done when:
    - 提醒表单在提交按钮附近明确说明不会自动发送消息
    - 提交成功反馈明确说明只保存内部提醒记录
    - 前端测试覆盖新的边界文案

- [x] 按 UX 实测增加上传前文件体积预检查
  - Done when:
    - 成员专项上传页在提交前拦截超过 10MB 的文件
    - 成员工作台上传区在提交前拦截超过 10MB 的文件
    - 前端测试覆盖专项上传页与工作台上传区两条路径

## P0 - 框架、盘点与基础设施

- [x] 搭建 Codex 自动化工作框架
  - Done when:
    - `AGENTS.md`、`TASKS.md`、`WORKLOG.md`、`BLOCKERS.md` 已建立
    - `scripts/verify.sh` 和 `scripts/codex-nightly.sh` 已建立
    - 基础脚本语法检查和 `git diff --check` 已纳入验证

- [x] 确认项目技术栈和启动方式
  - Done when:
    - `WORKLOG.md` 记录当前识别到的技术栈、入口和测试命令
    - `scripts/verify.sh` 能在本地执行完整基础验证
    - 不要求实现新业务功能

- [x] 固化后端健康检查验证
  - Done when:
    - 已有 `/health` 接口有明确测试覆盖
    - 验证脚本能运行相关测试
    - `WORKLOG.md` 记录后端启动命令

- [x] 梳理当前 API 能力清单
  - Done when:
    - `WORKLOG.md` 记录当前已实现的任务、材料、发票、分摊、确认接口
    - 标注与需求文档 FR-001 至 FR-015 的覆盖关系
    - 不改动业务逻辑

- [x] 整理第一阶段验收映射
  - Done when:
    - `TASKS.md` 或独立文档映射 AC-001 至 AC-018 的实现状态
    - 已完成、部分完成、未开始状态清晰
    - 下一批任务可从映射中继续拆分

- [x] 增加数据库迁移策略说明
  - Done when:
    - `WORKLOG.md` 或专门文档记录当前 `create_all` 策略的限制
    - 明确是否引入 Alembic 以及触发条件
    - 不要求立即迁移已有数据库

- [x] 明确第一阶段 Won't-have 边界
  - Done when:
    - `WORKLOG.md` 或文档记录 FR-011 Browser Use 自动录入、财务 API 对接、财务审批同步等第一阶段不实现
    - 说明后续如需启用 Browser Use 必须人工确认提交
    - 不实现任何浏览器自动化

## P0 - Review 发现对齐修复

- [x] 收口任务创建与匿名任务查询权限
  - Done when:
    - 创建报销任务必须要求已登录且具备管理员权限，匿名请求不能再直接创建任务
    - 任务列表、任务详情、任务成员和任务材料等接口不再把匿名请求当作可访问范围
    - 有 API 测试覆盖匿名、普通成员、任务管理员和无关管理员四类路径

- [x] 禁止成员侧接口通过匿名自报 `actor_id` 越权读取
  - Done when:
    - 费用明细、成员状态、缺失材料等成员侧接口必须依赖 bearer 身份上下文，而不是匿名请求自报 `actor_id`
    - bearer 身份与显式 `actor_id` / `member_id` 不一致时显式拒绝
    - 回归测试覆盖“知道成员编号但未登录”与“登录后冒充他人”两类失败路径

- [x] 收口 Telegram 绑定与提交身份边界
  - Done when:
    - Telegram 账号绑定不能再由匿名调用方随意为任意 `telegram_user_id` 建立绑定
    - Telegram 入站材料提交不再直接信任表单里的 `telegram_user_id` 作为真实身份来源
    - 未完成可信绑定或可信入站校验时，材料进入待归属而不是直接写入成员主链路

- [x] 收口格式化邮件提交成员身份解析边界
  - Done when:
    - 邮件入站不再接受匿名调用方直接指定 `resolved_member_id` 作为已确认成员身份
    - 成员身份只能来自受控绑定关系、受信任入站器或明确的待归属流程
    - 测试覆盖邮件伪造成员身份失败和未绑定进入待归属路径

- [x] 接入基于 VLM API 的扫描 PDF / 图片直提识别链路
  - Done when:
    - 图片、截图和纯扫描 PDF 不再统一停在 `ocr_not_configured`
    - 支持图像输入的 VLM API 可直接输出结构化识别结果，不要求先落传统 OCR 中间文本
    - VLM 输出继续经过结构化校验、字段置信度标注和失败显式暴露，不把模型结果当最终事实
    - 测试覆盖文本 PDF、扫描 PDF、图片、VLM 提取失败和空白文件路径

- [x] 实现真实合并打印 PDF 导出
  - Done when:
    - `merged_pdf` 导出生成真实 PDF 产物，而不是只返回计划占位 JSON
    - 异步导出任务、下载接口和前端页面都能展示真实产物状态与下载入口
    - 遇到损坏、加密或不可读材料时返回具体材料编号和失败原因，不静默跳过

- [x] 拆分成员发票工作台重构任务
  - Done when:
    - 原“重构成员发票工作台并补齐自助元数据管理”任务已拆成可单轮验证的子任务
    - 拆分结果覆盖“汇总视图、材料类型编辑、分摊对象编辑、入口收口”四个独立交付面
    - `WORKLOG.md` 已记录当前代码缺口与拆分依据

- [x] 建立成员发票工作台单任务汇总视图
  - Done when:
    - 成员可在单一任务上下文查看本人上传发票的识别字段、人工更正值、材料类型、当前分摊方案和确认状态
    - 页面按“待处理事项、异常原因、下一步动作”组织信息，不再要求用户在材料状态页和费用确认页之间自行拼接上下文
    - 有前端测试覆盖任务切换、摘要展示和关键异常提示

- [x] 增加成员侧材料类型自助更正能力
  - Done when:
    - 成员可在本人材料范围内修改发票或辅助材料的 `material_type`
    - 越权访问、非法类型和不允许修改的状态都会返回明确错误
    - 有 API 测试覆盖本人成功、越权失败和校验刷新边界

- [x] 增加成员侧金额分配对象自助调整能力
  - Done when:
    - 成员可在权限允许范围内调整本人发票的金额分配对象和分摊备注，且总额约束保持成立
    - 分摊调整后的确认状态刷新规则和失败原因在成员端可见
    - 有测试覆盖本人调整成功、无关成员被拒绝和确认状态重置

- [x] 收口成员发票工作台入口与下一步动作
  - Done when:
    - 成员任务列表和相关页面优先进入单任务发票工作台，而不是把材料状态、缺失材料、费用确认分散成并列主入口
    - 工作台内可明确跳转到上传、补材料、确认费用等下一步动作
    - 有前端测试覆盖主入口跳转和关键下一步动作入口

- [x] 调整任务内成员共享发票可见性策略
  - Done when:
    - 同一比赛任务内成员可查看当前已上传发票的基础元数据、当前分摊去向和必要附件摘要
    - 对“共享发票可见”与“敏感原始文件下载、支付截图全文、无关账号信息”之间的边界有明确规则和测试
    - `WORKLOG.md` 明确记录该任务属于相对原需求文档的产品变更，并同步更新相关权限测试假设

- [x] 补充原型图对照与 UI 规范文档
  - Done when:
    - 已读取 `docs/原型图.png` 并提炼出当前阶段必须遵循的基础信息架构和交互规范
    - 文档明确指出当前 UI 的主要不合理之处，而不是笼统写成“与原型不一致”
    - 文档明确“不要求完全复刻原型图，但必须遵循其基本规范”

- [x] 按原型图收口管理员端导航与任务推进信息架构
  - Done when:
    - 管理员端形成稳定的模块导航和当前任务上下文，不再主要依赖页面内部一组跳转按钮
    - 管理员首页优先展示任务推进阶段、异常数量和下一步动作，而不是泛化工作台说明
    - 有前端测试覆盖管理员任务入口、当前模块高亮和关键任务动作入口

- [x] 按原型图建立材料审核列表-详情联动视图
  - Done when:
    - 材料审核或复核主界面采用“材料列表 + 当前材料详情”联动结构
    - 同页可查看原始票据预览、识别字段、校验异常、分摊去向和处理动作
    - 不再要求管理员在复核、发票录入、分摊编辑之间频繁跳页才能完成单张发票处理

- [x] 按原型图收口成员端单任务处理闭环
  - Done when:
    - 成员可在单一任务上下文中连续完成上传材料、查看识别结果、查看缺失项、查看分摊去向和提交确认
    - 发票元数据、材料类型、关联附件和金额分配对象在成员侧有稳定可见入口
    - 页面不再把主要流程拆成多个弱关联页面要求用户自行判断下一步

## P1 - 报销任务与成员名单

- [x] 建立全局发票抬头和税号配置边界
  - Done when:
    - 存在可读取的全局抬头、税号配置模型或服务边界
    - 新建任务可默认继承全局抬头和税号
    - 有测试覆盖默认继承和任务级覆盖

- [x] 完善报销任务发布前校验
  - Done when:
    - 缺少成员名单、费用类别、项目信息、报销人信息时不能发布任务
    - 对应 API 测试覆盖成功和失败路径
    - `./scripts/verify.sh` 通过

- [x] 增加任务成员管理接口
  - Done when:
    - 可查询任务成员名单
    - 可在草稿状态添加、移除或更新任务成员
    - 开放提交后成员变更规则有测试或明确限制

- [x] 增加任务费用类别约束
  - Done when:
    - 任务只允许配置受支持的费用类别
    - 发票费用类型必须属于任务允许类别
    - 非法费用类别返回明确错误

- [x] 增加任务截止时间状态约束
  - Done when:
    - 超过截止时间的任务不能由成员继续提交材料
    - 管理员路径是否允许补交有明确规则或记录假设
    - 对应测试覆盖边界时间

- [x] 建立任务自动关闭检查边界
  - Done when:
    - 存在可调用的任务截止检查服务或命令
    - 到期开放任务可进入关闭状态
    - 不要求引入真实调度器

- [x] 增加任务状态流转条件检查
  - Done when:
    - `ready_to_export` 只能在复核条件满足时进入
    - `completed` 只能在导出完成后进入
    - 失败路径返回明确错误

## P1 - 材料提交与归档

- [x] 校验材料提交人必须属于任务成员
  - Done when:
    - 非任务成员不能向任务提交材料
    - Web、CLI、Telegram、Email 渠道统一经过该校验
    - 对应 API 测试覆盖

- [x] 增加材料类型与附件类型字段
  - Done when:
    - 材料可标记为发票、支付记录、比赛通知、行程单、订单截图或其他附件
    - API 能保存和返回材料类型
    - 非法类型返回明确错误

- [x] 建立材料文件保存抽象
  - Done when:
    - 原始上传文件有可替换的存储接口
    - 默认本地存储不会覆盖同名文件
    - 测试覆盖重复上传和文件元数据记录

- [x] 保存原始文件存储位置
  - Done when:
    - 材料记录保存不可变 storage key 或路径
    - 数据库不保存完整文件内容
    - 测试覆盖文件可由 storage key 定位

- [x] 增加材料上传文件校验规则
  - Done when:
    - 空文件、缺少文件名、不支持内容类型或超出大小限制时返回明确错误
    - 支持的文件类型在代码或配置中可查
    - 不吞掉上传失败原因

- [x] 支持批量上传部分成功结果
  - Done when:
    - 一次上传多个文件时返回逐文件成功或失败结果
    - 单个文件失败不伪装为全部成功
    - 测试覆盖部分成功和全部失败

- [x] 增加待归属材料状态
  - Done when:
    - 无法确定任务或提交人的材料可进入待归属状态
    - 普通成员不能访问待归属材料
    - 管理员后续处理边界有测试或明确 TODO

- [x] 建立待归属材料认领流程
  - Done when:
    - 管理员可将待归属材料绑定到任务和提交人
    - 认领操作记录操作者和时间
    - 普通成员不能认领无关材料

- [x] 增加跨渠道重复文件检测
  - Done when:
    - 同一任务内不同渠道提交相同文件 hash 时标记重复
    - 重复结果不依赖文件名
    - 测试覆盖 Web 与 CLI 或其他渠道重复场景

- [x] 建立发票与辅助材料关联模型
  - Done when:
    - 一张发票可关联支付记录、比赛通知、行程单、订单截图等材料
    - 一个附件是否可关联多张发票有明确规则或记录假设
    - 有最小测试覆盖关联和取消关联

## P1 - 发票、识别与人工更正

- [x] 建立 AI 识别任务占位模型
  - Done when:
    - 存在识别任务记录或领域模型，能表达 pending、succeeded、failed、needs_confirmation
    - AI 输出明确不是最终事实来源
    - 不接入真实外部 AI 服务

- [x] 保存识别原始结果和字段置信度
  - Done when:
    - 识别结果能保存原始响应、字段值、字段来源和置信度
    - 低置信度字段可标记为待确认
    - 测试覆盖低置信度路径

- [x] 建立识别任务触发边界
  - Done when:
    - 材料提交后能创建或排队识别任务占位
    - 不阻塞上传响应
    - 不调用真实 OCR 或外部 AI

- [x] 建立人工录入发票字段边界
  - Done when:
    - 管理员或成员可对材料录入发票号码、金额、抬头、税号、交易时间和费用类型
    - 录入后触发基础校验
    - 失败路径有测试

- [x] 增加人工更正识别字段记录
  - Done when:
    - 人工更正能记录字段来源和修改时间
    - 更正前后差异可追溯
    - 关键字段更正后会触发重新校验或记录待触发状态

- [x] 支持识别失败显式暴露
  - Done when:
    - OCR/PDF/AI 失败可记录失败原因
    - API 能返回材料识别失败状态
    - 失败不会被静默当作识别成功

- [x] 支持多次识别历史
  - Done when:
    - 同一材料可以保留多次识别尝试
    - 能查询最新有效结果和历史记录
    - 旧结果不会覆盖审计历史

## P1 - 校验规则与缺失材料

- [x] 扩展发票校验规则结果
  - Done when:
    - 校验结果包含 `rule_code`、目标对象、严重级别、状态、消息和结构化证据
    - 现有抬头、税号、重复发票规则继续通过
    - 对应测试覆盖

- [x] 未识别抬头或税号时输出待确认校验
  - Done when:
    - 抬头缺失时结果为 pending 或 failed，规则语义明确
    - 税号缺失时结果为 pending 或 failed，规则语义明确
    - 不把缺失字段静默当作通过

- [x] 实现金额超过阈值需要支付记录的校验骨架
  - Done when:
    - 单张发票金额达到配置阈值时输出缺失支付记录结果
    - 阈值默认值和配置来源有记录
    - 对应测试覆盖通过和失败路径

- [x] 实现支付记录金额匹配校验
  - Done when:
    - 已关联支付记录时可比较支付金额与发票金额
    - 金额缺失或不一致时输出明确校验结果
    - 是否要求精确匹配有记录或配置

- [x] 实现参赛费比赛通知校验
  - Done when:
    - 参赛费缺少比赛通知时输出 blocker 结果
    - 已关联比赛通知时通过
    - 规则不依赖文件名猜测

- [x] 实现航空费用附件完整性校验
  - Done when:
    - 航空费用缺少发票或行程单时输出缺失材料结果
    - 缺少舱位信息且无订单截图时输出待确认或失败
    - 测试覆盖主要分支

- [x] 实现网约车行程信息校验
  - Done when:
    - 市内交通为网约车且缺少行程信息时输出缺失材料结果
    - 无法判断是否网约车时输出待确认
    - 测试覆盖缺失和通过路径

- [x] 实现比赛时间范围校验
  - Done when:
    - 优先使用交易时间而不是开票时间
    - 默认比赛前后合理缓冲规则有记录
    - 缺少交易时间时输出待确认而非静默通过

- [x] 实现比赛地点范围校验
  - Done when:
    - 地点能与比赛城市、出发地、到达地或往返路径做基础匹配
    - 地点缺失时输出待确认
    - 测试覆盖匹配、不匹配和未知路径

- [x] 建立缺失材料清单模型
  - Done when:
    - 校验结果可聚合为成员或任务维度的缺失材料清单
    - 至少覆盖支付记录和比赛通知两类缺失项
    - 有测试覆盖聚合逻辑

- [x] 支持材料补充后重新校验
  - Done when:
    - 新增或关联附件后相关发票校验结果可重新计算
    - 旧校验结果不会被静默误用
    - 测试覆盖从失败变为通过

## P1 - 费用分摊与成员确认

- [x] 完善费用分摊提交权限
  - Done when:
    - 只有发票提交人、归属成员或管理员中的允许角色可提交分摊
    - 未授权成员不能修改无关发票分摊
    - 测试覆盖越权路径

- [x] 建立个人费用明细查询接口
  - Done when:
    - 成员可查询本人相关费用明细
    - 管理员可查询任务内全部费用明细
    - 普通成员不能查看无关成员明细

- [x] 支持成员费用异议处理状态
  - Done when:
    - 成员可提交异议说明
    - 异议状态能被管理员查询
    - 管理员处理异议后重新进入待确认有明确边界

- [x] 实现费用分摊确认失效规则
  - Done when:
    - 管理员或系统修改分摊金额后，相关成员确认状态失效
    - 旧确认记录不被静默当作有效确认
    - 对应回归测试覆盖

- [x] 引入费用明细版本号
  - Done when:
    - 成员确认绑定到具体费用明细版本
    - 金额或归属变化会生成新版本或递增版本
    - 查询接口能区分当前有效确认与历史确认

- [x] 支持成员逾期未确认识别
  - Done when:
    - 可查询任务内逾期未确认成员
    - 逾期判断基于任务截止时间或确认截止规则
    - 不静默把未确认当作已确认

- [x] 禁止管理员代确认默认路径
  - Done when:
    - 第一阶段默认不允许管理员代成员确认
    - 如果保留代确认接口占位，必须要求原因并审计
    - 有测试覆盖默认禁止

## P1 - 管理员复核与状态流转

- [x] 建立管理员复核状态流转
  - Done when:
    - 任务可从关闭进入复核，并在 Must 级问题清零后进入可导出状态
    - 有失败校验时不能最终确认
    - 对应测试覆盖主要状态流转

- [x] 建立复核汇总查询接口
  - Done when:
    - 管理员可查看任务内材料、识别结果、校验结果、分摊和确认状态聚合
    - 普通成员不可调用管理员复核视图
    - 有最小 API 测试

- [x] 阻止存在待归属材料的最终确认
  - Done when:
    - 任务存在待归属材料时不能进入可导出状态
    - 错误响应指出待处理材料数量或编号
    - 测试覆盖

- [x] 阻止存在未确认成员的最终确认
  - Done when:
    - 任一相关成员未确认当前费用明细时不能最终确认
    - 异议状态不能被静默当作确认
    - 测试覆盖未确认和异议路径

- [x] 支持管理员补材料提醒记录
  - Done when:
    - 管理员可记录对成员的补材料提醒
    - 提醒内容、目标成员、任务和时间可查询
    - 不要求接入真实短信、邮件或 Telegram 发送

- [x] 建立系统自动提醒占位
  - Done when:
    - 可根据缺失材料和未确认状态生成提醒任务占位
    - 生成逻辑幂等
    - 不调用真实外部通知渠道

## P1 - 导出与打印材料

- [x] 建立导出模块边界骨架
  - Done when:
    - 代码中存在导出模块的清晰占位边界
    - 不生成真实财务材料
    - 有最小测试确认接口或服务边界可调用

- [x] 建立导出任务模型
  - Done when:
    - 导出任务能表达 pending、running、succeeded、failed
    - 记录导出类型、任务编号、操作者、生成参数和时间
    - 有最小持久化测试

- [x] 导出报销汇总表
  - Done when:
    - 可按任务生成费用类型和总金额汇总
    - 输出格式优先 CSV 或 XLSX 边界明确
    - 测试覆盖汇总金额正确

- [x] 导出成员报销明细表
  - Done when:
    - 可按成员列出费用明细和金额
    - 只使用当前有效费用版本
    - 测试覆盖多人分摊

- [x] 导出发票明细表
  - Done when:
    - 可导出发票号码、金额、费用类型、提交人、校验状态
    - 重复发票和异常校验状态可见
    - 测试覆盖基础字段

- [x] 导出缺失材料清单
  - Done when:
    - 可按成员和费用列出缺失材料
    - 至少覆盖支付记录、比赛通知、行程信息
    - 测试覆盖空清单和非空清单

- [x] 生成财务填报草稿
  - Done when:
    - 可输出项目、报销人、总金额、发票明细等财务录入辅助信息
    - 输出不包含敏感 token 或本地私有路径
    - 测试覆盖字段完整性

- [x] 合并打印 PDF 占位
  - Done when:
    - 存在合并打印 PDF 的服务边界
    - 默认顺序按架构文档记录
    - 遇到损坏或加密 PDF 时返回具体材料编号，不静默跳过

- [x] 绑定导出结果到任务版本
  - Done when:
    - 导出文件记录生成时任务状态或数据版本
    - 数据变化后旧导出不会被当作最新
    - 测试覆盖导出后修改数据路径

## P1 - Web 前端与管理员后台

- [x] 确认 Web 前端技术栈和工程边界
  - Done when:
    - `WORKLOG.md` 记录是否采用架构文档建议的 React、TypeScript、Vite 和管理后台组件库
    - 明确前端目录、启动命令、构建命令和测试命令
    - 不实现业务页面

- [x] 建立 Web 前端项目骨架
  - Done when:
    - 存在独立前端入口、路由边界和 API 客户端边界
    - `./scripts/verify.sh` 能在存在前端工程时运行 lint/test/build
    - README 或 WORKLOG 记录本地启动方式

- [x] 建立前端 API 类型与错误处理边界
  - Done when:
    - 前端有任务、材料、发票、分摊、确认、校验和导出的基础类型定义
    - API 错误能以统一结构展示给用户
    - 不在前端吞掉服务端错误

- [x] 建立 Web 登录和角色入口占位
  - Done when:
    - 前端能表达成员、管理员和系统管理员三类角色入口
    - 未登录用户不能进入业务页面
    - 不接入真实 OAuth 时有明确 mock 或占位边界

- [x] 实现管理员任务列表页面
  - Done when:
    - 管理员可查看任务编号、比赛名称、状态、截止时间和异常摘要
    - 支持按状态筛选或基础搜索
    - 页面有加载、空状态和错误状态

- [x] 实现管理员任务创建页面
  - Done when:
    - 表单覆盖比赛名称、地点、起止时间、成员名单、费用类别、截止时间、管理员、项目、报销人、抬头和税号
    - 前端校验必填、日期顺序和空成员项
    - 提交失败展示服务端错误

- [x] 实现任务详情与状态操作页面
  - Done when:
    - 管理员可查看任务基础信息、成员名单、费用类别和当前状态
    - 可执行允许的状态流转操作
    - 非法流转错误清晰展示

- [x] 实现成员 Web 可提交任务页面
  - Done when:
    - 成员只能看到本人可参与或已参与任务
    - 显示比赛名称、比赛时间、任务状态和截止时间
    - 无可见任务时有明确空状态

- [x] 实现成员 Web 材料上传页面
  - Done when:
    - 成员可选择任务并上传一个或多个发票或附件文件
    - 可选择材料类型和提交渠道为 Web
    - 上传结果展示材料编号、重复状态和失败原因

- [x] 实现成员材料状态页面
  - Done when:
    - 成员可查看本人材料的识别状态、校验状态和缺失材料提示
    - 不展示无关成员材料详情
    - 支持加载、失败和空列表状态

- [x] 收口登录态入口并开放成员自助识别处理
  - Done when:
    - 未登录用户首页和导航不再暴露成员、管理员或系统管理功能板块
    - 登录用户首页和导航只展示当前账号可进入的工作台入口
    - 成员可在材料状态页查看识别状态、发起重新识别，并对本人发票执行人工填写或更正
    - 成员重识别只允许作用于本人材料，不能放宽为任意识别结果状态写入权限

- [x] 实现发票人工录入和更正页面
  - Done when:
    - 可录入或更正发票号码、金额、抬头、税号、交易时间、费用类型
    - 展示字段来源、置信度和待确认提示
    - 保存后能触发或提示重新校验

- [x] 实现费用分摊编辑页面
  - Done when:
    - 可为一张发票添加一个或多个归属成员和金额
    - 前端显示分摊合计与发票金额差额
    - 服务端拒绝时展示具体错误，不在前端伪造成功

- [x] 实现成员费用确认页面
  - Done when:
    - 成员可查看本人费用明细、关联发票和附件摘要
    - 成员可确认或提交异议原因
    - 明细版本过旧或已失效时提示刷新

- [x] 实现管理员复核总览页面
  - Done when:
    - 管理员可聚合查看任务内材料、识别结果、校验结果、分摊和确认状态
    - Must 级失败、待归属材料、未确认成员和异议状态突出显示
    - 普通成员不能访问复核页面

- [x] 实现缺失材料清单页面
  - Done when:
    - 管理员可按成员、发票或费用类型查看缺失材料
    - 成员可查看本人缺失材料
    - 支持空清单状态

- [x] 实现管理员人工更正与提醒页面
  - Done when:
    - 管理员可从复核页进入识别字段更正和金额更正
    - 管理员可记录补材料提醒
    - 更正和提醒操作失败时不伪装成功

- [x] 实现导出任务页面
  - Done when:
    - 管理员可创建汇总表、成员明细表、发票明细表、缺失材料清单、财务草稿和 PDF 合并导出任务
    - 页面展示导出任务状态、失败原因和真实下载入口
    - 未满足最终确认条件时不能静默导出

- [x] 建立前端权限可见性测试
  - Done when:
    - 成员页面不渲染管理员操作
    - 管理员页面不泄露无关长期凭证或敏感配置
    - 测试覆盖无权限、加载中、错误状态

- [x] 建立前端表单和上传组件测试
  - Done when:
    - 任务创建表单、材料上传、分摊编辑和确认操作有组件或集成测试
    - 覆盖服务端错误展示
    - 不通过弱化断言制造通过

- [x] 建立前端主流程 E2E 占位
  - Done when:
    - E2E 测试覆盖管理员创建任务、成员上传材料、录入发票、分摊、确认、管理员复核和导出入口
    - 可使用 mock API 或本地后端，边界记录清楚
    - 不接入真实外部 AI、Telegram、邮件或对象存储

- [x] 重构 Web 首页与角色工作台信息架构
  - Done when:
    - 首页、管理员任务入口和成员任务入口不再以大段边界说明为主，而是优先展示当前阶段、异常摘要和下一步动作
    - 前端存在统一的工作台壳层、导航和阶段化工作流视觉元素，桌面与移动端都可用
    - 相关前端测试覆盖首页与两类角色入口的关键工作流文案和主操作入口

- [x] 统一业务文案映射并重构后台工作台视觉
  - Done when:
    - 首页、成员页、管理员页和系统管理员页不再展示 API、路由、字段名、状态码或内部实现描述
    - 前端存在统一的业务文案映射层、错误提示组件和后台风格工作台组件
    - 管理员首页以任务表格为主视觉区域，成员页与系统管理员页也以实际操作入口和状态信息为主
    - 相关前端测试覆盖首页、管理员页、成员页和错误提示的关键业务文案

## P2 - CLI 客户端与 CLI API

- [x] 建立 CLI 项目骨架
  - Done when:
    - CLI 有独立入口或模块边界
    - 至少支持健康检查或任务列表调用的占位命令
    - README 或 WORKLOG 记录本地运行方式

- [x] 定义 CLI JSON 输出规范
  - Done when:
    - CLI JSON 输出包含 schema version
    - 成功和失败输出都是合法 JSON
    - 测试覆盖 `--json` 成功和错误路径

- [x] 建立 CLI 登录和 Token 存储占位
  - Done when:
    - CLI 有登录或绑定身份命令边界
    - Token 不打印到日志
    - 本地存储策略和权限要求有记录

- [x] 增加 CLI 任务查询能力
  - Done when:
    - CLI 可列出当前可提交任务
    - 输出包含任务编号、比赛名称、状态和截止时间
    - 有最小测试或可验证命令

- [x] 增加 CLI 可见任务权限过滤
  - Done when:
    - CLI API 只返回当前成员可参与任务
    - 不返回无关比赛任务
    - 测试覆盖无可见任务和有可见任务

- [x] 增加 CLI 材料提交占位流程
  - Done when:
    - CLI 能接收本地文件路径并调用后端材料提交接口
    - 上传失败能显示明确原因
    - 不在日志中输出敏感 token

- [x] 增加 CLI 批量上传逐文件结果
  - Done when:
    - 多文件提交返回每个文件的成功或失败状态
    - 部分成功时退出码和输出语义明确
    - 测试覆盖部分成功

- [x] 增加 CLI 本地预检查
  - Done when:
    - 提交前检查本地文件存在、可读、大小和基础类型
    - 预检查失败不发起上传
    - 错误信息包含具体文件路径

- [x] 增加 CLI 状态查询能力
  - Done when:
    - CLI 可查询本人相关材料识别、校验、缺失材料和确认状态
    - 文本和 JSON 输出都可用
    - 不返回无关成员材料详情

- [x] 增加 CLI 缺失材料查询能力
  - Done when:
    - CLI 可按任务输出本人需要补充的材料
    - `--json` 输出结构化列表
    - 测试覆盖无缺失和有缺失

- [x] 增加 CLI 分摊提交能力
  - Done when:
    - CLI 可指定发票、成员和金额分摊
    - 金额合计不等于发票金额时显示服务端错误
    - 不在 CLI 复制服务端业务规则

- [x] 增加 CLI 个人费用确认能力
  - Done when:
    - CLI 可展示本人费用明细并提交确认
    - CLI 可提交异议说明
    - 费用明细版本过旧时提示重新拉取

- [x] 记录 CLI 版本兼容策略
  - Done when:
    - CLI 请求包含客户端版本或能力标识
    - 服务端可返回版本过旧错误
    - JSON 输出破坏性变更策略有记录

- [x] 评估 CLI 目录递归上传
  - Done when:
    - `WORKLOG.md` 记录是否作为第一阶段 Could 功能保留
    - 若保留，拆出独立实现任务
    - 不在本任务中实现递归上传

## P2 - Telegram 与邮件渠道

- [x] 建立渠道提交统一入口边界
  - Done when:
    - Web、CLI、Telegram、邮件都复用统一材料提交服务
    - 渠道层不实现独立业务规则
    - 有测试或文档说明调用边界

- [x] 建立 Telegram 账号绑定模型
  - Done when:
    - Telegram 账号可绑定成员身份
    - 未绑定账号提交材料进入待归属
    - 不记录 Telegram token 到仓库或日志

- [x] 增加 Telegram 材料提交接入占位
  - Done when:
    - 存在 Telegram 入站处理模块或接口边界
    - 可将已绑定账号材料转入统一材料流程
    - 不接入真实 Telegram Bot

- [x] 定义格式化邮件提交规范
  - Done when:
    - 文档记录邮件主题、任务编号、正文元数据和附件规则
    - 格式错误时应返回或记录明确失败原因
    - 不实现真实邮件收取

- [x] 增加邮件材料提交接入占位
  - Done when:
    - 存在邮件入站处理模块或接口边界
    - 可将格式化邮件附件转入统一材料流程
    - 格式不合规材料不会静默丢失

## P3 - 生产配置、真实识别与部署补齐

- [x] 刷新需求验收映射和生产就绪差距清单
  - Done when:
    - `docs/第一阶段验收映射.md` 基于当前代码、测试和 `TASKS.md` 重新标注 FR-001 至 FR-015、AC-001 至 AC-018 的状态
    - 明确区分“占位/骨架完成”和“可生产使用完成”
    - 列出上线阻断项，且不把未接入的外部 AI、对象存储、队列、迁移和权限审计描述为已完成

- [x] 建立统一后端运行配置模型
  - Done when:
    - 后端集中读取并校验 `DATABASE_URL`、`MATERIAL_STORAGE_DIR`、CORS 允许源、公开 API base URL、后端监听 host/port 的运行配置
    - 后端端口可通过环境变量或启动脚本参数配置，README 给出本地和生产示例
    - 配置校验失败时显式报错，不静默回退到不安全生产默认值
    - 测试覆盖默认开发配置、生产必填配置缺失和非法端口

- [x] 建立前端运行端口和 API 地址配置边界
  - Done when:
    - Vite 开发服务 host/port 可通过环境变量配置，且不会影响生产构建产物
    - 前端 API base URL 配置文档覆盖同源 `/api`、本地跨端口联调和生产反向代理三种场景
    - 前端不会在页面或日志中展示 LLM API key、后端 secret 或长期 token
    - 有最小测试或构建验证覆盖默认 API base URL 行为

- [x] 增加 OpenAI 兼容 LLM Provider 配置
  - Done when:
    - 后端支持通过环境变量配置 OpenAI 兼容接口的 `api_key`、`base_url`、`model`、超时和重试上限
    - API key 只从环境变量或密钥管理读取，不入库、不写日志、不返回前端
    - 未配置 LLM 时识别任务明确进入 `failed` 或 `disabled` 状态，而不是伪造识别成功
    - 测试覆盖配置读取、缺失密钥、base URL 规范化和日志脱敏

- [x] 实现真实 PDF 文本提取和识别输入构建
  - Done when:
    - 文本 PDF 可提取可复制文本并进入识别输入，不直接依赖 LLM 猜测文件内容
    - 图片或扫描 PDF 在 VLM 图像直提链路未接入前，必须显式标记 `ocr_not_configured` 或同类失败原因
    - 损坏、加密、空白或不可解析文件会记录具体材料编号和失败原因
    - 测试使用本地样例文件覆盖成功提取和失败路径

- [x] 接入 OpenAI 兼容 LLM 结构化识别最小闭环
  - Done when:
    - 识别服务调用可替换的 OpenAI 兼容 LLM 客户端，输出通过 Pydantic/JSON Schema 校验后才写入识别结果
    - 至少覆盖发票号码、金额、抬头、税号、交易时间、地点、费用类型和附件类型的结构化提取
    - LLM 超时、非 JSON、字段缺失或置信度不足时显式暴露失败或待确认状态
    - 单元测试使用 fake LLM provider，不依赖真实网络或真实 API key

- [x] 拆分异步识别和导出任务执行机制
  - Done when:
    - 原“建立异步识别和导出任务执行机制”已拆成共享运行模式、识别执行链、导出执行链三个子任务
    - 每个子任务都有独立的可验证 Done when，避免单轮同时改动识别、导出、worker 和配置
    - 本轮不改动业务代码，只调整任务边界

- [x] 建立异步任务共享运行模式与执行入口
  - Done when:
    - 识别任务和导出任务共享的异步执行模式有明确配置边界，例如 `in_process` 与 `worker`
    - 存在可运行的 worker 或等价命令入口，用于消费后续识别/导出任务
    - 开发环境可显式启用 in-process 模式，生产环境默认要求外部 worker 或明确拒绝同步执行
    - 测试覆盖运行模式配置解析和非法配置失败路径

- [x] 实现识别任务异步执行与重试可观测性
  - Done when:
    - 材料上传接口只负责创建识别任务，不在请求内同步执行 PDF/LLM 识别
    - 识别任务可由 worker 或等价执行入口消费，状态、失败原因、重试次数可查询
    - 重复投递同一识别任务时不产生重复业务结果，失败记录和幂等边界有测试覆盖
    - 保留当前显式手动触发接口作为开发/排障入口，或明确记录其替代关系

- [x] 实现导出任务异步执行与产物状态查询
  - Done when:
    - 导出 API 只创建导出任务，不在请求内同步生成耗时导出结果
    - 导出任务可由 worker 或等价执行入口消费，状态、失败原因、重试次数可查询
    - 重复投递同一导出任务时不产生重复业务结果，失败记录和幂等边界有测试覆盖
    - 导出产物就绪前后状态和访问边界有最小测试覆盖

- [x] 生产化数据库迁移机制
  - Done when:
    - 引入 Alembic 或等价迁移工具并建立当前 schema 基线
    - 共享环境不再依赖 `create_all` 自动演进 schema
    - README 记录迁移、回滚和本地旧库处理方式
    - CI/验证脚本覆盖迁移脚本可执行性

- [x] 生产化对象存储和导出文件访问
  - Done when:
    - 原始材料和导出文件可配置使用 S3 兼容对象存储，默认本地存储仅作为开发模式
    - 下载必须经过后端鉴权或短期签名链接，不能暴露长期公开 URL
    - 存储 key、bucket、endpoint 和凭据通过配置注入且脱敏
    - 测试覆盖本地存储适配器和对象存储适配器的接口契约

- [x] 收口生产账号初始化和注册策略
  - Done when:
    - 生产环境禁止任意用户自选 `admin` 或 `system_admin` 角色注册
    - 管理员账号初始化、邀请或审批边界明确，并有审计记录
    - 开发调试角色入口可通过配置关闭，生产构建或生产配置下不可见不可用
    - 测试覆盖生产模式越权注册失败

- [x] 建立生产部署清单和 Docker Compose 基线
  - Done when:
    - 提供可运行的 api、worker、web、postgres、redis、minio、reverse-proxy 组合部署基线或等价部署说明
    - 后端端口、前端端口、API base URL、数据库、对象存储、LLM base URL 和 LLM API key 均可通过环境变量配置
    - `.env.example` 只包含占位变量，不包含真实 secret
    - 部署说明包含健康检查、日志位置、迁移命令和初始管理员创建方式

- [x] 统一根目录 `.env` 配置入口
  - Done when:
    - 后端 API/worker、本地前端开发服务和 Docker Compose 统一从仓库根目录 `.env` 读取配置
    - 根目录 `.env.example` 覆盖后端、前端和部署所需占位变量
    - `README.md` 或部署文档明确 `.env` 的使用方式与优先级

- [x] 调整默认开发端口并补充开发环境模板
  - Done when:
    - 后端默认开发端口改为 `9876`，相关 CLI 默认地址、容器默认端口和部署健康检查保持一致
    - 根目录新增开发环境模板文件，覆盖本地直跑 API 和 Web 联调所需变量
    - 文档明确生产模板与开发模板的区别

- [x] 补齐本地 `uv` 启动的项目打包配置
  - Done when:
    - `uv sync` 会把当前仓库作为本地项目安装到 `.venv`
    - `uv run python -m trms_backend` 与 `uv run python -m trms_cli` 不再因 `src/` 布局缺少打包配置而报 `No module named ...`
    - 至少完成一次相关导入或帮助命令验证

- [x] 兼容 DeepSeek 结构化识别响应格式
  - Done when:
    - DeepSeek Provider 不再发送当前不支持的 `response_format.type=json_schema`
    - 对 DeepSeek 返回的 `json_object` 响应可兼容顶层 `output` 包装和直接字段对象两种形态
    - 有单元测试覆盖 OpenAI 分支和 DeepSeek 分支

- [x] 拆分上线前安全与恢复演练任务
  - Done when:
    - 原任务已拆分到权限收口、安全回归、备份恢复演练和主流程演练等更小任务
    - `WORKLOG.md` 记录拆分原因、前置依赖和后续执行顺序

## P3 - 权限、安全、审计与可观测性

- [x] 实现用户名密码注册登录基础闭环
  - Done when:
    - 后端提供 `/api/auth/register`、`/api/auth/login`、`/api/auth/me`、`/api/auth/logout`
    - 密码只保存哈希，不以明文落库或返回
    - Web 前端可注册、登录、保存会话并进入成员、管理员或系统管理员入口
    - 保留现有业务 API 的 `actor_id` / `submitter_id` 迁移边界，不伪装为已完成全量权限收口

- [x] 支持单账号多角色绑定与切换
  - Done when:
    - 后端用户模型可为同一账号保存多个角色，而不是单个 `role`
    - 登录态与 bearer 身份上下文能表达“当前激活角色”和“可切换角色集合”
    - 前端可在一个账号内切换成员、管理员或系统管理员工作台，而不暴露无权限板块
    - 相关鉴权和回归测试覆盖多角色账号的可见范围与越权失败路径

- [x] 建立最小请求身份上下文占位
  - Done when:
    - API 有统一方式表达当前用户、角色和成员编号
    - 现有直接传 `submitter_id` 或 `member_id` 的路径有迁移边界
    - 不要求接入真实 OAuth

- [x] 将 Web 业务 API 迁移到 bearer 身份上下文
  - Done when:
    - Web 前端业务请求携带登录返回的 bearer token
    - 后端从 token 解析当前用户，替代关键路径中的前端自报 `actor_id`、`submitter_id`、`member_id`
    - 保留 CLI、Telegram、邮件接入器的独立身份边界，不把渠道身份混入 Web 会话

- [x] 拆分基础权限控制任务
  - Done when:
    - 原“增加基础权限控制”任务已按成员侧、管理员侧和导出/异步作业边界拆成更小任务
    - `WORKLOG.md` 记录拆分原因、当前匿名旧契约范围和后续收口顺序

- [x] 收口成员侧业务 API 身份上下文与可见范围
  - Done when:
    - 成员侧查询、确认和材料可见性相关接口统一消费请求身份上下文
    - bearer 身份与显式 `actor_id` / `member_id` / `submitter_id` 不一致时显式拒绝
    - 成员只能访问本人相关材料、费用和确认记录

- [x] 收口管理员侧任务管理与复核权限
  - Done when:
    - 任务详情、成员管理、状态流转、提醒和复核摘要等管理员接口统一消费请求身份上下文
    - 非任务负责人不能管理无关任务
    - 匿名或身份不一致请求不能伪装管理员执行任务管理操作

- [x] 收口导出与异步作业权限边界
  - Done when:
    - 导出任务、导出文件下载、识别/导出异步作业管理接口统一消费请求身份上下文
    - 管理员只能管理自己负责任务的导出和相关异步作业
    - 为后续“增加导出文件访问控制测试”提供稳定前置边界

- [x] 增加导出文件访问控制测试
  - Done when:
    - 成员不能访问无关导出文件
    - 管理员能访问所负责任务的导出文件
    - 未实现真实导出时有明确边界测试

- [x] 增加 API 错误响应格式约束
  - Done when:
    - 常见 400、403、404、409 错误有一致响应结构
    - 现有测试更新为校验错误语义而非脆弱文本
    - 不吞掉真实异常

- [x] 增加审计日志模型骨架
  - Done when:
    - 关键操作可记录操作者、对象、动作、时间和结果
    - 不记录 secret 或完整敏感材料内容
    - 有最小单元测试

- [x] 记录材料提交和待归属认领审计
  - Done when:
    - 材料提交和待归属认领写入审计日志
    - 审计日志包含成功结果或失败原因
    - 测试覆盖至少一种成功操作

- [x] 建立材料删除标记边界
  - Done when:
    - 材料删除标记有明确领域语义和最小 API 或服务边界
    - 删除标记后的可见性和权限规则有测试或明确记录
    - 不直接物理删除原始材料和审计历史

- [x] 记录材料删除标记审计
  - Done when:
    - 材料删除标记写入审计日志
    - 审计日志包含操作者、目标材料和结果
    - 测试覆盖至少一种成功操作

- [x] 记录识别和人工更正审计
  - Done when:
    - 识别结果生成和人工更正字段写入审计日志
    - 更正记录包含修改前后差异摘要
    - 不记录完整敏感文件内容

- [x] 记录分摊和确认审计
  - Done when:
    - 分摊金额变更、成员确认和异议写入审计日志
    - 管理员处理异议或代确认路径若存在必须审计
    - 测试覆盖分摊变更

- [x] 记录导出和下载审计
  - Done when:
    - 导出任务创建、成功、失败和下载写入审计日志
    - 失败日志包含导出类型和失败原因
    - 不记录完整文件 URL 或长期访问凭证

- [x] 增加敏感信息日志脱敏规则
  - Done when:
    - Token、OAuth 密钥、Telegram Bot Token、邮件密码不会出现在日志
    - 文件 URL 或本地路径按规则脱敏
    - 测试或静态检查覆盖关键日志辅助函数

- [x] 建立请求 ID 日志上下文
  - Done when:
    - API 请求可生成或透传 request_id
    - 错误日志包含 request_id
    - 不要求接入完整 APM

- [x] 增加基础指标边界
  - Done when:
    - 上传成功率、识别任务状态、校验失败类型、导出任务状态有可扩展指标边界
    - 不引入不必要的新监控依赖
    - WORKLOG 记录当前取舍

- [x] 增加备份和恢复策略说明
  - Done when:
    - 文档记录数据库、对象存储和原始材料备份策略建议
    - 明确上线前需要恢复演练
    - 不实现真实备份任务

- [x] 执行数据库与对象存储备份恢复演练
  - Done when:
    - 已按当前部署基线执行一次数据库备份、恢复和结果核对
    - 已按当前对象存储基线执行一次原始材料或导出产物备份、恢复和可读性核对
    - `WORKLOG.md` 记录演练命令、结果、未覆盖风险和后续改进点

## P3 - 测试、质量与端到端验收

- [x] 增加规则层单元测试覆盖矩阵
  - Done when:
    - 抬头税号、大额支付记录、附件完整性、比赛范围、重复发票规则有测试矩阵
    - 每条规则覆盖通过、失败、待确认路径
    - 不通过删减断言制造通过

- [x] 增加权限越权回归测试
  - Done when:
    - 成员不能访问无关材料、费用、确认记录和导出
    - 非管理员不能进入管理员复核路径
    - 测试覆盖 403 而不是只覆盖 404

- [x] 增加材料上传集成测试
  - Done when:
    - 覆盖文件保存、hash、重复检测、批量部分成功
    - 覆盖不同渠道进入统一流程
    - 不依赖外部对象存储

- [x] 增加导出任务集成测试
  - Done when:
    - 覆盖导出任务创建、状态变化、失败原因
    - 覆盖损坏 PDF 报告材料编号
    - 不要求生成真实财务可用材料

- [x] 增加 CLI 参数解析测试
  - Done when:
    - 覆盖 login、list-tasks、submit、status、missing、split、confirm 命令
    - 覆盖 `--json` 输出
    - 覆盖本地文件预检查失败

- [x] 建立主流程 E2E 测试骨架
  - Done when:
    - 测试覆盖创建任务、开放、提交材料、录入发票、校验、分摊、确认、复核、导出占位
    - 可在本地 `./scripts/verify.sh` 中运行
    - 不接入真实外部 AI、Telegram、邮件或对象存储

- [x] 增加上线前安全回归验证
  - Done when:
    - 已完成权限越权、导出下载、日志脱敏、CORS 配置和生产注册策略的回归验证
    - 相关验证入口已纳入现有测试或验证脚本，并在 `WORKLOG.md` 记录执行结果
    - 不把未覆盖的外部依赖或人工步骤伪装成自动化通过

- [x] 执行上线前主流程 E2E 演练并记录风险
  - Done when:
    - 已执行主流程 E2E：创建任务、提交材料、真实或 fake LLM 识别、校验、分摊、确认、复核、导出
    - `WORKLOG.md` 明确记录仍未验证的外部依赖和上线风险
    - 不把 Telegram、邮件、真实 OCR 或 Browser Use 的未接入能力伪装为已演练

- [x] 更新 README 的第一阶段运行说明
  - Done when:
    - README 记录后端启动、测试、CLI 占位、当前未实现外部依赖
    - 不夸大未实现功能
    - 与 `TASKS.md` 当前阶段一致

## P4 - Could 与后续增强评估

- [x] 评估历史成员信息复用
  - Done when:
    - 记录是否进入第一阶段或后续阶段
    - 若进入第一阶段，拆出成员模板任务
    - 不直接实现

- [x] 评估常见比赛模板
  - Done when:
    - 记录模板包含字段和使用场景
    - 明确是否会污染当前任务创建主路径
    - 不直接实现

- [x] 评估 CLI 离线暂存后同步
  - Done when:
    - 记录离线暂存的数据安全风险和同步冲突风险
    - 明确是否推迟到后续阶段
    - 不直接实现

- [x] 实现 CLI 目录递归上传
  - Done when:
    - `trms-cli submit` 支持显式目录参数的递归展开，并保持现有逐文件结果语义
    - 递归阶段的本地失败可并入现有 `partial_success` / `failed` 输出
    - 明确并测试目录遍历顺序、是否跟随符号链接以及对不支持文件类型的处理

- [x] 评估自动生成成员补材料消息
  - Done when:
    - 记录消息模板、渠道和审计要求
    - 明确是否依赖通知模块
    - 不直接实现

- [ ] 评估更复杂财务规则引擎
  - Done when:
    - 记录当前规则模块何时需要迁移到配置化或 DSL
    - 明确迁移触发条件
    - 不引入新依赖

- [ ] 评估 Browser Use 后续阶段方案
  - Done when:
    - 记录 Browser Use 自动填报的风险、人工确认点和退出条件
    - 明确第一阶段仍不实现
    - 不保存财务系统登录态

## P5 - 前端 Material 3 重写

本批任务将 Web 前端整体迁移到 Material Design 3 设计体系，并同步优化用户交互逻辑（任务驱动总览、列表-详情联动、单任务闭环、统一反馈）。每条都是单轮可验证的最小任务。

- [x] 评估并确认 Material 3 React 落地方案
  - Done when:
    - `WORKLOG.md` 记录最终采用的 M3 库（MUI v7 / sixui / 其它）和拒绝其它方案的理由
    - 记录引入新依赖的范围、bundle 体积影响和测试改动面预估
    - 不实际安装任何依赖，不改动业务代码

- [x] 引入 Material 3 主题与基线依赖
  - Done when:
    - `web/` 引入 MUI v7（`@mui/material`、`@emotion/react`、`@emotion/styled`、`@mui/icons-material`）
    - 建立 `src/theme/m3-theme.ts`，含完整 M3 颜色 token、字体、形状、tonal elevation
    - `main.tsx` 包裹 ThemeProvider + CssBaseline，支持亮/暗模式跟随系统
    - 现有 `styles.css` 暂时保留，后续按页迁移
    - `npm run lint` / `npm test` / `npm run build` 全部通过

- [x] 重构应用骨架：Top App Bar + Navigation Rail + Snackbar 容器
  - Done when:
    - `RootLayout` 改为 M3 Top App Bar（左：菜单 + 品牌；右：账号头像菜单）
    - 桌面端使用 Navigation Rail，移动端使用 Bottom Navigation
    - 全局加入 SnackbarProvider 与可全局调用的 `useSnackbar` hook
    - 顶部菜单含主题切换（亮/暗）、退出登录、角色切换
    - 既有路由全部沿用，对应 RootLayout 测试同步更新

- [x] 重写登录/注册页交互
  - Done when:
    - 登录与注册分两个 Tab（M3 Tabs 或 SegmentedButton）
    - 已登录态不再渲染表单，改为显示已登录卡片 + 进入入口
    - 开发 mock 入口收口为页内可折叠区域，仅 dev 环境显示
    - 错误提示统一用 Snackbar，不再用页面级红色卡片
    - `auth.tsx` 与登录相关测试同步更新

- [x] 重写首页：任务驱动总览
  - Done when:
    - 移除"页面边界已收口/可见板块/页面边界"等实现性卡片
    - 成员首页：KPI 卡（待提交、待确认、已归档）+ "继续上次任务" + 任务列表
    - 管理员首页：KPI 卡（异常材料、未确认成员、待审核任务、待导出）+ "今日最紧急任务" + 全部任务表
    - 首页相关测试同步更新

  > 实现说明：当前阶段成员首页 KPI 与全部任务表已经在 `/member` 任务列表页（Round 6 范围）；管理员首页同样位于 `/admin` 任务列表（已存在）。本轮 `/` 总览页只负责把"未登录"和"已登录后选择工作台"的体验改为 M3 任务驱动卡片视图，不重复实现 KPI 数据计算；KPI 重写在对应角色任务列表轮次完成。

- [x] 重写成员端任务列表（M3 Material 视觉）
  - Done when:
    - 任务列表 `/member` 改为 MUI v7 Stack + KPI Card 网格 + Card 包裹的 MUI Table，移动端可用
    - 状态使用 MUI Chip 颜色映射，下一步动作和"进入工作台"按钮保持现有路由语义不变
    - 加载、空态、错误三种状态都有清晰的 M3 视觉
    - `member-task-list.test.tsx` 全部通过，断言语义不弱化

- [x] 重写成员单任务工作台（Tabs 化）
  - Done when:
    - `/member/invoices/workbench` 主体框架改为 M3 Tabs（发票 / 缺失材料 / 费用确认）
    - 顶部展示任务上下文卡（比赛名、状态、截止时间、当前成员），并支持任务切换
    - 列表项与详情仍保持现有业务行为，本轮不改后端接口
    - 现有 `member-invoice-workbench.test.tsx` 测试通过或同步更新

- [x] 让 dashboard 公共组件视觉对齐 MUI Material 3
  - Done when:
    - `web/src/components/dashboard.tsx` 中 `PageHeader` / `SectionCard` / `StatCard` / `EmptyState` / `ErrorMessage` / `RoleWorkspace` / `TaskTable` / `StatusBadge` 内部全部改为基于 MUI v7 实现
    - 对外 API（props 名、children、heading 角色、aria-label）保持不变，不破坏现有 30+ 个测试和业务页面
    - 视觉与新 M3 主题对齐：surface、elevation、Chip 状态色、Card 圆角、Typography scale
    - `npm run lint` / `npm test` / `npm run build` 全部通过

- [x] 重写成员材料上传交互（拖拽 Dialog + Snackbar）
  - Done when:
    - `/member/materials/upload` 改为 M3 拖拽上传卡 + 单文件状态显示
    - 上传成功/失败反馈通过 Snackbar 推送，不再用页面级红色卡片
    - 上传结果保留逐文件结果显示
    - `member-material-upload.test.tsx` 测试通过

- [x] 重写成员材料状态页（M3 列表 + 详情视图）
  - Done when:
    - `/member/materials/status` 改为 M3 列表 + 详情或 Card 网格视图
    - 识别状态、校验异常、缺失材料用统一 Chip 颜色表达
    - 重新识别和状态刷新动作走 Snackbar 反馈
    - `member-material-status.test.tsx` 测试通过

- [x] 重写成员费用确认页与缺失材料页
  - Done when:
    - `/member/expenses/confirm` 改为 M3 表单 + Stepper 或分卡组织
    - `/member/materials/missing` 改为 M3 列表
    - 确认与异议提交走 Snackbar 反馈
    - 相关测试通过

- [x] 收口成员端旧二级路由为工作台跳转
  - Done when:
    - `/member/materials/upload` `status` `missing` `expenses/confirm` 在工作台 Tabs 完成后改为重定向到 `/member/invoices/workbench` 对应 tab
    - 不破坏现有外部链接（旧 URL 仍可访问，自动跳转）
    - 相关测试更新

- [x] 重写管理员侧 workspace shell（Material 3 化导航与任务上下文）
  - Done when:
    - `web/src/app/admin-workspace-shell.tsx` 改为基于 MUI 的 List + ListItemButton + Card 实现
    - 模块导航使用 ListItemIcon + selected 高亮 + 图标，每项有标题与说明
    - 当前任务上下文卡用 Card + Stack + Chip + dl/dt/dd 网格 + 快捷入口按钮组
    - aria-label "管理员模块导航"、"当前任务上下文" 与 "当前任务快捷入口" 全部保留
    - 现有 admin-task-list / admin-task-detail / admin-export-tasks / admin-review-overview 等测试通过

- [ ] 完善管理员任务详情：列表+详情联动深度优化
  - Done when:
    - `/admin/tasks/:id/review` 与 `/admin/tasks/:id/invoices` 在已是列表+详情的基础上，进一步用 M3 Tabs 区分识别字段、校验异常、附件预览
    - 单张发票预览、识别字段、校验异常、处理动作集中在右侧详情面板
    - 相关测试更新

- [x] 准备 Material 3 表单与上传基础组件
  - Done when:
    - 登录/注册页已采用 MUI TextField / Select（Round 4 完成）
    - dashboard 公共组件 `ErrorMessage` 改用 MUI Alert，错误展示语义统一
    - 新增 `web/src/components/FileDropZone.tsx`：M3 拖拽上传卡，支持点击选择、拖拽、单文件移除、文件类型图标、可选 hint，对外暴露 `files`、`onChange`、`accept`、`ariaLabel`、`fileListAriaLabel` 等 props，后续业务页面可直接消费
    - `npm run lint` / `npm test` / `npm run build` 全部通过

- [ ] 把成员/管理员业务表单整体迁移到 MUI TextField / Select / Autocomplete
  - Done when:
    - 任务创建表单、发票录入表单、分摊编辑表单、缺失材料筛选表单等使用 M3 TextField / Select / Autocomplete
    - Form 校验错误使用 helperText / error，不再使用自造 `.field-error`
    - 相关测试同步更新

- [ ] 把材料上传场景接入 FileDropZone
  - Done when:
    - `/member/materials/upload`、`/member/invoices/workbench` 上传区使用 FileDropZone
    - 上传中通过 LinearProgress 表达进度，结果通过 Snackbar 反馈
    - 相关测试同步更新

- [x] 引入 ConfirmDialog 全局基础设施
  - Done when:
    - 新增 `web/src/components/ConfirmDialog.tsx` 提供 `ConfirmDialogProvider`
    - 新增 `useConfirmDialog()` hook，返回 `confirm(options): Promise<boolean>`
    - 支持 `tone`、`destructive`、`requireTyping`、`confirmLabel`、`cancelLabel` 等 M3 选项
    - `RootLayout` 包裹 `ConfirmDialogProvider`，业务页面无需改动即可使用

- [ ] 把破坏性业务动作接入 ConfirmDialog
  - Done when:
    - 任务状态流转、删除、代确认、强制导出等动作前调用 `useConfirmDialog().confirm(...)`
    - 取消时不发起请求，确认时附带操作上下文记录
    - 测试覆盖确认与取消路径

- [x] 清理 `styles.css` 中与 MUI 主题冲突的全局 token
  - Done when:
    - 删除两个冲突的 `:root` 块、body 强制颜色与背景
    - 删除已被 MUI AppShell 完全替代的 `.topbar*`、`.workspace-shell`、`.workspace-header`、`.workspace-hero*`、`.workspace-nav*`、`.brand-mark`、`.session-pill` 等旧顶栏/Hero 类
    - 由 MUI CssBaseline 统一接管 body 字体、背景、字色
    - `npm run lint` / `npm run build` / `npm test` 全部通过

- [ ] 进一步收缩 `styles.css` 死代码
  - Done when:
    - 排查并删除 `.workflow-*`、`.kpi-*`、`.dashboard-grid`、`.workspace-meta-grid`、`.task-insight*`、`.task-stage-line`、`.anomaly-chip*`、`.task-workflow*` 等当前 .tsx 中未被引用的死类
    - 保留仍被业务页 `<article className="task-card">` 等引用的辅助类
    - `npm run lint` / `npm run build` / `npm test` 全部通过
