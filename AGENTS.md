# AGENTS.md

本文件是本仓库的项目级 Codex 工作规范。后续代理应优先遵守本文件；若更深层目录存在额外 `AGENTS.md`，以更深层文件为准。

## 工作语言

- 与用户沟通、文档、总结、工作日志默认使用中文。
- Git commit message 使用英文，格式为 `<type>: <message>`，例如 `fix: handle empty material upload`。

## 每轮工作方式

- 每轮只完成 `TASKS.md` 中一个最小可验证任务。
- 修改前必须阅读：
  - `AGENTS.md`
  - `TASKS.md`
  - `WORKLOG.md`
  - `BLOCKERS.md`
  - `README.md`
  - `docs/` 下的需求和架构文档
- 优先选择 `TASKS.md` 中第一个未完成且未被 `BLOCKERS.md` 阻塞的任务。
- 不要一次性重写大块架构。
- 不要做与当前任务无关的重构。
- 不要引入不必要的新依赖；确需新增依赖时，必须说明原因和影响范围。
- 如果任务过大，先拆分 `TASKS.md`，不要直接实现。
- 如果遇到不明确需求，做保守假设并记录到 `WORKLOG.md`。
- 如果不明确需求会阻塞实现，记录到 `BLOCKERS.md` 并切换到下一个未阻塞任务。
- 夜间无人值守场景下不要等待用户交互；无法继续时应记录阻塞并结束本轮。

## 修改约束

- 不删除用户已有内容。
- 不改动与当前任务无关的业务代码。
- 不把 secret、token、API key 或本地敏感配置写入仓库。
- 不通过删除、跳过、弱化测试来制造通过。
- 如果发现已有未提交改动，必须先判断是否与当前任务相关；无关改动不得回滚。

## 验证规则

- 每轮修改后必须运行：

```bash
./scripts/verify.sh
```

- 如果验证失败，优先修复本轮改动引入的问题。
- 如果无法修复，必须把失败原因记录到 `WORKLOG.md` 和 `BLOCKERS.md`。
- 不要留下未解释的 dirty working tree。
- 未实际运行的验证不得声称已经通过。

## 完成规则

- 完成任务后更新 `TASKS.md`。
- 更新 `WORKLOG.md`，记录完成内容、修改文件、验证结果、假设和后续建议。
- 如果验证通过，执行 `git add` 和 `git commit`。
- 每次 commit 只对应一个最小任务。
- 如果当前环境无法提交，必须在 `WORKLOG.md` 中记录原因。
