# UX 测试报告

## 1. 测试范围

- 角色：
  - 普通队员：注册、查看任务、进入工作台、上传真实发票/附件、查看共享发票、理解错误提示。
  - 管理员：注册、创建报销任务、切换任务状态、查看复核页、记录补材料提醒、查看导出页。
  - 财务/最终审核视角：以管理员导出页和复核页为主，检查汇总信息是否足够提交财务。
- 流程：
  - 首页与登录注册
  - 管理员创建比赛报销任务并开放收集
  - 两名队员分别上传真实材料
  - 管理员查看复核、提醒、导出
  - 窄屏移动端导出页检查
- 本轮实际通过 Playwright MCP 无头浏览器执行，不是静态阅读代码。

## 2. 测试环境

- 启动方式：
  - 后端：`uv run python -m trms_backend --host 127.0.0.1 --port 9877`
  - 前端：`cd web && VITE_API_BASE_URL=http://127.0.0.1:9877/api npm run dev -- --host 127.0.0.1 --port 4173`
- 前端地址：`http://127.0.0.1:4173`
- 后端地址：`http://127.0.0.1:9877`
- 浏览器：Playwright MCP，headless
- 数据库：隔离 SQLite `tmp/ux-runtime/ux-test.db`
- 材料目录：`tmp/ux-runtime/materials`
- Trace：
  - 当前 Playwright MCP 工具链未提供可落盘 trace 导出，本轮未生成 Playwright trace。
  - 浏览器中间快照保存在 `.playwright-mcp/`，该目录仅作本地调试，不纳入仓库。

## 3. 真实数据来源

- 原始目录：`/home/gsh/财务/ICPC区域赛/区域赛报销`
- 本轮复制到：`tmp/ux-real-data/`
- 实际使用的数据类型：
  - 武汉区域赛报名费电子发票 PDF
  - 武汉邀请函 PDF
  - 沈阳网约车电子发票 PDF
  - 沈阳行程图片 PNG
  - 沈阳 `.ofd` 文件仅用于兼容性检查，未直接上传
- 敏感信息处理：
  - 报告中不记录完整发票号码、完整税号、完整票据抬头明细页、完整个人证件/联系方式。
  - 仅保留比赛城市、材料类型、文件体积、可见业务字段。

## 4. 总体结论

当前系统可以启动，也能完成基础登录、建任务、上传材料和进入复核页，但还**不适合真实用户直接投入比赛报销使用**。

最大的阻塞问题是：**核心识别链路在当前环境下没有真正产出可用发票结构化数据，导致成员无法自然进入“分摊-确认”，管理员也无法得到可靠的复核与财务导出基础。**

本轮同时确认了若干明显 UX 缺陷：共享发票可见性未按产品要求生效、成员附件上传与发票归属关系不清、任务创建仍要求管理员理解“成员标识”而不是自然选择成员。

本轮已做 3 个低风险修复：

- 导出页英文/技术化文案改为中文业务文案。
- 中文文件名材料预览导致的 `Content-Disposition` 崩溃已修复，管理员复核页可以正常预览。
- 超大文件上传失败提示已从泛化报错改为“上传文件过大”。

## 5. 关键 UX 问题列表

### UX-001：识别链路未产出可用结构化结果，后续分摊与确认无法自然发生

- 严重程度：Blocker
- 用户角色：普通队员 / 管理员 / 财务
- 所在页面：
  - 成员工作台 `/member/invoices/workbench`
  - 管理员复核页 `/admin/tasks/:id/review`
  - 导出页 `/admin/tasks/:id/exports`
- 复现步骤：
  1. 管理员创建并开放一个真实报销任务。
  2. 队员上传真实 PDF 发票和 PNG 行程材料。
  3. 返回成员工作台和管理员复核页查看识别结果。
- 实际表现：
  - 成员端显示“暂无识别记录”。
  - 管理员端显示“识别处理中”，但没有结构化字段、没有发票主记录、没有可继续的自然业务动作。
  - 导出页仍然没有任何金额、发票字段或财务草稿可核对。
- 期望表现：
  - 上传后应明确给出识别结果，或明确提示“当前环境未配置识别服务，后续功能不可用”。
- 用户影响：
  - 成员不知道下一步是否该等待、补录还是联系管理员。
  - 管理员无法进入有效复核。
  - 财务看不到可提交数据。
- 建议修复：
  - 前端显式区分“识别服务未配置”“识别排队中”“识别失败”三种状态。
  - 若当前环境未配置识别能力，应在上传完成后直接给出清晰的阻塞说明和补救路径。
- 相关截图：
  - [member-workbench-empty.png](test-artifacts/ux/member-workbench-empty.png)
  - [admin-review-page.png](test-artifacts/ux/admin-review-page.png)

### UX-002：同任务成员共享发票视图未按产品要求生效

- 严重程度：High
- 用户角色：普通队员
- 所在页面：成员工作台“任务内其他成员已上传发票”
- 复现步骤：
  1. 成员 A 上传真实武汉报名费发票。
  2. 成员 B 上传真实沈阳网约车电子发票。
  3. 成员 A 或 B 重新进入工作台底部“任务内其他成员已上传发票”区域。
- 实际表现：
  - 页面仍显示“0 张”或“当前任务里还没有其他成员上传可共享查看的发票摘要”。
- 期望表现：
  - 同一比赛任务成员之间应至少能看到其他成员已上传发票的基础元数据和当前分摊去向。
- 用户影响：
  - 团队成员无法判断公共费用是否已有人提交，也无法避免重复上传。
- 建议修复：
  - 修正共享发票聚合逻辑，至少在成员上传后立即刷新并展示摘要。
- 相关截图：
  - [member-workbench-empty.png](test-artifacts/ux/member-workbench-empty.png)

### UX-003：附件上传后没有自然归属到发票，用户无法表达“这份行程单是给哪张发票的”

- 严重程度：High
- 用户角色：普通队员 / 管理员
- 所在页面：成员工作台、管理员复核页
- 复现步骤：
  1. 队员先上传真实发票 PDF。
  2. 再上传真实 PNG 行程单，材料类型选“行程单”。
  3. 查看发票卡片中的“关联附件与缺失项”。
- 实际表现：
  - 行程单被单独记录成另一份材料。
  - 发票卡片仍显示“附件 0 份 / 缺失 0 项”。
  - 页面没有一个自然入口让用户把这份附件挂到那张发票上。
- 期望表现：
  - 用户应能直观地把附件关联到已有发票，至少支持“选择归属发票”。
- 用户影响：
  - 队员会误以为附件已经补上，管理员却仍看不到关联关系。
- 建议修复：
  - 在成员工作台上传附件后，立即要求或引导用户选择关联发票。
  - 发票详情中增加“添加现有附件”入口。
- 相关截图：
  - [member-workbench-empty.png](test-artifacts/ux/member-workbench-empty.png)
  - [admin-review-page.png](test-artifacts/ux/admin-review-page.png)

### UX-004：任务创建仍要求管理员直接理解“成员标识”，不适合真实名单录入

- 严重程度：High
- 用户角色：管理员
- 所在页面：创建报销任务
- 复现步骤：
  1. 管理员进入“创建报销任务”。
  2. 根据真实邀请函尝试录入成员。
- 实际表现：
  - 表单字段是“成员 1 / 成员 2”，占位文案是“输入学号或成员标识”。
  - 没有姓名/学号双轨提示、没有批量导入、没有从真实名单中选择的入口。
- 期望表现：
  - 至少明确说明这里到底填“姓名”“学号”还是“系统内成员 ID”。
  - 更理想的是支持粘贴名单、模板导入或历史成员选择。
- 用户影响：
  - 管理员必须理解系统内部身份字段，录错后还会直接影响成员是否能看到任务。
- 建议修复：
  - 把“成员标识”改成更贴近业务的“成员姓名/学号”双提示。
  - 后续补齐批量导入或历史成员补全。
- 相关截图：
  - [admin-task-create-form.png](test-artifacts/ux/admin-task-create-form.png)

### UX-005：工作台把“无异常”展示得过于乐观，容易误导成员以为已经完成

- 严重程度：Medium
- 用户角色：普通队员
- 所在页面：成员工作台“待处理事项”
- 复现步骤：
  1. 上传真实发票后重新进入工作台。
  2. 观察顶部“待处理事项”和列表区。
- 实际表现：
  - 在识别未完成、附件未关联、金额未生成时，页面仍显示“当前无明显异常”。
- 期望表现：
  - 至少提示“识别尚未完成，暂时还不能确认金额”或“附件还未归属到发票”。
- 用户影响：
  - 普通队员很容易误以为自己已经提交完毕，不再跟进。
- 建议修复：
  - “无异常”只应在识别、关联、分摊、确认都完成后出现。
  - 当前阶段更适合展示“仍待系统识别/管理员处理”。
- 相关截图：
  - [member-workbench-empty.png](test-artifacts/ux/member-workbench-empty.png)

### UX-006：导出页直接向用户暴露英文和内部状态词

- 严重程度：Medium
- 用户角色：管理员 / 财务
- 所在页面：导出页
- 复现步骤：
  1. 管理员进入导出页。
  2. 查看顶部说明和阻塞原因。
- 实际表现：
  - 原页面出现英文说明和内部状态词，例如 `ready_to_export`、英文导出说明等。
- 期望表现：
  - 导出页应只展示中文业务文案，例如“当前任务还未满足导出前置条件”。
- 用户影响：
  - 财务和管理员会被迫理解系统内部实现状态。
- 建议修复：
  - 已在本轮修复：导出页顶部说明、阻塞原因和区块标题已改成中文业务文案。
- 相关截图：
  - [admin-export-page.png](test-artifacts/ux/admin-export-page.png)

### UX-007：中文文件名材料预览会被伪装成“网络连接异常”

- 严重程度：High
- 用户角色：管理员
- 所在页面：管理员复核页“原始票据预览”
- 复现步骤：
  1. 上传中文文件名的真实 PDF。
  2. 管理员进入复核页查看预览。
- 实际表现：
  - 原先页面显示“网络连接异常”，但后端实际根因是 `Content-Disposition` 无法编码中文文件名。
- 期望表现：
  - 中文文件名材料也应正常预览。
- 用户影响：
  - 管理员会误判为网络问题，无法定位真正原因。
- 建议修复：
  - 已在本轮修复：后端改为对非 Latin-1 文件名生成安全的 `filename*` 响应头。
- 相关截图：
  - [admin-review-page.png](test-artifacts/ux/admin-review-page.png)

### UX-008：超大真实 PDF 上传失败前端原先只给泛化错误，不告诉用户是“文件过大”

- 严重程度：Medium
- 用户角色：普通队员
- 所在页面：成员工作台上传区
- 复现步骤：
  1. 选择真实武汉邀请函 PDF（约 14.6 MB）。
  2. 上传到当前任务。
- 实际表现：
  - 原先只显示“当前操作未完成，请检查填写内容后重试。”
- 期望表现：
  - 明确提示“上传文件过大，请缩小到页面允许的大小后重试。”
- 用户影响：
  - 用户不知道该压缩文件、拆分文件，还是调整材料类型。
- 建议修复：
  - 已在本轮修复：前端补充 413 大文件错误映射。
  - 后续仍建议在上传前就做体积校验，并把限制贴近文件选择区显示。
- 相关截图：
  - [member-workbench-empty.png](test-artifacts/ux/member-workbench-empty.png)

### UX-009：手动提醒页面实际只是“记一条记录”，不是真正提醒

- 严重程度：Medium
- 用户角色：管理员
- 所在页面：管理员人工更正与补材料提醒
- 复现步骤：
  1. 管理员进入更正与提醒页。
  2. 选择成员并填写提醒内容。
  3. 点击“记录补材料提醒”。
- 实际表现：
  - 页面可以成功记录，但不会真的通知成员。
  - “只记录、不发送”的说明文字存在，但容易被忽略。
- 期望表现：
  - 若当前只做记录，应更显眼地写成“保存内部提醒记录，不会发送消息”。
- 用户影响：
  - 管理员可能误以为已经通知到成员。
- 建议修复：
  - 将“不会发送真实通知”的边界提升到按钮附近或提交确认文案中。
- 相关截图：
  - [admin-review-page.png](test-artifacts/ux/admin-review-page.png)

### UX-010：窄屏下导出页信息密度过高，模块纵向过长

- 严重程度：Low
- 用户角色：管理员 / 财务
- 所在页面：导出页窄屏
- 复现步骤：
  1. 使用约 `390x844` 视口打开导出页。
  2. 从顶部浏览到导出卡片区域。
- 实际表现：
  - 信息块堆叠很长，关键状态、阻塞原因和导出按钮被拉得很分散。
  - 底部导航和左侧导航切换后，主信息区域仍显得过密。
- 期望表现：
  - 移动端应先突出当前状态、阻塞原因和最常用导出入口。
- 用户影响：
  - 在手机上临时查看导出准备度时，扫描成本偏高。
- 建议修复：
  - 移动端将导出能力折叠为分组列表，默认先展示“当前可做 / 当前阻塞”。
- 相关截图：
  - [admin-export-mobile.png](test-artifacts/ux/admin-export-mobile.png)

## 6. 按角色的流程评价

### 普通队员流程

队员可以完成注册、看到自己参与的任务、进入工作台并上传真实材料，但**不能独立完成一轮真实报销闭环**。主要原因不是“页面打不开”，而是：

- 核心识别结果没有真正落到可确认字段上。
- 上传附件后没有自然归属到发票。
- 页面过早告诉用户“当前无明显异常”，容易让人误判已经完成。
- 共享发票能力没有按产品要求生效。

### 管理员流程

管理员可以创建任务、开放提交、看到材料列表、记录提醒、进入导出页，但**仍无法顺畅完成复核和汇总**。主要问题是：

- 成员身份录入仍偏内部标识导向。
- 复核页虽然有列表+详情结构，但有效业务信息太少，主要还是“未识别、未录入、未分摊”。
- 导出页能展示能力卡片，但实际还拿不到足够财务信息。

### 财务流程

财务视角目前**不能直接使用系统输出数据提交财务**。原因包括：

- 没有稳定的发票主记录、费用类型、金额、分摊结果。
- 没有可靠的抬头、税号、附件完整性汇总。
- 导出页虽然列出报销汇总表/财务填报草稿等入口，但当前任务状态和数据完整度都不足以支撑真实提交。

## 7. 优先级修复建议

### P0

- 明确并打通识别链路状态：未配置、排队中、失败、已完成必须清晰区分。
- 修正共享发票摘要逻辑，确保同任务成员能看到其他成员已上传发票的基础信息。
- 为附件建立自然的发票归属入口，否则真实“发票+支付记录/行程单”流程无法成立。

### P1

- 任务创建页改成更贴近业务的成员录入方式，至少明确填“姓名/学号”而不是模糊的“成员标识”。
- 成员工作台不要在“未识别/未归属/未分摊”时显示“当前无明显异常”。
- 提醒页显著声明“当前只记录，不会真实发送通知”。

### P2

- 继续清理英文和内部实现词。
- 优化导出页移动端密度和信息优先级。
- 上传区增加前置体积校验、批量混合材料引导和更明确的下一步提示。

## 8. 可复现测试说明

- 实际浏览器实测使用 Playwright MCP 完成。
- 项目内保存了一个最小 Playwright 脚本基线：
  - [tests/ux/real-user-flows.spec.mjs](tests/ux/real-user-flows.spec.mjs)
  - [tests/ux/README.md](tests/ux/README.md)
- 建议复跑方式：

```bash
npx -y playwright@1.59.1 test tests/ux/real-user-flows.spec.mjs
```

- 运行前需要先准备：

```bash
mkdir -p tmp/ux-real-data tmp/ux-runtime/materials test-artifacts/ux
# 然后按 README 或 tests/ux/README.md 中的方式启动隔离前后端实例
```

